(globalThis["TURBOPACK"] || (globalThis["TURBOPACK"] = [])).push([typeof document === "object" ? document.currentScript : undefined,
"[project]/node_modules/zbsearch/dist/browser/components.js [app-client] (ecmascript) <locals>", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([]);
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$components$2f$defaults$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__$3c$locals$3e$__ = __turbopack_context__.i("[project]/node_modules/zbsearch/dist/browser/components/defaults.js [app-client] (ecmascript) <locals>");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$components$2f$documents$2d$store$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/zbsearch/dist/browser/components/documents-store.js [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$components$2f$index$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/zbsearch/dist/browser/components/index.js [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$components$2f$tokenizer$2f$index$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/zbsearch/dist/browser/components/tokenizer/index.js [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$components$2f$sorter$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/zbsearch/dist/browser/components/sorter.js [app-client] (ecmascript)");
;
;
;
;
;
;
}),
"[project]/node_modules/zbsearch/dist/browser/components/algorithms.js [app-client] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "BM25",
    ()=>BM25,
    "prioritizeTokenScores",
    ()=>prioritizeTokenScores
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$errors$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/zbsearch/dist/browser/errors.js [app-client] (ecmascript)");
;
function prioritizeTokenScores(arrays, boost, threshold = 0, keywordsCount) {
    if (boost === 0) {
        throw (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$errors$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["createError"])("INVALID_BOOST_VALUE");
    }
    const tokenScoresMap = new Map();
    const mapsLength = arrays.length;
    for(let i = 0; i < mapsLength; i++){
        const arr = arrays[i];
        const entriesLength = arr.length;
        for(let j = 0; j < entriesLength; j++){
            const [token, score] = arr[j];
            const boostScore = score * boost;
            const oldScore = tokenScoresMap.get(token)?.[0];
            if (oldScore !== undefined) {
                tokenScoresMap.set(token, [
                    oldScore * 1.5 + boostScore,
                    (tokenScoresMap?.get(token)?.[1] || 0) + 1
                ]);
            } else {
                tokenScoresMap.set(token, [
                    boostScore,
                    1
                ]);
            }
        }
    }
    const tokenScores = [];
    for (const tokenScoreEntry of tokenScoresMap.entries()){
        tokenScores.push([
            tokenScoreEntry[0],
            tokenScoreEntry[1][0]
        ]);
    }
    const results = tokenScores.sort((a, b)=>b[1] - a[1]);
    // If threshold is 1, it means we will return all the results with at least one search term,
    // prioritizing the ones that contains more search terms (fuzzy match)
    if (threshold === 1) {
        return results;
    }
    // For threshold = 0 when keywordsCount is 1 (single term search),
    // we return all matches since they automatically contain 100% of keywords
    if (threshold === 0 && keywordsCount === 1) {
        return results;
    }
    // Prepare keywords count tracking for threshold handling
    const allResults = results.length;
    const tokenScoreWithKeywordsCount = [];
    for (const tokenScoreEntry of tokenScoresMap.entries()){
        tokenScoreWithKeywordsCount.push([
            tokenScoreEntry[0],
            tokenScoreEntry[1][0],
            tokenScoreEntry[1][1]
        ]);
    }
    // Find the index of the last result with all keywords.
    // Order the documents by the number of keywords they contain, and then by the score.
    const keywordsPerToken = tokenScoreWithKeywordsCount.sort((a, b)=>{
        // Compare by the third element, higher numbers first
        if (a[2] > b[2]) return -1;
        if (a[2] < b[2]) return 1;
        // If the third elements are equal, compare by the second element, higher numbers first
        if (a[1] > b[1]) return -1;
        if (a[1] < b[1]) return 1;
        // If both the second and third elements are equal, consider the elements equal
        return 0;
    });
    let lastTokenWithAllKeywords = undefined;
    for(let i = 0; i < allResults; i++){
        if (keywordsPerToken[i][2] === keywordsCount) {
            lastTokenWithAllKeywords = i;
        } else {
            break;
        }
    }
    // If no results had all the keywords, either bail out earlier or normalize
    if (typeof lastTokenWithAllKeywords === "undefined") {
        if (threshold === 0) {
            return [];
        }
        lastTokenWithAllKeywords = 0;
    }
    const keywordsPerTokenLength = keywordsPerToken.length;
    const resultsWithIdAndScore = new Array(keywordsPerTokenLength);
    for(let i = 0; i < keywordsPerTokenLength; i++){
        resultsWithIdAndScore[i] = [
            keywordsPerToken[i][0],
            keywordsPerToken[i][1]
        ];
    }
    // If threshold is 0, it means we will only return all the results that contains ALL the search terms (exact match)
    if (threshold === 0) {
        return resultsWithIdAndScore.slice(0, lastTokenWithAllKeywords + 1);
    }
    const thresholdLength = lastTokenWithAllKeywords + Math.ceil(threshold * (allResults - lastTokenWithAllKeywords));
    return resultsWithIdAndScore.slice(0, Math.min(allResults, thresholdLength));
}
function BM25(tf, matchingCount, docsCount, fieldLength, averageFieldLength, { k, b, d }) {
    const idf = Math.log(1 + (docsCount - matchingCount + .5) / (matchingCount + .5));
    return idf * (d + tf * (k + 1)) / (tf + k * (1 - b + b * fieldLength / averageFieldLength));
}
}),
"[project]/node_modules/zbsearch/dist/browser/components/defaults.js [app-client] (ecmascript) <locals>", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "assertSchemaHasNoReservedKeys",
    ()=>assertSchemaHasNoReservedKeys,
    "formatElapsedTime",
    ()=>formatElapsedTime,
    "getDocumentIndexId",
    ()=>getDocumentIndexId,
    "getInnerType",
    ()=>getInnerType,
    "getVectorSize",
    ()=>getVectorSize,
    "isArrayType",
    ()=>isArrayType,
    "isGeoPointType",
    ()=>isGeoPointType,
    "isVectorType",
    ()=>isVectorType,
    "validateSchema",
    ()=>validateSchema
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$constants$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/zbsearch/dist/browser/constants.js [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$errors$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/zbsearch/dist/browser/errors.js [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$utils$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/zbsearch/dist/browser/utils.js [app-client] (ecmascript)");
;
;
;
;
function formatElapsedTime(n) {
    return {
        raw: Number(n),
        formatted: (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$utils$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["formatNanoseconds"])(n)
    };
}
function getDocumentIndexId(doc) {
    if (doc.id) {
        if (typeof doc.id !== "string") {
            throw (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$errors$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["createError"])("DOCUMENT_ID_MUST_BE_STRING", typeof doc.id);
        }
        return doc.id;
    }
    return (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$utils$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["uniqueId"])();
}
function assertSchemaHasNoReservedKeys(schema, prefix = "") {
    for (const [prop, type] of Object.entries(schema)){
        if (prop === __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$constants$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["RESERVED_VECTOR_INDEX_KEY"]) {
            throw (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$errors$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["createError"])("RESERVED_SCHEMA_PROPERTY", `${prefix}${prop}`);
        }
        if (typeof type === "object" && type !== null && !Array.isArray(type)) {
            assertSchemaHasNoReservedKeys(type, `${prefix}${prop}.`);
        }
    }
}
function validateSchema(doc, schema) {
    for (const [prop, type] of Object.entries(schema)){
        const value = doc[prop];
        if (typeof value === "undefined") {
            continue;
        }
        if (type === "geopoint" && typeof value === "object" && typeof value.lon === "number" && typeof value.lat === "number") {
            continue;
        }
        if (type === "enum" && (typeof value === "string" || typeof value === "number")) {
            continue;
        }
        if (type === "enum[]" && Array.isArray(value)) {
            const valueLength = value.length;
            for(let i = 0; i < valueLength; i++){
                if (typeof value[i] !== "string" && typeof value[i] !== "number") {
                    return prop + "." + i;
                }
            }
            continue;
        }
        if (isVectorType(type)) {
            const vectorSize = getVectorSize(type);
            if (!Array.isArray(value) || value.length !== vectorSize) {
                throw (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$errors$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["createError"])("INVALID_INPUT_VECTOR", prop, vectorSize, value.length);
            }
            continue;
        }
        if (isArrayType(type)) {
            if (!Array.isArray(value)) {
                return prop;
            }
            const expectedType = getInnerType(type);
            const valueLength = value.length;
            for(let i = 0; i < valueLength; i++){
                if (typeof value[i] !== expectedType) {
                    return prop + "." + i;
                }
            }
            continue;
        }
        if (typeof type === "object") {
            if (!value || typeof value !== "object") {
                return prop;
            }
            // using as ResultDocument is not exactly right but trying to be type-safe here is not useful
            const subProp = validateSchema(value, type);
            if (subProp) {
                return prop + "." + subProp;
            }
            continue;
        }
        if (typeof value !== type) {
            return prop;
        }
    }
    return undefined;
}
const IS_ARRAY_TYPE = {
    string: false,
    number: false,
    boolean: false,
    enum: false,
    geopoint: false,
    "string[]": true,
    "number[]": true,
    "boolean[]": true,
    "enum[]": true
};
const INNER_TYPE = {
    "string[]": "string",
    "number[]": "number",
    "boolean[]": "boolean",
    "enum[]": "enum"
};
function isGeoPointType(type) {
    return type === "geopoint";
}
function isVectorType(type) {
    return typeof type === "string" && /^vector\[\d+\]$/.test(type);
}
function isArrayType(type) {
    return typeof type === "string" && IS_ARRAY_TYPE[type];
}
function getInnerType(type) {
    return INNER_TYPE[type];
}
function getVectorSize(type) {
    const size = Number(type.slice(7, -1));
    switch(true){
        case isNaN(size):
            throw (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$errors$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["createError"])("INVALID_VECTOR_VALUE", type);
        case size <= 0:
            throw (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$errors$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["createError"])("INVALID_VECTOR_SIZE", type);
        default:
            return size;
    }
}
}),
"[project]/node_modules/zbsearch/dist/browser/components/documents-store.js [app-client] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "count",
    ()=>count,
    "create",
    ()=>create,
    "createDocumentsStore",
    ()=>createDocumentsStore,
    "get",
    ()=>get,
    "getAll",
    ()=>getAll,
    "getMultiple",
    ()=>getMultiple,
    "load",
    ()=>load,
    "remove",
    ()=>remove,
    "save",
    ()=>save,
    "store",
    ()=>store
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$components$2f$internal$2d$document$2d$id$2d$store$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/zbsearch/dist/browser/components/internal-document-id-store.js [app-client] (ecmascript)");
;
function create(_, sharedInternalDocumentStore) {
    return {
        sharedInternalDocumentStore,
        docs: {},
        count: 0
    };
}
function get(store, id) {
    const internalId = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$components$2f$internal$2d$document$2d$id$2d$store$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["getInternalDocumentId"])(store.sharedInternalDocumentStore, id);
    return store.docs[internalId];
}
function getMultiple(store, ids) {
    const idsLength = ids.length;
    const found = Array.from({
        length: idsLength
    });
    for(let i = 0; i < idsLength; i++){
        const internalId = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$components$2f$internal$2d$document$2d$id$2d$store$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["getInternalDocumentId"])(store.sharedInternalDocumentStore, ids[i]);
        found[i] = store.docs[internalId];
    }
    return found;
}
function getAll(store) {
    return store.docs;
}
function store(store, id, internalId, doc) {
    if (typeof store.docs[internalId] !== "undefined") {
        return false;
    }
    store.docs[internalId] = doc;
    store.count++;
    return true;
}
function remove(store, id) {
    const internalId = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$components$2f$internal$2d$document$2d$id$2d$store$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["getInternalDocumentId"])(store.sharedInternalDocumentStore, id);
    if (typeof store.docs[internalId] === "undefined") {
        return false;
    }
    delete store.docs[internalId];
    store.count--;
    return true;
}
function count(store) {
    return store.count;
}
function load(sharedInternalDocumentStore, raw) {
    const rawDocument = raw;
    return {
        docs: rawDocument.docs,
        count: rawDocument.count,
        sharedInternalDocumentStore
    };
}
function save(store) {
    return {
        docs: store.docs,
        count: store.count
    };
}
function createDocumentsStore() {
    return {
        create,
        get,
        getMultiple,
        getAll,
        store,
        remove,
        count,
        load,
        save
    };
}
}),
"[project]/node_modules/zbsearch/dist/browser/components/facets.js [app-client] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "getFacets",
    ()=>getFacets
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$errors$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/zbsearch/dist/browser/errors.js [app-client] (ecmascript)");
;
function sortAsc(a, b) {
    return a[1] - b[1];
}
function sortDesc(a, b) {
    return b[1] - a[1];
}
function sortingPredicateBuilder(order = "desc") {
    return order.toLowerCase() === "asc" ? sortAsc : sortDesc;
}
function getValueAtPath(doc, pathParts) {
    let value = doc;
    for(let i = 0; i < pathParts.length; i++){
        if (value === null || value === undefined || typeof value !== "object") {
            return undefined;
        }
        value = value[pathParts[i]];
    }
    return value;
}
function incrementNumberFacet(ranges, values, facetValue, alreadyInsertedValues) {
    for(let i = 0; i < ranges.length; i++){
        const range = ranges[i];
        if (alreadyInsertedValues?.has(range.key)) {
            continue;
        }
        if (facetValue >= range.from && facetValue <= range.to) {
            const current = values[range.key];
            if (current === undefined) {
                values[range.key] = 1;
            } else {
                values[range.key] = current + 1;
                alreadyInsertedValues?.add(range.key);
            }
        }
    }
}
function incrementBooleanStringOrEnumFacet(values, propertyType, facetValue, alreadyInsertedValues) {
    const defaultValue = propertyType === "boolean" ? "false" : "";
    const value = facetValue?.toString() ?? defaultValue;
    if (alreadyInsertedValues?.has(value)) {
        return;
    }
    const current = values[value];
    values[value] = current === undefined ? 1 : current + 1;
    alreadyInsertedValues?.add(value);
}
function prepareRangeValues(ranges) {
    const values = {};
    for(let i = 0; i < ranges.length; i++){
        const range = ranges[i];
        values[`${range.from}-${range.to}`] = 0;
    }
    return values;
}
function prepareRanges(ranges) {
    const prepared = Array.from({
        length: ranges.length
    });
    for(let i = 0; i < ranges.length; i++){
        const range = ranges[i];
        prepared[i] = {
            from: range.from,
            to: range.to,
            key: `${range.from}-${range.to}`
        };
    }
    return prepared;
}
function prepareFacet(facet, facetsConfig, propertyType, pathParts) {
    const getFacetValue = pathParts ? (doc)=>getValueAtPath(doc, pathParts) : (doc)=>doc[facet];
    let values = {};
    let process;
    let finalize = ()=>values;
    switch(propertyType){
        case "number":
            {
                const { ranges } = facetsConfig[facet];
                const preparedRanges = prepareRanges(ranges);
                values = prepareRangeValues(ranges);
                process = (doc)=>{
                    const facetValue = getFacetValue(doc);
                    if (typeof facetValue === "number") {
                        incrementNumberFacet(preparedRanges, values, facetValue);
                    }
                };
                break;
            }
        case "number[]":
            {
                const { ranges } = facetsConfig[facet];
                const preparedRanges = prepareRanges(ranges);
                values = prepareRangeValues(ranges);
                process = (doc)=>{
                    const facetValue = getFacetValue(doc);
                    if (!Array.isArray(facetValue)) {
                        return;
                    }
                    const alreadyInsertedValues = new Set();
                    for(let i = 0; i < facetValue.length; i++){
                        incrementNumberFacet(preparedRanges, values, facetValue[i], alreadyInsertedValues);
                    }
                };
                break;
            }
        case "boolean":
        case "enum":
        case "string":
            {
                const innerType = propertyType;
                process = (doc)=>{
                    const facetValue = getFacetValue(doc);
                    incrementBooleanStringOrEnumFacet(values, innerType, facetValue);
                };
                if (propertyType === "string") {
                    const stringFacetDefinition = facetsConfig[facet];
                    const sortingPredicate = sortingPredicateBuilder(stringFacetDefinition.sort);
                    const offset = stringFacetDefinition.offset ?? 0;
                    const limit = stringFacetDefinition.limit ?? 10;
                    finalize = ()=>Object.fromEntries(Object.entries(values).sort(sortingPredicate).slice(offset, offset + limit));
                }
                break;
            }
        case "boolean[]":
        case "enum[]":
        case "string[]":
            {
                const innerType = propertyType === "boolean[]" ? "boolean" : "string";
                process = (doc)=>{
                    const facetValue = getFacetValue(doc);
                    if (!Array.isArray(facetValue)) {
                        return;
                    }
                    const alreadyInsertedValues = new Set();
                    for(let i = 0; i < facetValue.length; i++){
                        incrementBooleanStringOrEnumFacet(values, innerType, facetValue[i], alreadyInsertedValues);
                    }
                };
                break;
            }
        default:
            throw (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$errors$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["createError"])("FACET_NOT_SUPPORTED", propertyType);
    }
    return {
        values,
        process,
        finalize
    };
}
function getFacets(zbsearch, results, facetsConfig) {
    const facets = {};
    const facetKeys = Object.keys(facetsConfig);
    const properties = zbsearch.index.getSearchablePropertiesWithTypes(zbsearch.data.index);
    const docs = zbsearch.data.docs.docs;
    const preparedFacets = Array.from({
        length: facetKeys.length
    });
    for(let i = 0; i < facetKeys.length; i++){
        const facet = facetKeys[i];
        const pathParts = facet.includes(".") ? facet.split(".") : null;
        preparedFacets[i] = prepareFacet(facet, facetsConfig, properties[facet], pathParts);
        facets[facet] = {
            count: 0,
            values: preparedFacets[i].values
        };
    }
    const resultsLength = results.length;
    for(let i = 0; i < resultsLength; i++){
        const doc = docs[results[i][0]];
        if (!doc) {
            continue;
        }
        for(let j = 0; j < facetKeys.length; j++){
            preparedFacets[j].process(doc);
        }
    }
    for(let i = 0; i < facetKeys.length; i++){
        const facet = facetKeys[i];
        const preparedFacet = preparedFacets[i];
        facets[facet].count = Object.keys(preparedFacet.values).length;
        facets[facet].values = preparedFacet.finalize();
    }
    return facets;
}
}),
"[project]/node_modules/zbsearch/dist/browser/components/groups.js [app-client] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "getGroups",
    ()=>getGroups
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$errors$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/zbsearch/dist/browser/errors.js [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$utils$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/zbsearch/dist/browser/utils.js [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$components$2f$internal$2d$document$2d$id$2d$store$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/zbsearch/dist/browser/components/internal-document-id-store.js [app-client] (ecmascript)");
;
;
;
const DEFAULT_REDUCE = {
    reducer: (_, acc, res, index)=>{
        acc[index] = res;
        return acc;
    },
    getInitialValue: (length)=>Array.from({
            length
        })
};
const ALLOWED_TYPES = [
    "string",
    "number",
    "boolean"
];
function getGroups(zbsearch, results, groupBy) {
    const properties = groupBy.properties;
    const propertiesLength = properties.length;
    const schemaProperties = zbsearch.index.getSearchablePropertiesWithTypes(zbsearch.data.index);
    for(let i = 0; i < propertiesLength; i++){
        const property = properties[i];
        if (typeof schemaProperties[property] === "undefined") {
            throw (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$errors$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["createError"])("UNKNOWN_GROUP_BY_PROPERTY", property);
        }
        if (!ALLOWED_TYPES.includes(schemaProperties[property])) {
            throw (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$errors$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["createError"])("INVALID_GROUP_BY_PROPERTY", property, ALLOWED_TYPES.join(", "), schemaProperties[property]);
        }
    }
    const allIDs = results.map(([id])=>(0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$components$2f$internal$2d$document$2d$id$2d$store$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["getDocumentIdFromInternalId"])(zbsearch.internalDocumentIDStore, id));
    // allDocs is already sorted by the sortBy algorithm
    // We leverage on that to limit the number of documents returned
    const allDocs = zbsearch.documentsStore.getMultiple(zbsearch.data.docs, allIDs);
    const allDocsLength = allDocs.length;
    const returnedCount = groupBy.maxResult || Number.MAX_SAFE_INTEGER;
    const listOfValues = [];
    // We want to understand which documents have which values
    // and group them by the property and values
    const g = {};
    for(let i = 0; i < propertiesLength; i++){
        const groupByKey = properties[i];
        const group = {
            property: groupByKey,
            perValue: {}
        };
        const values = new Set();
        for(let j = 0; j < allDocsLength; j++){
            const doc = allDocs[j];
            const value = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$utils$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["getNested"])(doc, groupByKey);
            // we don't want to consider undefined values
            if (typeof value === "undefined") {
                continue;
            }
            const keyValue = typeof value !== "boolean" ? value : "" + value;
            const perValue = group.perValue[keyValue] ?? {
                indexes: [],
                count: 0
            };
            if (perValue.count >= returnedCount) {
                continue;
            }
            // We use the index to keep track of the original order
            perValue.indexes.push(j);
            perValue.count++;
            group.perValue[keyValue] = perValue;
            values.add(value);
        }
        listOfValues.push(Array.from(values));
        g[groupByKey] = group;
    }
    const combinations = calculateCombination(listOfValues);
    const combinationsLength = combinations.length;
    const groups = [];
    for(let i = 0; i < combinationsLength; i++){
        const combination = combinations[i];
        const combinationLength = combination.length;
        const group = {
            values: [],
            indexes: []
        };
        const indexes = [];
        for(let j = 0; j < combinationLength; j++){
            const value = combination[j];
            const property = properties[j];
            indexes.push(g[property].perValue[typeof value !== "boolean" ? value : "" + value].indexes);
            group.values.push(value);
        }
        // We leverage on the index to sort the results by the original order
        group.indexes = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$utils$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["intersect"])(indexes).sort((a, b)=>a - b);
        // don't generate empty groups
        if (group.indexes.length === 0) {
            continue;
        }
        groups.push(group);
    }
    const groupsLength = groups.length;
    const res = Array.from({
        length: groupsLength
    });
    for(let i = 0; i < groupsLength; i++){
        const group = groups[i];
        const reduce = groupBy.reduce || DEFAULT_REDUCE;
        const docs = group.indexes.map((index)=>{
            return {
                id: allIDs[index],
                score: results[index][1],
                document: allDocs[index]
            };
        });
        const func = reduce.reducer.bind(null, group.values);
        const initialValue = reduce.getInitialValue(group.indexes.length);
        const aggregationValue = docs.reduce(func, initialValue);
        res[i] = {
            values: group.values,
            result: aggregationValue
        };
    }
    return res;
}
function calculateCombination(arrs, index = 0) {
    if (index + 1 === arrs.length) return arrs[index].map((item)=>[
            item
        ]);
    const head = arrs[index];
    const c = calculateCombination(arrs, index + 1);
    const combinations = [];
    for (const value of head){
        for (const combination of c){
            const result = [
                value
            ];
            (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$utils$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["safeArrayPush"])(result, combination);
            combinations.push(result);
        }
    }
    return combinations;
}
}),
"[project]/node_modules/zbsearch/dist/browser/components/hooks.js [app-client] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "FUNCTION_COMPONENTS",
    ()=>FUNCTION_COMPONENTS,
    "OBJECT_COMPONENTS",
    ()=>OBJECT_COMPONENTS,
    "SINGLE_OR_ARRAY_COMPONENTS",
    ()=>SINGLE_OR_ARRAY_COMPONENTS,
    "runAfterCreate",
    ()=>runAfterCreate,
    "runAfterSearch",
    ()=>runAfterSearch,
    "runBeforeSearch",
    ()=>runBeforeSearch,
    "runMultipleHook",
    ()=>runMultipleHook,
    "runSingleHook",
    ()=>runSingleHook
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$utils$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/zbsearch/dist/browser/utils.js [app-client] (ecmascript)");
;
const OBJECT_COMPONENTS = [
    "tokenizer",
    "index",
    "documentsStore",
    "sorter",
    "pinning"
];
const FUNCTION_COMPONENTS = [
    "validateSchema",
    "getDocumentIndexId",
    "getDocumentProperties",
    "formatElapsedTime"
];
const SINGLE_OR_ARRAY_COMPONENTS = [];
function runSingleHook(hooks, zbsearch, id, doc) {
    if (!hooks.length) {
        return;
    }
    const needAsync = hooks.some(__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$utils$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["isAsyncFunction"]);
    if (needAsync) {
        return (async ()=>{
            for (const hook of hooks){
                await hook(zbsearch, id, doc);
            }
        })();
    } else {
        for (const hook of hooks){
            hook(zbsearch, id, doc);
        }
    }
}
function runMultipleHook(hooks, zbsearch, docsOrIds) {
    if (!hooks.length) {
        return;
    }
    const needAsync = hooks.some(__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$utils$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["isAsyncFunction"]);
    if (needAsync) {
        return (async ()=>{
            for (const hook of hooks){
                await hook(zbsearch, docsOrIds);
            }
        })();
    } else {
        for (const hook of hooks){
            hook(zbsearch, docsOrIds);
        }
    }
}
function runAfterSearch(hooks, db, params, language, results) {
    const needAsync = hooks.some(__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$utils$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["isAsyncFunction"]);
    if (needAsync) {
        return (async ()=>{
            for (const hook of hooks){
                await hook(db, params, language, results);
            }
        })();
    } else {
        for (const hook of hooks){
            hook(db, params, language, results);
        }
    }
}
function runBeforeSearch(hooks, db, params, language) {
    const needAsync = hooks.some(__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$utils$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["isAsyncFunction"]);
    if (needAsync) {
        return (async ()=>{
            for (const hook of hooks){
                await hook(db, params, language);
            }
        })();
    } else {
        for (const hook of hooks){
            hook(db, params, language);
        }
    }
}
function runAfterCreate(hooks, db) {
    const needAsync = hooks.some(__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$utils$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["isAsyncFunction"]);
    if (needAsync) {
        return (async ()=>{
            for (const hook of hooks){
                await hook(db);
            }
        })();
    } else {
        for (const hook of hooks){
            hook(db);
        }
    }
}
}),
"[project]/node_modules/zbsearch/dist/browser/components/index.js [app-client] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "addSearchablePropertyToIndex",
    ()=>addSearchablePropertyToIndex,
    "calculateResultScores",
    ()=>calculateResultScores,
    "create",
    ()=>create,
    "createIndex",
    ()=>createIndex,
    "getSearchableProperties",
    ()=>getSearchableProperties,
    "getSearchablePropertiesWithTypes",
    ()=>getSearchablePropertiesWithTypes,
    "insert",
    ()=>insert,
    "insertDocumentScoreParameters",
    ()=>insertDocumentScoreParameters,
    "insertRadixTokens",
    ()=>insertRadixTokens,
    "insertTokenScoreParameters",
    ()=>insertTokenScoreParameters,
    "insertVector",
    ()=>insertVector,
    "load",
    ()=>load,
    "loadAsync",
    ()=>loadAsync,
    "remove",
    ()=>remove,
    "removeDocumentScoreParameters",
    ()=>removeDocumentScoreParameters,
    "removeTokenScoreParameters",
    ()=>removeTokenScoreParameters,
    "save",
    ()=>save,
    "search",
    ()=>search,
    "searchByGeoWhereClause",
    ()=>searchByGeoWhereClause,
    "searchByWhereClause",
    ()=>searchByWhereClause,
    "searchSuggestions",
    ()=>searchSuggestions
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$errors$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/zbsearch/dist/browser/errors.js [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$trees$2f$avl$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/zbsearch/dist/browser/trees/avl.js [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$trees$2f$bkd$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/zbsearch/dist/browser/trees/bkd.js [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$trees$2f$bool$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/zbsearch/dist/browser/trees/bool.js [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$trees$2f$flat$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/zbsearch/dist/browser/trees/flat.js [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$trees$2f$radix$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/zbsearch/dist/browser/trees/radix.js [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$trees$2f$vector$2d$index$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/zbsearch/dist/browser/trees/vector-index.js [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$utils$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/zbsearch/dist/browser/utils.js [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$components$2f$algorithms$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/zbsearch/dist/browser/components/algorithms.js [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$components$2f$defaults$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__$3c$locals$3e$__ = __turbopack_context__.i("[project]/node_modules/zbsearch/dist/browser/components/defaults.js [app-client] (ecmascript) <locals>");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$components$2f$levenshtein$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/zbsearch/dist/browser/components/levenshtein.js [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$components$2f$internal$2d$document$2d$id$2d$store$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/zbsearch/dist/browser/components/internal-document-id-store.js [app-client] (ecmascript)");
;
;
;
;
;
;
;
;
;
;
;
;
function insertDocumentScoreParameters(index, prop, id, tokens, docsCount, internalId) {
    const resolvedInternalId = internalId ?? (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$components$2f$internal$2d$document$2d$id$2d$store$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["getInternalDocumentId"])(index.sharedInternalDocumentStore, id);
    index.avgFieldLength[prop] = ((index.avgFieldLength[prop] ?? 0) * (docsCount - 1) + tokens.length) / docsCount;
    index.fieldLengths[prop][resolvedInternalId] = tokens.length;
    index.frequencies[prop][resolvedInternalId] = {};
    return resolvedInternalId;
}
function insertTokenScoreParameters(index, prop, id, tokens, token, internalId) {
    let tokenFrequency = 0;
    const tokenLength = tokens.length;
    for(let i = 0; i < tokenLength; i++){
        if (tokens[i] === token) {
            tokenFrequency++;
        }
    }
    const resolvedInternalId = internalId ?? (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$components$2f$internal$2d$document$2d$id$2d$store$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["getInternalDocumentId"])(index.sharedInternalDocumentStore, id);
    const tf = tokenLength > 0 ? tokenFrequency / tokenLength : 0;
    index.frequencies[prop][resolvedInternalId][token] = tf;
}
function insertRadixTokens(index, prop, node, id, internalId, tokens, docsCount) {
    insertDocumentScoreParameters(index, prop, id, tokens, docsCount, internalId);
    const frequencies = index.frequencies[prop][internalId];
    const tokenLength = tokens.length;
    for(let i = 0; i < tokenLength; i++){
        const token = tokens[i];
        if (Object.hasOwn(frequencies, token)) {
            frequencies[token] += 1;
        } else {
            frequencies[token] = 1;
            node.insert(token, internalId);
        }
    }
}
function removeDocumentScoreParameters(index, prop, id, docsCount) {
    const internalId = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$components$2f$internal$2d$document$2d$id$2d$store$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["getInternalDocumentId"])(index.sharedInternalDocumentStore, id);
    if (docsCount > 1) {
        index.avgFieldLength[prop] = (index.avgFieldLength[prop] * docsCount - index.fieldLengths[prop][internalId]) / (docsCount - 1);
    } else {
        index.avgFieldLength[prop] = undefined;
    }
    index.fieldLengths[prop][internalId] = undefined;
    index.frequencies[prop][internalId] = undefined;
}
function removeTokenScoreParameters(_index, _prop, _token) {
// Document frequency is derived from radix postings at search time.
}
function create(zbsearch, sharedInternalDocumentStore, schema, index, prefix = "") {
    if (!index) {
        index = {
            sharedInternalDocumentStore,
            indexes: {},
            vectorIndexes: {},
            searchableProperties: [],
            searchablePropertiesWithTypes: {},
            frequencies: {},
            tokenOccurrences: {},
            avgFieldLength: {},
            fieldLengths: {}
        };
    }
    for (const [prop, type] of Object.entries(schema)){
        const path = `${prefix}${prefix ? "." : ""}${prop}`;
        if (typeof type === "object" && !Array.isArray(type)) {
            // Nested
            create(zbsearch, sharedInternalDocumentStore, type, index, path);
            continue;
        }
        addSearchablePropertyToIndex(zbsearch, index, path, type);
    }
    return index;
}
function addSearchablePropertyToIndex(zbsearch, index, path, type) {
    if ((0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$components$2f$defaults$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__$3c$locals$3e$__["isVectorType"])(type)) {
        const factory = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$trees$2f$vector$2d$index$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["resolveVectorIndexFactory"])(path, zbsearch.indexes);
        index.searchableProperties.push(path);
        index.searchablePropertiesWithTypes[path] = type;
        index.vectorIndexes[path] = {
            type: "Vector",
            node: factory({
                dim: (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$components$2f$defaults$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__$3c$locals$3e$__["getVectorSize"])(type),
                property: path
            }),
            isArray: false
        };
        return;
    }
    const isArray = /\[/.test(type);
    switch(type){
        case "boolean":
        case "boolean[]":
            index.indexes[path] = {
                type: "Bool",
                node: new __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$trees$2f$bool$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["BoolNode"](),
                isArray
            };
            break;
        case "number":
        case "number[]":
            index.indexes[path] = {
                type: "AVL",
                node: new __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$trees$2f$avl$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["AVLTree"](0, []),
                isArray
            };
            break;
        case "string":
        case "string[]":
            index.indexes[path] = {
                type: "Radix",
                node: new __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$trees$2f$radix$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["RadixTree"](),
                isArray
            };
            index.avgFieldLength[path] = 0;
            index.frequencies[path] = {};
            index.tokenOccurrences[path] = {};
            index.fieldLengths[path] = {};
            break;
        case "enum":
        case "enum[]":
            index.indexes[path] = {
                type: "Flat",
                node: new __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$trees$2f$flat$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["FlatTree"](),
                isArray
            };
            break;
        case "geopoint":
            index.indexes[path] = {
                type: "BKD",
                node: new __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$trees$2f$bkd$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["BKDTree"](),
                isArray
            };
            break;
        default:
            throw (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$errors$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["createError"])("INVALID_SCHEMA_TYPE", Array.isArray(type) ? "array" : type, path);
    }
    index.searchableProperties.push(path);
    index.searchablePropertiesWithTypes[path] = type;
}
function insertScalarValue(_implementation, index, prop, id, internalId, value, language, tokenizer, docsCount, options) {
    const { type, node } = index.indexes[prop];
    switch(type){
        case "Bool":
            {
                node[value ? "true" : "false"].add(internalId);
                break;
            }
        case "AVL":
            {
                const avlRebalanceThreshold = options?.avlRebalanceThreshold ?? 1;
                node.insert(value, internalId, avlRebalanceThreshold);
                break;
            }
        case "Radix":
            {
                const tokens = tokenizer.tokenize(value, language, prop, false);
                insertRadixTokens(index, prop, node, id, internalId, tokens, docsCount);
                break;
            }
        case "Flat":
            {
                node.insert(value, internalId);
                break;
            }
        case "BKD":
            {
                node.insert(value, [
                    internalId
                ]);
                break;
            }
    }
}
function insert(implementation, index, prop, id, internalId, value, schemaType, language, tokenizer, docsCount, options) {
    if ((0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$components$2f$defaults$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__$3c$locals$3e$__["isVectorType"])(schemaType)) {
        return insertVector(index, prop, value, id, internalId);
    }
    if (!(0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$components$2f$defaults$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__$3c$locals$3e$__["isArrayType"])(schemaType)) {
        return insertScalarValue(implementation, index, prop, id, internalId, value, language, tokenizer, docsCount, options);
    }
    const elements = value;
    const elementsLength = elements.length;
    for(let i = 0; i < elementsLength; i++){
        insertScalarValue(implementation, index, prop, id, internalId, elements[i], language, tokenizer, docsCount, options);
    }
}
function insertVector(index, prop, value, id, internalDocumentId) {
    index.vectorIndexes[prop].node.add(internalDocumentId, value);
}
function removeScalar(implementation, index, prop, id, internalId, value, schemaType, language, tokenizer, docsCount) {
    if ((0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$components$2f$defaults$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__$3c$locals$3e$__["isVectorType"])(schemaType)) {
        index.vectorIndexes[prop].node.remove(internalId);
        return true;
    }
    const { type, node } = index.indexes[prop];
    switch(type){
        case "AVL":
            {
                node.removeDocument(value, internalId);
                return true;
            }
        case "Bool":
            {
                node[value ? "true" : "false"].delete(internalId);
                return true;
            }
        case "Radix":
            {
                const tokens = tokenizer.tokenize(value, language, prop);
                implementation.removeDocumentScoreParameters(index, prop, id, docsCount);
                for (const token of tokens){
                    implementation.removeTokenScoreParameters(index, prop, token);
                    node.removeDocumentByWord(token, internalId);
                }
                return true;
            }
        case "Flat":
            {
                node.removeDocument(internalId, value);
                return true;
            }
        case "BKD":
            {
                node.removeDocByID(value, internalId);
                return false;
            }
    }
}
function remove(implementation, index, prop, id, internalId, value, schemaType, language, tokenizer, docsCount) {
    if (!(0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$components$2f$defaults$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__$3c$locals$3e$__["isArrayType"])(schemaType)) {
        return removeScalar(implementation, index, prop, id, internalId, value, schemaType, language, tokenizer, docsCount);
    }
    const innerSchemaType = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$components$2f$defaults$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__$3c$locals$3e$__["getInnerType"])(schemaType);
    const elements = value;
    const elementsLength = elements.length;
    for(let i = 0; i < elementsLength; i++){
        removeScalar(implementation, index, prop, id, internalId, elements[i], innerSchemaType, language, tokenizer, docsCount);
    }
    return true;
}
function calculateResultScores(index, prop, term, ids, docsCount, bm25Relevance, resultsMap, boostPerProperty, whereFiltersIDs, keywordMatchesMap) {
    const avgFieldLength = index.avgFieldLength[prop];
    const fieldLengths = index.fieldLengths[prop];
    const zbsearchFrequencies = index.frequencies[prop];
    const termOccurrences = getTermDocumentFrequency(index, prop, term);
    // Calculate TF-IDF value for each term, in each document, for each index.
    const documentIDsLength = ids.length;
    for(let k = 0; k < documentIDsLength; k++){
        const internalId = ids[k];
        if (whereFiltersIDs && !whereFiltersIDs.has(internalId)) {
            continue;
        }
        if (keywordMatchesMap) {
            // Track keyword matches per property
            if (!keywordMatchesMap.has(internalId)) {
                keywordMatchesMap.set(internalId, new Map());
            }
            const propertyMatches = keywordMatchesMap.get(internalId);
            propertyMatches.set(prop, (propertyMatches.get(prop) || 0) + 1);
        }
        const tf = zbsearchFrequencies?.[internalId]?.[term] ?? 0;
        const bm25 = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$components$2f$algorithms$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["BM25"])(tf, termOccurrences, docsCount, fieldLengths[internalId], avgFieldLength, bm25Relevance);
        if (resultsMap.has(internalId)) {
            resultsMap.set(internalId, resultsMap.get(internalId) + bm25 * boostPerProperty);
        } else {
            resultsMap.set(internalId, bm25 * boostPerProperty);
        }
    }
}
function collectFindResultIds(searchResult) {
    const ids = new Set();
    for(const word in searchResult){
        const docIds = searchResult[word];
        for(let i = 0; i < docIds.length; i++){
            ids.add(docIds[i]);
        }
    }
    return ids;
}
function filterIdsToCandidates(ids, candidates) {
    const filtered = [];
    for(let i = 0; i < ids.length; i++){
        const id = ids[i];
        if (candidates.has(id)) {
            filtered.push(id);
        }
    }
    return filtered;
}
// Prefix-expanded words (the query token is a strict prefix of the indexed word) score at a demoted weight so that documents matching the full query token always outrank partial, prefix-only matches.
const PREFIX_EXPANSION_SCORE_DEMOTION = .5;
function bm25Idf(documentFrequency, docsCount) {
    return Math.log(1 + (docsCount - documentFrequency + .5) / (documentFrequency + .5));
}
// Demotion factor for a prefix-expanded word. Beyond the flat demotion, the word's effective IDF is capped at the exact token's IDF (Lucene blends term statistics across expansions the same way): a rare expansion must not outscore the exact token just because it appears in fewer documents.
function prefixExpansionDemotion(tokenDf, wordDf, docsCount) {
    if (tokenDf === undefined) {
        return PREFIX_EXPANSION_SCORE_DEMOTION;
    }
    const wordIdf = bm25Idf(wordDf, docsCount);
    if (!wordIdf) {
        return PREFIX_EXPANSION_SCORE_DEMOTION;
    }
    return PREFIX_EXPANSION_SCORE_DEMOTION * Math.min(1, bm25Idf(tokenDf, docsCount) / wordIdf);
}
function searchThresholdZero(index, tokens, propertiesToSearch, exact, tolerance, boost, relevance, docsCount, whereFiltersIDs) {
    const findCache = new Map();
    const candidateIds = new Set();
    const matchingProperties = [];
    for (const prop of propertiesToSearch){
        if (!(prop in index.indexes)) {
            continue;
        }
        const tree = index.indexes[prop];
        if (tree.type !== "Radix") {
            throw (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$errors$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["createError"])("WRONG_SEARCH_PROPERTY_TYPE", prop);
        }
        const boostPerProperty = boost[prop] ?? 1;
        if (boostPerProperty <= 0) {
            throw (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$errors$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["createError"])("INVALID_BOOST_VALUE", boostPerProperty);
        }
        let propertyIntersection;
        const tokenLength = tokens.length;
        for(let i = 0; i < tokenLength; i++){
            const token = tokens[i];
            const cacheKey = `${prop}\0${token}`;
            let searchResult = findCache.get(cacheKey);
            if (!searchResult) {
                searchResult = tree.node.find({
                    term: token,
                    exact,
                    tolerance
                });
                findCache.set(cacheKey, searchResult);
            }
            if (!Object.keys(searchResult).length) {
                propertyIntersection = undefined;
                break;
            }
            const tokenIds = collectFindResultIds(searchResult);
            propertyIntersection = propertyIntersection === undefined ? tokenIds : (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$utils$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["setIntersection"])(propertyIntersection, tokenIds);
            if (!propertyIntersection.size) {
                break;
            }
        }
        if (!propertyIntersection || !propertyIntersection.size) {
            continue;
        }
        matchingProperties.push(prop);
        for (const id of propertyIntersection){
            if (!whereFiltersIDs || whereFiltersIDs.has(id)) {
                candidateIds.add(id);
            }
        }
    }
    if (!candidateIds.size) {
        return [];
    }
    const resultsMap = new Map();
    for (const prop of matchingProperties){
        const tree = index.indexes[prop];
        if (tree.type !== "Radix") {
            continue;
        }
        const boostPerProperty = boost[prop] ?? 1;
        const tokenLength = tokens.length;
        for(let i = 0; i < tokenLength; i++){
            const token = tokens[i];
            const cacheKey = `${prop}\0${token}`;
            const searchResult = findCache.get(cacheKey);
            const words = Object.keys(searchResult);
            for(let j = 0; j < words.length; j++){
                const word = words[j];
                const filteredIds = filterIdsToCandidates(searchResult[word], candidateIds);
                if (filteredIds.length > 0) {
                    calculateResultScores(index, prop, word, filteredIds, docsCount, relevance, resultsMap, boostPerProperty * (word === token ? 1 : prefixExpansionDemotion(searchResult[token]?.length, searchResult[word].length, docsCount)), whereFiltersIDs);
                }
            }
        }
    }
    return Array.from(resultsMap.entries()).map(([id, score])=>[
            id,
            score
        ]).sort((a, b)=>b[1] - a[1]);
}
function search(index, term, tokenizer, language, propertiesToSearch, exact, tolerance, boost, relevance, docsCount, whereFiltersIDs, threshold = 0, prefix = true) {
    const tokens = tokenizer.tokenize(term, language);
    const keywordsCount = tokens.length || 1;
    if (!tokens.length && !term) {
        tokens.push("");
    }
    const radixExact = term ? exact || !prefix && !tolerance : false;
    if (!threshold && tokens.length > 1) {
        return searchThresholdZero(index, tokens, propertiesToSearch, radixExact, tolerance, boost, relevance, docsCount, whereFiltersIDs);
    }
    // Track keyword matches per document and property
    const keywordMatchesMap = new Map();
    // Track which tokens were found in the search
    const tokenFoundMap = new Map();
    const resultsMap = new Map();
    for (const prop of propertiesToSearch){
        if (!(prop in index.indexes)) {
            continue;
        }
        const tree = index.indexes[prop];
        const { type } = tree;
        if (type !== "Radix") {
            throw (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$errors$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["createError"])("WRONG_SEARCH_PROPERTY_TYPE", prop);
        }
        const boostPerProperty = boost[prop] ?? 1;
        if (boostPerProperty <= 0) {
            throw (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$errors$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["createError"])("INVALID_BOOST_VALUE", boostPerProperty);
        }
        // Process each token in the search term
        const tokenLength = tokens.length;
        for(let i = 0; i < tokenLength; i++){
            const token = tokens[i];
            const searchResult = tree.node.find({
                term: token,
                exact: radixExact,
                tolerance
            });
            // See if this token was found (for threshold=0 filtering)
            const termsFound = Object.keys(searchResult);
            if (termsFound.length > 0) {
                tokenFoundMap.set(token, true);
            }
            // Process each matching term
            const termsFoundLength = termsFound.length;
            for(let j = 0; j < termsFoundLength; j++){
                const word = termsFound[j];
                const ids = searchResult[word];
                calculateResultScores(index, prop, word, ids, docsCount, relevance, resultsMap, boostPerProperty * (word === token ? 1 : prefixExpansionDemotion(searchResult[token]?.length, ids.length, docsCount)), whereFiltersIDs, keywordMatchesMap);
            }
        }
    }
    // Convert to array and sort by score
    const results = Array.from(resultsMap.entries()).map(([id, score])=>[
            id,
            score
        ]).sort((a, b)=>b[1] - a[1]);
    if (results.length === 0) {
        return [];
    }
    // If threshold is 1, return all results
    if (threshold === 1) {
        return results;
    }
    // For threshold=0, check if all tokens were found
    if (threshold === 0) {
        // Quick return for single tokens - already validated
        if (keywordsCount === 1) {
            return results;
        }
        // For multiple tokens, verify that ALL tokens were found
        // If any token wasn't found, return an empty result
        for (const token of tokens){
            if (!tokenFoundMap.get(token)) {
                return [];
            }
        }
        // Find documents that have all keywords in at least one property
        const fullMatches = results.filter(([id])=>{
            const propertyMatches = keywordMatchesMap.get(id);
            if (!propertyMatches) return false;
            // Check if any property has all keywords
            return Array.from(propertyMatches.values()).some((matches)=>matches === keywordsCount);
        });
        return fullMatches;
    }
    // Find documents that have all keywords in at least one property
    const fullMatches = results.filter(([id])=>{
        const propertyMatches = keywordMatchesMap.get(id);
        if (!propertyMatches) return false;
        // Check if any property has all keywords
        return Array.from(propertyMatches.values()).some((matches)=>matches === keywordsCount);
    });
    // If we have full matches and threshold < 1, return full matches plus a percentage of partial matches
    if (fullMatches.length > 0) {
        const remainingResults = results.filter(([id])=>!fullMatches.some(([fid])=>fid === id));
        const additionalResults = Math.ceil(remainingResults.length * threshold);
        return [
            ...fullMatches,
            ...remainingResults.slice(0, additionalResults)
        ];
    }
    // If no full matches, return all results
    return results;
}
function searchSuggestions(index, queryTokens, propertiesToSearch, boost, relevance, docsCount, whereFiltersIDs) {
    const matches = new Map();
    const tokenCount = queryTokens.length;
    for (const prop of propertiesToSearch){
        if (!(prop in index.indexes)) {
            continue;
        }
        const tree = index.indexes[prop];
        if (tree.type !== "Radix") {
            throw (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$errors$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["createError"])("WRONG_SEARCH_PROPERTY_TYPE", prop);
        }
        const boostPerProperty = boost[prop] ?? 1;
        if (boostPerProperty <= 0) {
            throw (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$errors$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["createError"])("INVALID_BOOST_VALUE", boostPerProperty);
        }
        const avgFieldLength = index.avgFieldLength[prop];
        const fieldLengths = index.fieldLengths[prop];
        const frequencies = index.frequencies[prop];
        for(let i = 0; i < tokenCount; i++){
            const { token, exact, tolerance, completion } = queryTokens[i];
            const wholeWordWithTolerance = exact && tolerance > 0;
            const searchResult = tree.node.find({
                term: token,
                exact: exact && !wholeWordWithTolerance,
                tolerance
            });
            const words = Object.keys(searchResult);
            const tokenDocumentFrequency = searchResult[token]?.length;
            for(let j = 0; j < words.length; j++){
                const word = words[j];
                const ids = searchResult[word];
                if (!ids.length) {
                    continue;
                }
                if (wholeWordWithTolerance && word !== token && (Math.abs(word.length - token.length) > tolerance || (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$components$2f$levenshtein$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["levenshtein"])(token, word) > tolerance)) {
                    continue;
                }
                const wordBoost = boostPerProperty * (word === token ? 1 : prefixExpansionDemotion(tokenDocumentFrequency, ids.length, docsCount));
                const termOccurrences = getTermDocumentFrequency(index, prop, word);
                for(let k = 0; k < ids.length; k++){
                    const internalId = ids[k];
                    if (whereFiltersIDs && !whereFiltersIDs.has(internalId)) {
                        continue;
                    }
                    const tf = frequencies?.[internalId]?.[word] ?? 0;
                    const score = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$components$2f$algorithms$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["BM25"])(tf, termOccurrences, docsCount, fieldLengths[internalId], avgFieldLength, relevance) * wordBoost;
                    let match = matches.get(internalId);
                    if (!match) {
                        match = {
                            words: new Array(tokenCount).fill(undefined),
                            wordScores: new Array(tokenCount).fill(0),
                            matchedTokens: 0,
                            score: 0
                        };
                        matches.set(internalId, match);
                    }
                    match.score += score;
                    if (completion) {
                        const completions = match.completions ?? (match.completions = new Map());
                        completions.set(word, (completions.get(word) ?? 0) + score);
                    }
                    if (match.words[i] === undefined || score > match.wordScores[i]) {
                        if (match.words[i] === undefined) {
                            match.matchedTokens++;
                        }
                        match.words[i] = word;
                        match.wordScores[i] = score;
                    }
                }
            }
        }
    }
    return matches;
}
function searchByWhereClause(index, tokenizer, filters, language) {
    // Handle logical operators
    if ("and" in filters && filters.and && Array.isArray(filters.and)) {
        const andFilters = filters.and;
        if (andFilters.length === 0) {
            return new Set();
        }
        const results = andFilters.map((filter)=>searchByWhereClause(index, tokenizer, filter, language));
        return (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$utils$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["setIntersection"])(...results);
    }
    if ("or" in filters && filters.or && Array.isArray(filters.or)) {
        const orFilters = filters.or;
        if (orFilters.length === 0) {
            return new Set();
        }
        const results = orFilters.map((filter)=>searchByWhereClause(index, tokenizer, filter, language));
        // Use reduce to union all sets
        return results.reduce((acc, set)=>(0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$utils$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["setUnion"])(acc, set), new Set());
    }
    if ("not" in filters && filters.not) {
        const notFilter = filters.not;
        // Get all document IDs from the internal document store
        const allDocs = new Set();
        // Get all document IDs from the internal document store
        const docsStore = index.sharedInternalDocumentStore;
        for(let i = 1; i <= docsStore.internalIdToId.length; i++){
            allDocs.add(i);
        }
        const notResult = searchByWhereClause(index, tokenizer, notFilter, language);
        return (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$utils$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["setDifference"])(allDocs, notResult);
    }
    // Handle regular property filters (existing logic)
    const filterKeys = Object.keys(filters);
    const filtersMap = filterKeys.reduce((acc, key)=>({
            [key]: new Set(),
            ...acc
        }), {});
    for (const param of filterKeys){
        const operation = filters[param];
        if (typeof index.indexes[param] === "undefined") {
            throw (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$errors$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["createError"])("UNKNOWN_FILTER_PROPERTY", param);
        }
        const { node, type, isArray } = index.indexes[param];
        if (type === "Bool") {
            const idx = node;
            const filteredIDs = operation ? idx.true : idx.false;
            filtersMap[param] = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$utils$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["setUnion"])(filtersMap[param], filteredIDs);
            continue;
        }
        if (type === "BKD") {
            let reqOperation;
            if ("radius" in operation) {
                reqOperation = "radius";
            } else if ("polygon" in operation) {
                reqOperation = "polygon";
            } else {
                throw new Error(`Invalid operation ${operation}`);
            }
            if (reqOperation === "radius") {
                const { value, coordinates, unit = "m", inside = true, highPrecision = false } = operation[reqOperation];
                const distanceInMeters = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$utils$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["convertDistanceToMeters"])(value, unit);
                const ids = node.searchByRadius(coordinates, distanceInMeters, inside, undefined, highPrecision);
                filtersMap[param] = addGeoResult(filtersMap[param], ids);
            } else {
                const { coordinates, inside = true, highPrecision = false } = operation[reqOperation];
                const ids = node.searchByPolygon(coordinates, inside, undefined, highPrecision);
                filtersMap[param] = addGeoResult(filtersMap[param], ids);
            }
            continue;
        }
        if (type === "Radix" && (typeof operation === "string" || Array.isArray(operation))) {
            for (const raw of [
                operation
            ].flat()){
                const term = tokenizer.tokenize(raw, language, param);
                for (const t of term){
                    const filteredIDsResults = node.find({
                        term: t,
                        exact: true
                    });
                    filtersMap[param] = addFindResult(filtersMap[param], filteredIDsResults);
                }
            }
            continue;
        }
        const operationKeys = Object.keys(operation);
        if (operationKeys.length > 1) {
            throw (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$errors$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["createError"])("INVALID_FILTER_OPERATION", operationKeys.length);
        }
        if (type === "Flat") {
            const results = new Set(isArray ? node.filterArr(operation) : node.filter(operation));
            filtersMap[param] = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$utils$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["setUnion"])(filtersMap[param], results);
            continue;
        }
        if (type === "AVL") {
            const operationOpt = operationKeys[0];
            const operationValue = operation[operationOpt];
            let filteredIDs;
            switch(operationOpt){
                case "gt":
                    {
                        filteredIDs = node.greaterThan(operationValue, false);
                        break;
                    }
                case "gte":
                    {
                        filteredIDs = node.greaterThan(operationValue, true);
                        break;
                    }
                case "lt":
                    {
                        filteredIDs = node.lessThan(operationValue, false);
                        break;
                    }
                case "lte":
                    {
                        filteredIDs = node.lessThan(operationValue, true);
                        break;
                    }
                case "eq":
                    {
                        const ret = node.find(operationValue);
                        filteredIDs = ret ?? new Set();
                        break;
                    }
                case "between":
                    {
                        const [min, max] = operationValue;
                        filteredIDs = node.rangeSearch(min, max);
                        break;
                    }
                default:
                    throw (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$errors$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["createError"])("INVALID_FILTER_OPERATION", operationOpt);
            }
            filtersMap[param] = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$utils$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["setUnion"])(filtersMap[param], filteredIDs);
        }
    }
    // AND operation: calculate the intersection between all the IDs in filterMap
    return (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$utils$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["setIntersection"])(...Object.values(filtersMap));
}
function getSearchableProperties(index) {
    return index.searchableProperties;
}
function getSearchablePropertiesWithTypes(index) {
    return index.searchablePropertiesWithTypes;
}
function deserializeIndexEntry(rawIndex) {
    const { node, type, isArray } = rawIndex;
    switch(type){
        case "Radix":
            return {
                type: "Radix",
                node: __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$trees$2f$radix$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["RadixTree"].fromJSON(node),
                isArray
            };
        case "Flat":
            return {
                type: "Flat",
                node: __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$trees$2f$flat$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["FlatTree"].fromJSON(node),
                isArray
            };
        case "AVL":
            return {
                type: "AVL",
                node: __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$trees$2f$avl$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["AVLTree"].fromJSON(node),
                isArray
            };
        case "BKD":
            return {
                type: "BKD",
                node: __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$trees$2f$bkd$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["BKDTree"].fromJSON(node),
                isArray
            };
        case "Bool":
            return {
                type: "Bool",
                node: __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$trees$2f$bool$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["BoolNode"].fromJSON(node),
                isArray
            };
        default:
            return rawIndex;
    }
}
function load(sharedInternalDocumentStore, raw, indexesConfig) {
    const { indexes: rawIndexes, vectorIndexes: rawVectorIndexes, searchableProperties, searchablePropertiesWithTypes, frequencies, tokenOccurrences, avgFieldLength, fieldLengths } = raw;
    const indexes = {};
    const vectorIndexes = {};
    for (const prop of Object.keys(rawIndexes)){
        indexes[prop] = deserializeIndexEntry(rawIndexes[prop]);
    }
    for (const idx of Object.keys(rawVectorIndexes)){
        vectorIndexes[idx] = {
            type: "Vector",
            isArray: false,
            node: (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$trees$2f$vector$2d$index$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["deserializeVectorIndex"])(idx, rawVectorIndexes[idx], indexesConfig)
        };
    }
    return {
        sharedInternalDocumentStore,
        indexes,
        vectorIndexes,
        searchableProperties,
        searchablePropertiesWithTypes,
        frequencies,
        tokenOccurrences,
        avgFieldLength,
        fieldLengths
    };
}
async function loadAsync(sharedInternalDocumentStore, raw, indexesConfig) {
    const { indexes: rawIndexes, vectorIndexes: rawVectorIndexes, searchableProperties, searchablePropertiesWithTypes, frequencies, tokenOccurrences, avgFieldLength, fieldLengths } = raw;
    const indexes = {};
    const vectorIndexes = {};
    const props = Object.keys(rawIndexes);
    const vectorProps = Object.keys(rawVectorIndexes);
    for(let i = 0; i < props.length; i++){
        const prop = props[i];
        indexes[prop] = deserializeIndexEntry(rawIndexes[prop]);
        if (i < props.length - 1 || vectorProps.length > 0) {
            await (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$utils$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["yieldToEventLoop"])();
        }
    }
    for(let i = 0; i < vectorProps.length; i++){
        const idx = vectorProps[i];
        vectorIndexes[idx] = {
            type: "Vector",
            isArray: false,
            node: (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$trees$2f$vector$2d$index$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["deserializeVectorIndex"])(idx, rawVectorIndexes[idx], indexesConfig)
        };
        if (i < vectorProps.length - 1) {
            await (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$utils$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["yieldToEventLoop"])();
        }
    }
    return {
        sharedInternalDocumentStore,
        indexes,
        vectorIndexes,
        searchableProperties,
        searchablePropertiesWithTypes,
        frequencies,
        tokenOccurrences,
        avgFieldLength,
        fieldLengths
    };
}
function save(index) {
    const { indexes, vectorIndexes, searchableProperties, searchablePropertiesWithTypes, frequencies, tokenOccurrences: _tokenOccurrences, avgFieldLength, fieldLengths } = index;
    const dumpVectorIndexes = {};
    for (const idx of Object.keys(vectorIndexes)){
        dumpVectorIndexes[idx] = vectorIndexes[idx].node.toJSON();
    }
    // eslint-disable-next-line @typescript-eslint/no-explicit-any
    const savedIndexes = {};
    for (const name of Object.keys(indexes)){
        const { type, node, isArray } = indexes[name];
        if (type === "Flat" || type === "Radix" || type === "AVL" || type === "BKD" || type === "Bool") {
            savedIndexes[name] = {
                type,
                node: node.toJSON(),
                isArray
            };
        } else {
            savedIndexes[name] = indexes[name];
            savedIndexes[name].node = savedIndexes[name].node.toJSON();
        }
    }
    return {
        indexes: savedIndexes,
        vectorIndexes: dumpVectorIndexes,
        searchableProperties,
        searchablePropertiesWithTypes,
        frequencies,
        tokenOccurrences: {},
        avgFieldLength,
        fieldLengths
    };
}
function getTermDocumentFrequency(index, prop, term) {
    const treeIndex = index.indexes[prop];
    if (treeIndex?.type === "Radix") {
        return treeIndex.node.getDocumentFrequency(term);
    }
    const stored = index.tokenOccurrences[prop]?.[term];
    return typeof stored === "number" ? stored : 0;
}
function createIndex() {
    return {
        create,
        insert,
        remove,
        insertDocumentScoreParameters,
        insertTokenScoreParameters,
        removeDocumentScoreParameters,
        removeTokenScoreParameters,
        calculateResultScores,
        search,
        supportsSuggestions: true,
        searchByWhereClause,
        getSearchableProperties,
        getSearchablePropertiesWithTypes,
        load,
        loadAsync,
        save
    };
}
function addGeoResult(set, ids) {
    if (!set) {
        set = new Set();
    }
    const idsLength = ids.length;
    for(let i = 0; i < idsLength; i++){
        const entry = ids[i].docIDs;
        const idsLength = entry.length;
        for(let j = 0; j < idsLength; j++){
            set.add(entry[j]);
        }
    }
    return set;
}
function createGeoTokenScores(ids, centerPoint, highPrecision = false) {
    const distanceFn = highPrecision ? __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$trees$2f$bkd$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["BKDTree"].vincentyDistance : __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$trees$2f$bkd$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["BKDTree"].haversineDistance;
    const results = [];
    // Calculate distances for all results to find the maximum
    const distances = [];
    for (const { point } of ids){
        distances.push(distanceFn(centerPoint, point));
    }
    const maxDistance = Math.max(...distances);
    // Create results with inverse distance scores (higher score = closer)
    let index = 0;
    for (const { docIDs } of ids){
        const distance = distances[index];
        // Use inverse score: closer points get higher scores
        // Add 1 to avoid division by zero for points at exact center
        const score = maxDistance - distance + 1;
        for (const docID of docIDs){
            results.push([
                docID,
                score
            ]);
        }
        index++;
    }
    // Sort by score (higher first - closer points)
    results.sort((a, b)=>b[1] - a[1]);
    return results;
}
function isGeosearchOnlyQuery(filters, index) {
    const filterKeys = Object.keys(filters);
    if (filterKeys.length !== 1) {
        return {
            isGeoOnly: false
        };
    }
    const param = filterKeys[0];
    const operation = filters[param];
    if (typeof index.indexes[param] === "undefined") {
        return {
            isGeoOnly: false
        };
    }
    const { type } = index.indexes[param];
    if (type === "BKD" && operation && ("radius" in operation || "polygon" in operation)) {
        return {
            isGeoOnly: true,
            geoProperty: param,
            geoOperation: operation
        };
    }
    return {
        isGeoOnly: false
    };
}
function searchByGeoWhereClause(index, filters) {
    const indexTyped = index;
    const geoInfo = isGeosearchOnlyQuery(filters, indexTyped);
    if (!geoInfo.isGeoOnly || !geoInfo.geoProperty || !geoInfo.geoOperation) {
        return null;
    }
    const { node } = indexTyped.indexes[geoInfo.geoProperty];
    const operation = geoInfo.geoOperation;
    // Cast node to BKDTree since we already verified it's type 'BKD'
    const bkdNode = node;
    let results;
    if ("radius" in operation) {
        const { value, coordinates, unit = "m", inside = true, highPrecision = false } = operation.radius;
        const centerPoint = coordinates;
        const distanceInMeters = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$utils$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["convertDistanceToMeters"])(value, unit);
        results = bkdNode.searchByRadius(centerPoint, distanceInMeters, inside, "asc", highPrecision);
        return createGeoTokenScores(results, centerPoint, highPrecision);
    } else if ("polygon" in operation) {
        const { coordinates, inside = true, highPrecision = false } = operation.polygon;
        results = bkdNode.searchByPolygon(coordinates, inside, "asc", highPrecision);
        const centroid = __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$trees$2f$bkd$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["BKDTree"].calculatePolygonCentroid(coordinates);
        return createGeoTokenScores(results, centroid, highPrecision);
    }
    return null;
}
function addFindResult(set, filteredIDsResults) {
    if (!set) {
        set = new Set();
    }
    const keys = Object.keys(filteredIDsResults);
    const keysLength = keys.length;
    for(let i = 0; i < keysLength; i++){
        const ids = filteredIDsResults[keys[i]];
        const idsLength = ids.length;
        for(let j = 0; j < idsLength; j++){
            set.add(ids[j]);
        }
    }
    return set;
}
}),
"[project]/node_modules/zbsearch/dist/browser/components/inference.js [app-client] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "inferSchemaFromDocument",
    ()=>inferSchemaFromDocument
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$constants$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/zbsearch/dist/browser/constants.js [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$utils$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/zbsearch/dist/browser/utils.js [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$components$2f$index$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/zbsearch/dist/browser/components/index.js [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$components$2f$sorter$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/zbsearch/dist/browser/components/sorter.js [app-client] (ecmascript)");
;
;
;
;
function inferTypeFromValue(value) {
    switch(typeof value){
        case "string":
            return "string";
        case "number":
            return "number";
        case "boolean":
            return "boolean";
    }
    if (Array.isArray(value)) {
        if (value.length === 0) {
            return undefined;
        }
        const innerType = inferTypeFromValue(value[0]);
        if (innerType === "string" || innerType === "number" || innerType === "boolean") {
            return `${innerType}[]`;
        }
        return undefined;
    }
    // getDocumentProperties already treats any object with numeric lat/lon as a geopoint value, so inferring `geopoint` is the only consistent choice.
    if (typeof value === "object" && value !== null) {
        const point = value;
        if (typeof point.lat === "number" && typeof point.lon === "number") {
            return "geopoint";
        }
    }
    return undefined;
}
function flattenDocument(doc, prefix, leaves) {
    for (const [key, value] of Object.entries(doc)){
        if (key === "id" || key === __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$constants$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["RESERVED_VECTOR_INDEX_KEY"]) {
            continue;
        }
        if (value === null || typeof value === "undefined") {
            continue;
        }
        const path = prefix ? `${prefix}.${key}` : key;
        if (typeof value === "object" && !Array.isArray(value)) {
            if (typeof value.lat === "number" && typeof value.lon === "number") {
                leaves.push([
                    path,
                    value
                ]);
                continue;
            }
            flattenDocument(value, path, leaves);
            continue;
        }
        leaves.push([
            path,
            value
        ]);
    }
}
function hasPathConflict(knownProperties, path) {
    for (const known of Object.keys(knownProperties)){
        if (path.startsWith(`${known}.`) || known.startsWith(`${path}.`)) {
            return true;
        }
    }
    return false;
}
function setNestedSchemaProperty(schema, path, type) {
    const tokens = path.split(".");
    const lastToken = tokens[tokens.length - 1];
    let current = schema;
    for(let i = 0; i < tokens.length - 1; i++){
        const token = tokens[i];
        const next = current[token];
        if (typeof next !== "object" || next === null || Array.isArray(next)) {
            current[token] = {};
        }
        current = current[token];
    }
    current[lastToken] = type;
}
function inferSchemaFromDocument(zbsearch, doc) {
    if (!zbsearch.inferSchema) {
        return false;
    }
    const leaves = [];
    flattenDocument(doc, "", leaves);
    const index = zbsearch.data.index;
    const sorter = zbsearch.data.sorting;
    const knownProperties = zbsearch.index.getSearchablePropertiesWithTypes(index);
    let changed = false;
    for (const [path, value] of leaves){
        const knownType = knownProperties[path];
        if (knownType) {
            // The index already knows this property (e.g. after load()): make sure the runtime schema reflects its type so validateSchema keeps enforcing it.
            if ((0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$utils$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["getNested"])(zbsearch.schema, path) !== knownType) {
                setNestedSchemaProperty(zbsearch.schema, path, knownType);
            }
            continue;
        }
        const type = inferTypeFromValue(value);
        if (!type) {
            continue;
        }
        if (hasPathConflict(knownProperties, path)) {
            continue;
        }
        (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$components$2f$index$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["addSearchablePropertyToIndex"])(zbsearch, index, path, type);
        (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$components$2f$sorter$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["addSortablePropertyToSorter"])(sorter, path, type);
        setNestedSchemaProperty(zbsearch.schema, path, type);
        knownProperties[path] = type;
        changed = true;
    }
    if (changed) {
        zbsearch.caches = {};
    }
    return changed;
}
}),
"[project]/node_modules/zbsearch/dist/browser/components/internal-document-id-store.js [app-client] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "createInternalDocumentIDStore",
    ()=>createInternalDocumentIDStore,
    "getDocumentIdFromInternalId",
    ()=>getDocumentIdFromInternalId,
    "getInternalDocumentId",
    ()=>getInternalDocumentId,
    "load",
    ()=>load,
    "save",
    ()=>save
]);
function createInternalDocumentIDStore() {
    return {
        idToInternalId: new Map(),
        internalIdToId: [],
        save,
        load
    };
}
function save(store) {
    return {
        internalIdToId: store.internalIdToId
    };
}
function load(zbsearch, raw) {
    const { internalIdToId } = raw;
    zbsearch.internalDocumentIDStore.idToInternalId.clear();
    zbsearch.internalDocumentIDStore.internalIdToId = [];
    const internalIdToIdLength = internalIdToId.length;
    for(let i = 0; i < internalIdToIdLength; i++){
        const internalIdItem = internalIdToId[i];
        zbsearch.internalDocumentIDStore.idToInternalId.set(internalIdItem, i + 1);
        zbsearch.internalDocumentIDStore.internalIdToId.push(internalIdItem);
    }
}
function getInternalDocumentId(store, id) {
    if (typeof id === "string") {
        const internalId = store.idToInternalId.get(id);
        if (internalId) {
            return internalId;
        }
        const currentId = store.idToInternalId.size + 1;
        store.idToInternalId.set(id, currentId);
        store.internalIdToId.push(id);
        return currentId;
    }
    if (id > store.internalIdToId.length) {
        return getInternalDocumentId(store, id.toString());
    }
    return id;
}
function getDocumentIdFromInternalId(store, internalId) {
    if (store.internalIdToId.length < internalId) {
        throw new Error(`Invalid internalId ${internalId}`);
    }
    return store.internalIdToId[internalId - 1];
}
}),
"[project]/node_modules/zbsearch/dist/browser/components/levenshtein.js [app-client] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "boundedLevenshtein",
    ()=>boundedLevenshtein,
    "levenshtein",
    ()=>levenshtein,
    "syncBoundedLevenshtein",
    ()=>syncBoundedLevenshtein
]);
/**
* Inspired by:
* https://github.com/Yomguithereal/talisman/blob/86ae55cbd040ff021d05e282e0e6c71f2dde21f8/src/metrics/levenshtein.js#L218-L340
*/ function _boundedLevenshtein(term, word, tolerance) {
    // Handle base cases
    if (tolerance < 0) return -1;
    if (term === word) return 0;
    const m = term.length;
    const n = word.length;
    // Special case for empty strings
    if (m === 0) return n <= tolerance ? n : -1;
    if (n === 0) return m <= tolerance ? m : -1;
    // term = term.toLowerCase()
    // word = word.toLowerCase()
    const diff = Math.abs(m - n);
    // Special case for prefixes
    // If the searching word starts with the indexed word, return early.
    if (term.startsWith(word)) {
        // We just check if the remaining characters are within the tolerance
        return diff <= tolerance ? diff : -1;
    }
    // If the indexed word starts with the searching word, return early.
    if (word.startsWith(term)) {
        // any prefixed word is within the tolerance
        return 0;
    }
    // If the length difference is greater than the tolerance, return early
    if (diff > tolerance) return -1;
    // Initialize the matrix
    const matrix = [];
    for(let i = 0; i <= m; i++){
        matrix[i] = [
            i
        ];
        for(let j = 1; j <= n; j++){
            matrix[i][j] = i === 0 ? j : 0;
        }
    }
    // Fill the matrix
    for(let i = 1; i <= m; i++){
        let rowMin = Infinity;
        for(let j = 1; j <= n; j++){
            if (term[i - 1] === word[j - 1]) {
                matrix[i][j] = matrix[i - 1][j - 1];
            } else {
                matrix[i][j] = Math.min(matrix[i - 1][j] + 1, matrix[i][j - 1] + 1, matrix[i - 1][j - 1] + 1);
            }
            rowMin = Math.min(rowMin, matrix[i][j]);
        }
        // Early termination if all values in this row exceed tolerance
        if (rowMin > tolerance) {
            return -1;
        }
    }
    return matrix[m][n] <= tolerance ? matrix[m][n] : -1;
}
function boundedLevenshtein(term, w, tolerance) {
    const distance = _boundedLevenshtein(term, w, tolerance);
    return {
        distance,
        isBounded: distance >= 0
    };
}
function syncBoundedLevenshtein(term, w, tolerance) {
    const distance = _boundedLevenshtein(term, w, tolerance);
    return {
        distance,
        isBounded: distance >= 0
    };
}
function levenshtein(a, b) {
    /* c8 ignore next 3 */ if (!a.length) {
        return b.length;
    }
    /* c8 ignore next 3 */ if (!b.length) {
        return a.length;
    }
    const swap = a;
    if (a.length > b.length) {
        a = b;
        b = swap;
    }
    const row = Array.from({
        length: a.length + 1
    }, (_, i)=>i);
    let val = 0;
    for(let i = 1; i <= b.length; i++){
        let prev = i;
        for(let j = 1; j <= a.length; j++){
            if (b[i - 1] === a[j - 1]) {
                val = row[j - 1];
            } else {
                val = Math.min(row[j - 1] + 1, Math.min(prev + 1, row[j] + 1));
            }
            row[j - 1] = prev;
            prev = val;
        }
        row[a.length] = prev;
    }
    return row[a.length];
}
}),
"[project]/node_modules/zbsearch/dist/browser/components/pinning-manager.js [app-client] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "applyPinningRules",
    ()=>applyPinningRules
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$components$2f$internal$2d$document$2d$id$2d$store$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/zbsearch/dist/browser/components/internal-document-id-store.js [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$components$2f$pinning$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/zbsearch/dist/browser/components/pinning.js [app-client] (ecmascript)");
;
;
function applyPinningRules(zbsearch, pinningStore, uniqueDocsArray, searchTerm) {
    // Get all matching rules for the current search term
    const matchingRules = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$components$2f$pinning$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["getMatchingRules"])(pinningStore, searchTerm);
    if (matchingRules.length === 0) {
        return uniqueDocsArray;
    }
    // Collect all promotions from matching rules
    const allPromotions = matchingRules.flatMap((rule)=>rule.consequence.promote);
    // Sort promotions by position (lower position = higher priority)
    allPromotions.sort((a, b)=>a.position - b.position);
    // Create a Set of pinned internal document IDs for fast lookup
    const pinnedInternalIds = new Set();
    const promotionsMap = new Map();
    const positionsTaken = new Set();
    for (const promotion of allPromotions){
        const internalId = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$components$2f$internal$2d$document$2d$id$2d$store$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["getInternalDocumentId"])(zbsearch.internalDocumentIDStore, promotion.doc_id);
        // Skip if document doesn't exist in the database or if this position is already taken
        if (internalId === undefined) {
            continue;
        }
        // If this document is already pinned to a different position, keep the first one (lower position)
        if (promotionsMap.has(internalId)) {
            const existingPosition = promotionsMap.get(internalId);
            if (promotion.position < existingPosition) {
                promotionsMap.set(internalId, promotion.position);
            }
            continue;
        }
        // If this position is already taken, skip this promotion (first wins)
        if (positionsTaken.has(promotion.position)) {
            continue;
        }
        pinnedInternalIds.add(internalId);
        promotionsMap.set(internalId, promotion.position);
        positionsTaken.add(promotion.position);
    }
    // If no valid promotions were found, return original results
    if (promotionsMap.size === 0) {
        return uniqueDocsArray;
    }
    const originalScoresByInternalId = new Map();
    for (const [id, score] of uniqueDocsArray){
        originalScoresByInternalId.set(id, score);
    }
    const unpinnedResults = [];
    for (const entry of uniqueDocsArray){
        if (!pinnedInternalIds.has(entry[0])) {
            unpinnedResults.push(entry);
        }
    }
    const BASE_PIN_SCORE = 1e6;
    const pinnedResults = [];
    for (const [internalId, position] of promotionsMap){
        const existingScore = originalScoresByInternalId.get(internalId);
        if (existingScore !== undefined) {
            pinnedResults.push([
                internalId,
                BASE_PIN_SCORE - position
            ]);
        } else {
            const doc = zbsearch.documentsStore.get(zbsearch.data.docs, internalId);
            if (doc) {
                pinnedResults.push([
                    internalId,
                    0
                ]);
            }
        }
    }
    // Sort pinned results by their assigned scores (which correspond to positions)
    pinnedResults.sort((a, b)=>{
        const posA = promotionsMap.get(a[0]) ?? Infinity;
        const posB = promotionsMap.get(b[0]) ?? Infinity;
        return posA - posB;
    });
    // Insert pinned results at their specified positions
    const finalResults = [];
    const pinnedByPosition = new Map();
    for (const pinnedResult of pinnedResults){
        const position = promotionsMap.get(pinnedResult[0]);
        pinnedByPosition.set(position, pinnedResult);
    }
    let unpinnedIndex = 0;
    let currentPosition = 0;
    // Build the final results array by interleaving pinned and unpinned results
    while(currentPosition < unpinnedResults.length + pinnedResults.length){
        if (pinnedByPosition.has(currentPosition)) {
            // Insert pinned document at this position
            finalResults.push(pinnedByPosition.get(currentPosition));
            currentPosition++;
        } else if (unpinnedIndex < unpinnedResults.length) {
            // Insert next unpinned document
            finalResults.push(unpinnedResults[unpinnedIndex]);
            unpinnedIndex++;
            currentPosition++;
        } else {
            break;
        }
    }
    // Add any remaining pinned results that have positions beyond the unpinned results length
    for (const [position, pinnedResult] of pinnedByPosition.entries()){
        if (position >= finalResults.length) {
            finalResults.push(pinnedResult);
        }
    }
    return finalResults;
}
}),
"[project]/node_modules/zbsearch/dist/browser/components/pinning.js [app-client] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "createPinning",
    ()=>createPinning,
    "getMatchingRules",
    ()=>getMatchingRules,
    "load",
    ()=>load,
    "save",
    ()=>save
]);
function create(sharedInternalDocumentStore) {
    return {
        sharedInternalDocumentStore,
        rules: new Map()
    };
}
function addRule(store, rule) {
    if (store.rules.has(rule.id)) {
        throw new Error(`PINNING_RULE_ALREADY_EXISTS: A pinning rule with id "${rule.id}" already exists. Use updateRule to modify it.`);
    }
    store.rules.set(rule.id, rule);
}
function updateRule(store, rule) {
    if (!store.rules.has(rule.id)) {
        throw new Error(`PINNING_RULE_NOT_FOUND: Cannot update pinning rule with id "${rule.id}" because it does not exist. Use addRule to create it.`);
    }
    store.rules.set(rule.id, rule);
}
function removeRule(store, ruleId) {
    return store.rules.delete(ruleId);
}
function getRule(store, ruleId) {
    return store.rules.get(ruleId);
}
function getAllRules(store) {
    return Array.from(store.rules.values());
}
function matchesCondition(term, condition) {
    const normalizedTerm = term.toLowerCase().trim();
    const normalizedPattern = condition.pattern.toLowerCase().trim();
    switch(condition.anchoring){
        case "is":
            return normalizedTerm === normalizedPattern;
        case "starts_with":
            return normalizedTerm.startsWith(normalizedPattern);
        case "contains":
            return normalizedTerm.includes(normalizedPattern);
        default:
            return false;
    }
}
function matchesRule(term, rule) {
    if (!term) {
        return false;
    }
    // All conditions must match (AND logic)
    return rule.conditions.every((condition)=>matchesCondition(term, condition));
}
function getMatchingRules(store, term) {
    if (!term) {
        return [];
    }
    const matchingRules = [];
    for (const rule of store.rules.values()){
        if (matchesRule(term, rule)) {
            matchingRules.push(rule);
        }
    }
    return matchingRules;
}
function load(sharedInternalDocumentStore, raw) {
    const rawStore = raw;
    return {
        sharedInternalDocumentStore,
        rules: new Map(rawStore?.rules ?? [])
    };
}
function save(store) {
    return {
        rules: Array.from(store.rules.entries())
    };
}
function createPinning() {
    return {
        create,
        addRule,
        updateRule,
        removeRule,
        getRule,
        getAllRules,
        getMatchingRules,
        load,
        save
    };
}
}),
"[project]/node_modules/zbsearch/dist/browser/components/plugins.js [app-client] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "AVAILABLE_PLUGIN_HOOKS",
    ()=>AVAILABLE_PLUGIN_HOOKS,
    "getAllPluginsByHook",
    ()=>getAllPluginsByHook
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$errors$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/zbsearch/dist/browser/errors.js [app-client] (ecmascript)");
;
const AVAILABLE_PLUGIN_HOOKS = [
    "beforeInsert",
    "afterInsert",
    "beforeRemove",
    "afterRemove",
    "beforeUpdate",
    "afterUpdate",
    "beforeUpsert",
    "afterUpsert",
    "beforeSearch",
    "afterSearch",
    "beforeInsertMultiple",
    "afterInsertMultiple",
    "beforeRemoveMultiple",
    "afterRemoveMultiple",
    "beforeUpdateMultiple",
    "afterUpdateMultiple",
    "beforeUpsertMultiple",
    "afterUpsertMultiple",
    "beforeLoad",
    "afterLoad",
    "afterCreate"
];
function getAllPluginsByHook(zbsearch, hook) {
    const pluginsToRun = [];
    const pluginsLength = zbsearch.plugins?.length;
    if (!pluginsLength) {
        return pluginsToRun;
    }
    for(let i = 0; i < pluginsLength; i++){
        try {
            const plugin = zbsearch.plugins[i];
            if (typeof plugin[hook] === "function") {
                pluginsToRun.push(plugin[hook]);
            }
        } catch (error) {
            console.error("Caught error in getAllPluginsByHook:", error);
            throw (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$errors$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["createError"])("PLUGIN_CRASHED");
        }
    }
    return pluginsToRun;
}
}),
"[project]/node_modules/zbsearch/dist/browser/components/sorter.js [app-client] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "addSortablePropertyToSorter",
    ()=>addSortablePropertyToSorter,
    "createSorter",
    ()=>createSorter,
    "load",
    ()=>load,
    "loadAsync",
    ()=>loadAsync,
    "save",
    ()=>save
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$errors$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/zbsearch/dist/browser/errors.js [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$utils$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/zbsearch/dist/browser/utils.js [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$components$2f$defaults$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__$3c$locals$3e$__ = __turbopack_context__.i("[project]/node_modules/zbsearch/dist/browser/components/defaults.js [app-client] (ecmascript) <locals>");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$components$2f$internal$2d$document$2d$id$2d$store$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/zbsearch/dist/browser/components/internal-document-id-store.js [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$components$2f$tokenizer$2f$languages$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/zbsearch/dist/browser/components/tokenizer/languages.js [app-client] (ecmascript)");
;
;
;
;
;
function addSortablePropertyToSorter(sorter, path, type) {
    if (!sorter.enabled || (sorter.unsortableProperties ?? []).includes(path)) {
        return;
    }
    switch(type){
        case "boolean":
        case "number":
        case "string":
            sorter.sortableProperties.push(path);
            sorter.sortablePropertiesWithTypes[path] = type;
            sorter.sorts[path] = {
                docs: new Map(),
                orderedDocsToRemove: new Map(),
                orderedDocs: [],
                type
            };
            break;
        default:
            // We don't allow to sort by arrays, enums, geopoints, or vectors
            return;
    }
}
function innerCreate(zbsearch, sharedInternalDocumentStore, schema, sortableDeniedProperties, prefix) {
    const sorter = {
        language: zbsearch.tokenizer.language,
        sharedInternalDocumentStore,
        enabled: true,
        isSorted: true,
        sortableProperties: [],
        sortablePropertiesWithTypes: {},
        sorts: {},
        unsortableProperties: sortableDeniedProperties
    };
    for (const [prop, type] of Object.entries(schema)){
        const path = `${prefix}${prefix ? "." : ""}${prop}`;
        if (sortableDeniedProperties.includes(path)) {
            continue;
        }
        if (typeof type === "object" && !Array.isArray(type)) {
            // Nested
            const ret = innerCreate(zbsearch, sharedInternalDocumentStore, type, sortableDeniedProperties, path);
            (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$utils$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["safeArrayPush"])(sorter.sortableProperties, ret.sortableProperties);
            sorter.sorts = {
                ...sorter.sorts,
                ...ret.sorts
            };
            sorter.sortablePropertiesWithTypes = {
                ...sorter.sortablePropertiesWithTypes,
                ...ret.sortablePropertiesWithTypes
            };
            continue;
        }
        if ((0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$components$2f$defaults$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__$3c$locals$3e$__["isVectorType"])(type)) {
            continue;
        }
        switch(type){
            case "boolean":
            case "number":
            case "string":
                addSortablePropertyToSorter(sorter, path, type);
                break;
            case "geopoint":
            case "enum":
                continue;
            case "enum[]":
            case "boolean[]":
            case "number[]":
            case "string[]":
                continue;
            default:
                throw (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$errors$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["createError"])("INVALID_SORT_SCHEMA_TYPE", Array.isArray(type) ? "array" : type, path);
        }
    }
    return sorter;
}
function create(zbsearch, sharedInternalDocumentStore, schema, config) {
    const isSortEnabled = config?.enabled !== false;
    if (!isSortEnabled) {
        return {
            disabled: true
        };
    }
    return innerCreate(zbsearch, sharedInternalDocumentStore, schema, (config || {}).unsortableProperties || [], "");
}
function insert(sorter, prop, id, value) {
    if (!sorter.enabled) {
        return;
    }
    sorter.isSorted = false;
    const internalId = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$components$2f$internal$2d$document$2d$id$2d$store$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["getInternalDocumentId"])(sorter.sharedInternalDocumentStore, id);
    const s = sorter.sorts[prop];
    // This happen during a document updating
    // Because we re-use the same internalId
    // We need to clean-up the data structure before re-inserting
    // to avoid duplicates in the orderedDocs array
    // See: https://github.com/oramasearch/orama/issues/629
    if (s.orderedDocsToRemove.has(internalId)) {
        ensureOrderedDocsAreDeletedByProperty(sorter, prop);
    }
    s.docs.set(internalId, s.orderedDocs.length);
    s.orderedDocs.push([
        internalId,
        value
    ]);
}
function ensureIsSorted(sorter) {
    if (sorter.isSorted || !sorter.enabled) {
        return;
    }
    const properties = Object.keys(sorter.sorts);
    for (const prop of properties){
        ensurePropertyIsSorted(sorter, prop);
    }
    sorter.isSorted = true;
}
function stringSort(language, value, d) {
    return value[1].localeCompare(d[1], (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$components$2f$tokenizer$2f$languages$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["getLocale"])(language));
}
function numberSort(value, d) {
    return value[1] - d[1];
}
function booleanSort(value, d) {
    return d[1] ? -1 : 1;
}
function ensurePropertyIsSorted(sorter, prop) {
    const s = sorter.sorts[prop];
    let predicate;
    switch(s.type){
        case "string":
            predicate = stringSort.bind(null, sorter.language);
            break;
        case "number":
            predicate = numberSort.bind(null);
            break;
        case "boolean":
            predicate = booleanSort.bind(null);
            break;
    }
    s.orderedDocs.sort(predicate);
    // Increment position for the greather documents
    const orderedDocsLength = s.orderedDocs.length;
    for(let i = 0; i < orderedDocsLength; i++){
        const docId = s.orderedDocs[i][0];
        s.docs.set(docId, i);
    }
}
function ensureOrderedDocsAreDeleted(sorter) {
    const properties = Object.keys(sorter.sorts);
    for (const prop of properties){
        ensureOrderedDocsAreDeletedByProperty(sorter, prop);
    }
}
function ensureOrderedDocsAreDeletedByProperty(sorter, prop) {
    const s = sorter.sorts[prop];
    if (!s.orderedDocsToRemove.size) return;
    s.orderedDocs = s.orderedDocs.filter((doc)=>!s.orderedDocsToRemove.has(doc[0]));
    s.orderedDocsToRemove.clear();
}
function remove(sorter, prop, id) {
    if (!sorter.enabled) {
        return;
    }
    const s = sorter.sorts[prop];
    const internalId = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$components$2f$internal$2d$document$2d$id$2d$store$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["getInternalDocumentId"])(sorter.sharedInternalDocumentStore, id);
    const index = s.docs.get(internalId);
    if (!index) return;
    s.docs.delete(internalId);
    s.orderedDocsToRemove.set(internalId, true);
}
function sortBy(sorter, docIds, by) {
    if (!sorter.enabled) {
        throw (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$errors$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["createError"])("SORT_DISABLED");
    }
    const property = by.property;
    const isDesc = by.order === "DESC";
    const s = sorter.sorts[property];
    if (!s) {
        throw (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$errors$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["createError"])("UNABLE_TO_SORT_ON_UNKNOWN_FIELD", property, sorter.sortableProperties.join(", "));
    }
    ensureOrderedDocsAreDeletedByProperty(sorter, property);
    ensureIsSorted(sorter);
    docIds.sort((a, b)=>{
        // This sort algorithm works leveraging on
        // that s.docs is a map of docId -> position
        // If a document is not indexed, it will be not present in the map
        const indexOfA = s.docs.get((0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$components$2f$internal$2d$document$2d$id$2d$store$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["getInternalDocumentId"])(sorter.sharedInternalDocumentStore, a[0]));
        const indexOfB = s.docs.get((0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$components$2f$internal$2d$document$2d$id$2d$store$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["getInternalDocumentId"])(sorter.sharedInternalDocumentStore, b[0]));
        const isAIndexed = typeof indexOfA !== "undefined";
        const isBIndexed = typeof indexOfB !== "undefined";
        if (!isAIndexed && !isBIndexed) {
            return 0;
        }
        // unindexed documents are always at the end
        if (!isAIndexed) {
            return 1;
        }
        if (!isBIndexed) {
            return -1;
        }
        return isDesc ? indexOfB - indexOfA : indexOfA - indexOfB;
    });
    return docIds;
}
function getSortableProperties(sorter) {
    if (!sorter.enabled) {
        return [];
    }
    return sorter.sortableProperties;
}
function getSortablePropertiesWithTypes(sorter) {
    if (!sorter.enabled) {
        return {};
    }
    return sorter.sortablePropertiesWithTypes;
}
function load(sharedInternalDocumentStore, raw) {
    const rawDocument = raw;
    if (!rawDocument.enabled) {
        return {
            enabled: false
        };
    }
    const sorts = Object.keys(rawDocument.sorts).reduce((acc, prop)=>{
        const { docs, orderedDocs, type } = rawDocument.sorts[prop];
        acc[prop] = {
            docs: new Map(Object.entries(docs).map(([k, v])=>[
                    +k,
                    v
                ])),
            orderedDocsToRemove: new Map(),
            orderedDocs,
            type
        };
        return acc;
    }, {});
    return {
        sharedInternalDocumentStore,
        language: rawDocument.language,
        sortableProperties: rawDocument.sortableProperties,
        sortablePropertiesWithTypes: rawDocument.sortablePropertiesWithTypes,
        sorts,
        enabled: true,
        isSorted: rawDocument.isSorted,
        unsortableProperties: rawDocument.unsortableProperties ?? []
    };
}
async function loadAsync(sharedInternalDocumentStore, raw) {
    const rawDocument = raw;
    if (!rawDocument.enabled) {
        return {
            enabled: false
        };
    }
    const sorts = {};
    const props = Object.keys(rawDocument.sorts);
    for(let i = 0; i < props.length; i++){
        const prop = props[i];
        const { docs, orderedDocs, type } = rawDocument.sorts[prop];
        sorts[prop] = {
            docs: new Map(Object.entries(docs).map(([k, v])=>[
                    +k,
                    v
                ])),
            orderedDocsToRemove: new Map(),
            orderedDocs,
            type
        };
        if (i < props.length - 1) {
            await (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$utils$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["yieldToEventLoop"])();
        }
    }
    return {
        sharedInternalDocumentStore,
        language: rawDocument.language,
        sortableProperties: rawDocument.sortableProperties,
        sortablePropertiesWithTypes: rawDocument.sortablePropertiesWithTypes,
        sorts,
        enabled: true,
        isSorted: rawDocument.isSorted,
        unsortableProperties: rawDocument.unsortableProperties ?? []
    };
}
function save(sorter) {
    if (!sorter.enabled) {
        return {
            enabled: false
        };
    }
    ensureOrderedDocsAreDeleted(sorter);
    ensureIsSorted(sorter);
    const sorts = Object.keys(sorter.sorts).reduce((acc, prop)=>{
        const { docs, orderedDocs, type } = sorter.sorts[prop];
        acc[prop] = {
            docs: Object.fromEntries(docs.entries()),
            orderedDocs,
            type
        };
        return acc;
    }, {});
    return {
        language: sorter.language,
        sortableProperties: sorter.sortableProperties,
        sortablePropertiesWithTypes: sorter.sortablePropertiesWithTypes,
        sorts,
        enabled: sorter.enabled,
        isSorted: sorter.isSorted,
        unsortableProperties: sorter.unsortableProperties ?? []
    };
}
function createSorter() {
    return {
        create,
        insert,
        remove,
        save,
        load,
        loadAsync,
        sortBy,
        getSortableProperties,
        getSortablePropertiesWithTypes
    };
}
}),
"[project]/node_modules/zbsearch/dist/browser/components/tokenizer/diacritics.js [app-client] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "replaceDiacritics",
    ()=>replaceDiacritics
]);
const DIACRITICS_CHARCODE_START = 192;
const DIACRITICS_CHARCODE_END = 383;
const CHARCODE_REPLACE_MAPPING = [
    65,
    65,
    65,
    65,
    65,
    65,
    65,
    67,
    69,
    69,
    69,
    69,
    73,
    73,
    73,
    73,
    69,
    78,
    79,
    79,
    79,
    79,
    79,
    null,
    79,
    85,
    85,
    85,
    85,
    89,
    80,
    115,
    97,
    97,
    97,
    97,
    97,
    97,
    97,
    99,
    101,
    101,
    101,
    101,
    105,
    105,
    105,
    105,
    101,
    110,
    111,
    111,
    111,
    111,
    111,
    null,
    111,
    117,
    117,
    117,
    117,
    121,
    112,
    121,
    65,
    97,
    65,
    97,
    65,
    97,
    67,
    99,
    67,
    99,
    67,
    99,
    67,
    99,
    68,
    100,
    68,
    100,
    69,
    101,
    69,
    101,
    69,
    101,
    69,
    101,
    69,
    101,
    71,
    103,
    71,
    103,
    71,
    103,
    71,
    103,
    72,
    104,
    72,
    104,
    73,
    105,
    73,
    105,
    73,
    105,
    73,
    105,
    73,
    105,
    73,
    105,
    74,
    106,
    75,
    107,
    107,
    76,
    108,
    76,
    108,
    76,
    108,
    76,
    108,
    76,
    108,
    78,
    110,
    78,
    110,
    78,
    110,
    110,
    78,
    110,
    79,
    111,
    79,
    111,
    79,
    111,
    79,
    111,
    82,
    114,
    82,
    114,
    82,
    114,
    83,
    115,
    83,
    115,
    83,
    115,
    83,
    115,
    84,
    116,
    84,
    116,
    84,
    116,
    85,
    117,
    85,
    117,
    85,
    117,
    85,
    117,
    85,
    117,
    85,
    117,
    87,
    119,
    89,
    121,
    89,
    90,
    122,
    90,
    122,
    90,
    122,
    115
];
// Non-Latin foldings the Latin table above cannot cover. These unify common orthographic variants so that, e.g., Russian "ёлка"/"елка" and Arabic "آلاف"/"الاف" converge to the same token.
const EXTRA_FOLDINGS = {
    1025: 1045,
    1105: 1077,
    1570: 1575,
    1571: 1575,
    1573: 1575,
    1649: 1575,
    1609: 1610
};
function replaceChar(charCode) {
    if (charCode < DIACRITICS_CHARCODE_START) return charCode;
    if (charCode <= DIACRITICS_CHARCODE_END) {
        /* c8 ignore next  */ return CHARCODE_REPLACE_MAPPING[charCode - DIACRITICS_CHARCODE_START] || charCode;
    }
    return EXTRA_FOLDINGS[charCode] ?? charCode;
}
function replaceDiacritics(str) {
    const len = str.length;
    for(let idx = 0; idx < len; idx++){
        const charCode = str.charCodeAt(idx);
        if (charCode < DIACRITICS_CHARCODE_START) {
            continue;
        }
        const replaced = replaceChar(charCode);
        if (replaced === charCode) {
            continue;
        }
        const codes = new Array(len);
        for(let j = 0; j < idx; j++){
            codes[j] = str.charCodeAt(j);
        }
        codes[idx] = replaced;
        for(let j = idx + 1; j < len; j++){
            codes[j] = replaceChar(str.charCodeAt(j));
        }
        return String.fromCharCode(...codes);
    }
    return str;
}
}),
"[project]/node_modules/zbsearch/dist/browser/components/tokenizer/english-stemmer.js [app-client] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "stemmer",
    ()=>stemmer
]);
// eslint-disable-next-line @typescript-eslint/ban-ts-comment
// @ts-nocheck
const step2List = {
    ational: "ate",
    tional: "tion",
    enci: "ence",
    anci: "ance",
    izer: "ize",
    bli: "ble",
    alli: "al",
    entli: "ent",
    eli: "e",
    ousli: "ous",
    ization: "ize",
    ation: "ate",
    ator: "ate",
    alism: "al",
    iveness: "ive",
    fulness: "ful",
    ousness: "ous",
    aliti: "al",
    iviti: "ive",
    biliti: "ble",
    logi: "log"
};
const step3List = {
    icate: "ic",
    ative: "",
    alize: "al",
    iciti: "ic",
    ical: "ic",
    ful: "",
    ness: ""
};
// Consonant
const c = "[^aeiou]";
// Vowel
const v = "[aeiouy]";
// Consonant sequence
const C = c + "[^aeiouy]*";
// Vowel sequence
const V = v + "[aeiou]*";
// [C]VC... is m>0
const mgr0 = "^(" + C + ")?" + V + C;
// [C]VC[V] is m=1
const meq1 = "^(" + C + ")?" + V + C + "(" + V + ")?$";
// [C]VCVC... is m>1
const mgr1 = "^(" + C + ")?" + V + C + V + C;
// vowel in stem
const s_v = "^(" + C + ")?" + v;
function stemmer(w) {
    let stem;
    let suffix;
    let re;
    let re2;
    let re3;
    let re4;
    if (w.length < 3) {
        return w;
    }
    const firstch = w.substring(0, 1);
    if (firstch == "y") {
        w = firstch.toUpperCase() + w.substring(1);
    }
    re = /^(.+?)(ss|i)es$/;
    re2 = /^(.+?)([^s])s$/;
    if (re.test(w)) {
        w = w.replace(re, "$1$2");
    } else if (re2.test(w)) {
        w = w.replace(re2, "$1$2");
    }
    re = /^(.+?)eed$/;
    re2 = /^(.+?)(ed|ing)$/;
    if (re.test(w)) {
        const fp = re.exec(w);
        re = new RegExp(mgr0);
        if (re.test(fp[1])) {
            re = /.$/;
            w = w.replace(re, "");
        }
    } else if (re2.test(w)) {
        const fp = re2.exec(w);
        stem = fp[1];
        re2 = new RegExp(s_v);
        if (re2.test(stem)) {
            w = stem;
            re2 = /(at|bl|iz)$/;
            re3 = new RegExp("([^aeiouylsz])\\1$");
            re4 = new RegExp("^" + C + v + "[^aeiouwxy]$");
            if (re2.test(w)) {
                w = w + "e";
            } else if (re3.test(w)) {
                re = /.$/;
                w = w.replace(re, "");
            } else if (re4.test(w)) {
                w = w + "e";
            }
        }
    }
    re = /^(.+?)y$/;
    if (re.test(w)) {
        const fp = re.exec(w);
        stem = fp?.[1];
        re = new RegExp(s_v);
        if (stem && re.test(stem)) {
            w = stem + "i";
        }
    }
    re = /^(.+?)(ational|tional|enci|anci|izer|bli|alli|entli|eli|ousli|ization|ation|ator|alism|iveness|fulness|ousness|aliti|iviti|biliti|logi)$/;
    if (re.test(w)) {
        const fp = re.exec(w);
        stem = fp?.[1];
        suffix = fp?.[2];
        re = new RegExp(mgr0);
        if (stem && re.test(stem)) {
            // eslint-disable-next-line @typescript-eslint/ban-ts-comment
            // @ts-ignore
            w = stem + step2List[suffix];
        }
    }
    re = /^(.+?)(icate|ative|alize|iciti|ical|ful|ness)$/;
    if (re.test(w)) {
        const fp = re.exec(w);
        stem = fp?.[1];
        suffix = fp?.[2];
        re = new RegExp(mgr0);
        // eslint-disable-next-line @typescript-eslint/ban-ts-comment
        // @ts-ignore
        if (stem && re.test(stem)) {
            // eslint-disable-next-line @typescript-eslint/ban-ts-comment
            // @ts-ignore
            w = stem + step3List[suffix];
        }
    }
    re = /^(.+?)(al|ance|ence|er|ic|able|ible|ant|ement|ment|ent|ou|ism|ate|iti|ous|ive|ize)$/;
    re2 = /^(.+?)(s|t)(ion)$/;
    if (re.test(w)) {
        const fp = re.exec(w);
        stem = fp?.[1];
        re = new RegExp(mgr1);
        if (stem && re.test(stem)) {
            w = stem;
        }
    } else if (re2.test(w)) {
        const fp = re2.exec(w);
        stem = (fp?.[1] ?? "") + (fp?.[2] ?? "");
        re2 = new RegExp(mgr1);
        if (re2.test(stem)) {
            w = stem;
        }
    }
    re = /^(.+?)e$/;
    if (re.test(w)) {
        const fp = re.exec(w);
        stem = fp?.[1];
        re = new RegExp(mgr1);
        re2 = new RegExp(meq1);
        re3 = new RegExp("^" + C + v + "[^aeiouwxy]$");
        if (stem && (re.test(stem) || re2.test(stem) && !re3.test(stem))) {
            w = stem;
        }
    }
    re = /ll$/;
    re2 = new RegExp(mgr1);
    if (re.test(w) && re2.test(w)) {
        re = /.$/;
        w = w.replace(re, "");
    }
    if (firstch == "y") {
        w = firstch.toLowerCase() + w.substring(1);
    }
    return w;
}
}),
"[project]/node_modules/zbsearch/dist/browser/components/tokenizer/index.js [app-client] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "createTokenizer",
    ()=>createTokenizer,
    "normalizeToken",
    ()=>normalizeToken
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$errors$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/zbsearch/dist/browser/errors.js [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$components$2f$tokenizer$2f$diacritics$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/zbsearch/dist/browser/components/tokenizer/diacritics.js [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$components$2f$tokenizer$2f$languages$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/zbsearch/dist/browser/components/tokenizer/languages.js [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$components$2f$tokenizer$2f$english$2d$stemmer$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/zbsearch/dist/browser/components/tokenizer/english-stemmer.js [app-client] (ecmascript)");
;
;
;
;
function normalizeToken(prop, token, withCache = true) {
    const key = `${this.language}:${prop}:${token}`;
    if (withCache && this.normalizationCache.has(key)) {
        return this.normalizationCache.get(key);
    }
    // Fold diacritics BEFORE stopword lookup and stemming, so that accented and unaccented surface forms of the same word converge (e.g. Portuguese "pão"/"pao" must neither stem differently nor be treated as a stopword only in one of its two spellings, depending on how the user typed it). `stopWordsSet` is folded to match, see `createTokenizer`.
    if (!__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$components$2f$tokenizer$2f$languages$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["LANGUAGES_WITH_SIGNIFICANT_DIACRITICS"].has(this.language)) {
        token = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$components$2f$tokenizer$2f$diacritics$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["replaceDiacritics"])(token);
    }
    // Remove stopwords if enabled
    if (this.stopWordsSet?.has(token)) {
        if (withCache) {
            this.normalizationCache.set(key, "");
        }
        return "";
    }
    // Apply stemming if enabled
    if (this.stemmer && !this.stemmerSkipProperties.has(prop)) {
        token = this.stemmer(token);
    }
    if (withCache) {
        this.normalizationCache.set(key, token);
    }
    return token;
}
/* c8 ignore next 10 */ function trim(text) {
    while(text[text.length - 1] === ""){
        text.pop();
    }
    while(text[0] === ""){
        text.shift();
    }
    return text;
}
// Fallback for runtimes without Intl.Segmenter: maximal runs of Unicode letters/numbers.
// Less precise than UAX #29 word segmentation (e.g. it keeps "l'amour" whole instead of splitting on the apostrophe) but script-agnostic.
const UNICODE_WORD = /[\p{L}\p{N}]+/gu;
let multilingualSegmenter;
function splitMultilingual(input) {
    if (multilingualSegmenter === undefined && typeof Intl !== "undefined" && typeof Intl.Segmenter === "function") {
        multilingualSegmenter = new Intl.Segmenter(undefined, {
            granularity: "word"
        });
    }
    if (multilingualSegmenter) {
        const parts = [];
        for (const { segment, isWordLike } of multilingualSegmenter.segment(input)){
            if (isWordLike) {
                parts.push(segment.toLowerCase());
            }
        }
        return parts;
    }
    return input.toLowerCase().match(UNICODE_WORD) ?? [];
}
function tokenize(input, language, prop, withCache = true) {
    if (language && language !== this.language) {
        throw (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$errors$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["createError"])("LANGUAGE_NOT_SUPPORTED", language);
    }
    /* c8 ignore next 3 */ if (typeof input !== "string") {
        return [
            input
        ];
    }
    const property = prop ?? "";
    if (prop && this.tokenizeSkipProperties.has(prop)) {
        const token = this.normalizeToken(property, input, withCache);
        return token ? [
            token
        ] : [];
    }
    const parts = this.language === __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$components$2f$tokenizer$2f$languages$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["MULTILINGUAL_LANGUAGE"] ? splitMultilingual(input) : input.toLowerCase().split(__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$components$2f$tokenizer$2f$languages$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["SPLITTERS"][this.language]);
    const tokens = [];
    const partsLength = parts.length;
    for(let i = 0; i < partsLength; i++){
        const part = parts[i];
        if (!part) continue;
        const token = this.normalizeToken(property, part, withCache);
        if (token) {
            tokens.push(token);
        }
    }
    const trimTokens = trim(tokens);
    if (!this.allowDuplicates) {
        return Array.from(new Set(trimTokens));
    }
    return trimTokens;
}
function createTokenizer(config = {}) {
    if (!config.language) {
        config.language = "english";
    } else if (config.language !== __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$components$2f$tokenizer$2f$languages$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["MULTILINGUAL_LANGUAGE"] && !__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$components$2f$tokenizer$2f$languages$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["SUPPORTED_LANGUAGES"].includes(config.language)) {
        throw (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$errors$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["createError"])("LANGUAGE_NOT_SUPPORTED", config.language);
    }
    // Handle stemming - It is disabled by default
    let stemmer;
    if (config.stemming || config.stemmer && !("stemming" in config)) {
        if (config.stemmer) {
            if (typeof config.stemmer !== "function") {
                throw (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$errors$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["createError"])("INVALID_STEMMER_FUNCTION_TYPE");
            }
            stemmer = config.stemmer;
        } else {
            if (config.language === "english") {
                stemmer = __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$components$2f$tokenizer$2f$english$2d$stemmer$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["stemmer"];
            } else {
                throw (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$errors$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["createError"])("MISSING_STEMMER", config.language);
            }
        }
    }
    // Handle stopwords
    let stopWords;
    if (config.stopWords !== false) {
        stopWords = [];
        if (Array.isArray(config.stopWords)) {
            stopWords = config.stopWords;
        } else if (typeof config.stopWords === "function") {
            stopWords = config.stopWords(stopWords);
        } else if (config.stopWords) {
            throw (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$errors$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["createError"])("CUSTOM_STOP_WORDS_MUST_BE_FUNCTION_OR_ARRAY");
        }
        // Make sure stopWords is just an array of strings
        if (!Array.isArray(stopWords)) {
            throw (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$errors$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["createError"])("CUSTOM_STOP_WORDS_MUST_BE_FUNCTION_OR_ARRAY");
        }
        for (const s of stopWords){
            if (typeof s !== "string") {
                throw (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$errors$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["createError"])("CUSTOM_STOP_WORDS_MUST_BE_FUNCTION_OR_ARRAY");
            }
        }
    }
    // Create the tokenizer
    const tokenizer = {
        tokenize,
        language: config.language,
        stemmer,
        stemmerSkipProperties: new Set(config.stemmerSkipProperties ? [
            config.stemmerSkipProperties
        ].flat() : []),
        tokenizeSkipProperties: new Set(config.tokenizeSkipProperties ? [
            config.tokenizeSkipProperties
        ].flat() : []),
        stopWords,
        stopWordsSet: stopWords ? new Set(__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$components$2f$tokenizer$2f$languages$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["LANGUAGES_WITH_SIGNIFICANT_DIACRITICS"].has(config.language) ? stopWords : stopWords.map(__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$components$2f$tokenizer$2f$diacritics$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["replaceDiacritics"])) : undefined,
        allowDuplicates: Boolean(config.allowDuplicates),
        normalizeToken,
        normalizationCache: new Map()
    };
    tokenizer.tokenize = tokenize.bind(tokenizer);
    tokenizer.normalizeToken = normalizeToken;
    return tokenizer;
}
}),
"[project]/node_modules/zbsearch/dist/browser/components/tokenizer/languages.js [app-client] (ecmascript)", ((__turbopack_context__) => {
"use strict";

// Maps each supported language to its BCP-47 locale tag.
// The keys are the canonical language names accepted by ZBSearch (they define
// `SUPPORTED_LANGUAGES` and the `Language` type); the values are passed to
// `String.prototype.localeCompare` for locale-aware string sorting (see `getLocale`).
// Note: these locale tags are intentionally decoupled from the file-id codes used by
// `@zbsearch/stemmers` / `@zbsearch/stopwords` (e.g. Danish stems live in `dk.js` but the
// locale is `da`), so keep them as valid BCP-47 primary subtags.
__turbopack_context__.s([
    "LANGUAGES_WITH_SIGNIFICANT_DIACRITICS",
    ()=>LANGUAGES_WITH_SIGNIFICANT_DIACRITICS,
    "MULTILINGUAL_LANGUAGE",
    ()=>MULTILINGUAL_LANGUAGE,
    "SPLITTERS",
    ()=>SPLITTERS,
    "SUPPORTED_LANGUAGES",
    ()=>SUPPORTED_LANGUAGES,
    "SUPPORTED_LANGUAGE_LOCALES",
    ()=>SUPPORTED_LANGUAGE_LOCALES,
    "getLocale",
    ()=>getLocale
]);
const SUPPORTED_LANGUAGE_LOCALES = {
    arabic: "ar",
    armenian: "hy",
    bulgarian: "bg",
    czech: "cs",
    danish: "da",
    dutch: "nl",
    english: "en",
    finnish: "fi",
    french: "fr",
    german: "de",
    greek: "el",
    hungarian: "hu",
    indian: "hi",
    indonesian: "id",
    irish: "ga",
    italian: "it",
    lithuanian: "lt",
    nepali: "ne",
    norwegian: "no",
    portuguese: "pt",
    romanian: "ro",
    russian: "ru",
    serbian: "sr",
    slovak: "sk",
    slovenian: "sl",
    spanish: "es",
    swedish: "sv",
    tamil: "ta",
    turkish: "tr",
    ukrainian: "uk",
    vietnamese: "vi",
    sanskrit: "sa"
};
const MULTILINGUAL_LANGUAGE = "multilingual";
// The letters `replaceDiacritics` can fold to plain ASCII: the Latin-1 Supplement and Latin
// Extended-A blocks (U+00C0–U+017F), minus the × (U+00D7) and ÷ (U+00F7) math symbols that sit
// inside them and must keep splitting words. Every splitter has to accept these as word
// characters, including the ones for languages that do not use them: a splitter only whitelists
// its own alphabet, so an unlisted accent does not merely survive into the token, it cuts the
// word in half ("gâteau" -> "g", "teau" under the English splitter) during the split — long
// before `normalizeToken` gets a chance to fold it. Keep in sync with the tables in
// `diacritics.ts`.
const FOLDABLE_LETTERS = "\\u00C0-\\u00D6\\u00D8-\\u00F6\\u00F8-\\u017F";
// Per-language alphabets, as character-class bodies. `FOLDABLE_LETTERS` is *prepended* to each one rather than appended, because several of them end in a literal `-` that would otherwise be read as the start of a range.
const SPLITTER_ALPHABETS = {
    dutch: "A-Za-zàèéìòóù0-9_'-",
    english: "A-Za-zàèéìòóù0-9_'-",
    french: "a-z0-9äâàéèëêïîöôùüûœç-",
    italian: "A-Za-zàèéìòóù0-9_'-",
    norwegian: "a-z0-9_æøåÆØÅäÄöÖüÜ",
    portuguese: "a-z0-9à-úÀ-Ú",
    russian: "a-z0-9а-яА-ЯёЁ",
    spanish: "a-z0-9A-Zá-úÁ-ÚñÑüÜ",
    swedish: "a-z0-9_åÅäÄöÖüÜ-",
    german: "a-z0-9A-ZäöüÄÖÜß",
    finnish: "a-z0-9äöÄÖ",
    danish: "a-z0-9æøåÆØÅ",
    hungarian: "a-z0-9áéíóöőúüűÁÉÍÓÖŐÚÜŰ",
    romanian: "a-z0-9ăâîșțĂÂÎȘȚ",
    serbian: "a-z0-9čćžšđČĆŽŠĐ",
    turkish: "a-z0-9çÇğĞıİöÖşŞüÜ",
    lithuanian: "a-z0-9ąčęėįšųūžĄČĘĖĮŠŲŪŽ",
    // U+0671 (ٱ) sits outside the ء-ي range but is folded by `EXTRA_FOLDINGS`, so it belongs here.
    arabic: "a-z0-9ء-ي\\u0671",
    nepali: "a-z0-9अ-ह",
    irish: "a-z0-9áéíóúÁÉÍÓÚ",
    indian: "a-z0-9अ-ह",
    armenian: "a-z0-9ա-ֆ",
    greek: "a-z0-9α-ωά-ώ",
    indonesian: "a-z0-9",
    ukrainian: "a-z0-9а-яА-ЯіїєІЇЄ",
    slovenian: "a-z0-9čžšČŽŠ",
    bulgarian: "a-z0-9а-яА-Я",
    tamil: "a-z0-9அ-ஹ",
    sanskrit: "a-z0-9A-Zāīūṛḷṃṁḥśṣṭḍṇṅñḻḹṝ",
    vietnamese: "a-z0-9A-ZáàảãạăắằẳẵặâấầẩẫậéèẻẽẹêếềểễệíìỉĩịóòỏõọôốồổỗộơớờởỡợúùủũụưứừửữựýỳỷỹỵđÁÀẢÃẠĂẮẰẲẴẶÂẤẦẨẪẬÉÈẺẼẸÊẾỀỂỄỆÍÌỈĨỊÓÒỎÕỌÔỐỒỔỖỘƠỚỜỞỠỢÚÙỦŨỤƯỨỪỬỮỰÝỲỶỸỴĐ_",
    czech: "A-Z0-9a-zěščřžýáíéúůóťďĚŠČŘŽÝÁÍÉÓÚŮŤĎ-",
    slovak: "A-Z0-9a-záäčďéíĺľňóôŕšťúýžÁÄČĎÉÍĹĽŇÓÔŔŠŤÚÝŽ-"
};
const SPLITTERS = Object.fromEntries(Object.entries(SPLITTER_ALPHABETS).map(([language, alphabet])=>[
        language,
        new RegExp(`[^${FOLDABLE_LETTERS}${alphabet}]+`, "gim")
    ]));
const SUPPORTED_LANGUAGES = Object.keys(SUPPORTED_LANGUAGE_LOCALES);
function getLocale(language) {
    return language !== undefined && SUPPORTED_LANGUAGES.includes(language) ? SUPPORTED_LANGUAGE_LOCALES[language] : undefined;
}
const LANGUAGES_WITH_SIGNIFICANT_DIACRITICS = new Set([
    "vietnamese"
]);
}),
"[project]/node_modules/zbsearch/dist/browser/constants.js [app-client] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "MODE_FULLTEXT_SEARCH",
    ()=>MODE_FULLTEXT_SEARCH,
    "MODE_HYBRID_SEARCH",
    ()=>MODE_HYBRID_SEARCH,
    "MODE_VECTOR_SEARCH",
    ()=>MODE_VECTOR_SEARCH,
    "RESERVED_VECTOR_INDEX_KEY",
    ()=>RESERVED_VECTOR_INDEX_KEY
]);
const MODE_FULLTEXT_SEARCH = "fulltext";
const MODE_HYBRID_SEARCH = "hybrid";
const MODE_VECTOR_SEARCH = "vector";
const RESERVED_VECTOR_INDEX_KEY = "__vector";
}),
"[project]/node_modules/zbsearch/dist/browser/errors.js [app-client] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "createError",
    ()=>createError
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$components$2f$tokenizer$2f$languages$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/zbsearch/dist/browser/components/tokenizer/languages.js [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$utils$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/zbsearch/dist/browser/utils.js [app-client] (ecmascript)");
;
;
const allLanguages = [
    ...__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$components$2f$tokenizer$2f$languages$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["SUPPORTED_LANGUAGES"],
    __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$components$2f$tokenizer$2f$languages$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["MULTILINGUAL_LANGUAGE"]
].join("\n - ");
const errors = {
    NO_LANGUAGE_WITH_CUSTOM_TOKENIZER: "Do not pass the language option to create when using a custom tokenizer.",
    LANGUAGE_NOT_SUPPORTED: `Language "%s" is not supported.\nSupported languages are:\n - ${allLanguages}`,
    INVALID_STEMMER_FUNCTION_TYPE: `config.stemmer property must be a function.`,
    MISSING_STEMMER: `As of version 1.0.0 zbsearch does not ship non English stemmers by default. To solve this, please explicitly import and specify the "%s" stemmer from the package @zbsearch/stemmers. See https://docs.zbsearch.com/docs/zbsearch-js/text-analysis/stemming for more information.`,
    CUSTOM_STOP_WORDS_MUST_BE_FUNCTION_OR_ARRAY: "Custom stop words array must only contain strings.",
    UNSUPPORTED_COMPONENT: `Unsupported component "%s".`,
    COMPONENT_MUST_BE_FUNCTION: `The component "%s" must be a function.`,
    COMPONENT_MUST_BE_FUNCTION_OR_ARRAY_FUNCTIONS: `The component "%s" must be a function or an array of functions.`,
    INVALID_SCHEMA_TYPE: `Unsupported schema type "%s" at "%s". Expected "string", "boolean" or "number" or array of them.`,
    DOCUMENT_ID_MUST_BE_STRING: `Document id must be of type "string". Got "%s" instead.`,
    DOCUMENT_ALREADY_EXISTS: `A document with id "%s" already exists.`,
    DOCUMENT_DOES_NOT_EXIST: `A document with id "%s" does not exists.`,
    MISSING_DOCUMENT_PROPERTY: `Missing searchable property "%s".`,
    INVALID_DOCUMENT_PROPERTY: `Invalid document property "%s": expected "%s", got "%s"`,
    UNKNOWN_INDEX: `Invalid property name "%s". Expected a wildcard string ("*") or array containing one of the following properties: %s`,
    INVALID_BOOST_VALUE: `Boost value must be a number greater than, or less than 0.`,
    INVALID_FILTER_OPERATION: `You can only use one operation per filter, you requested %d.`,
    SCHEMA_VALIDATION_FAILURE: `Cannot insert document due schema validation failure on "%s" property.`,
    INVALID_BATCH_SIZE: `Batch size must be an integer greater than 0. Got "%s" instead.`,
    INVALID_SORT_SCHEMA_TYPE: `Unsupported sort schema type "%s" at "%s". Expected "string" or "number".`,
    CANNOT_SORT_BY_ARRAY: `Cannot configure sort for "%s" because it is an array (%s).`,
    UNABLE_TO_SORT_ON_UNKNOWN_FIELD: `Unable to sort on unknown field "%s". Allowed fields: %s`,
    SORT_DISABLED: `Sort is disabled. Please read the documentation at https://docs.zbsearch.com/docs/zbsearch-js for more information.`,
    UNKNOWN_GROUP_BY_PROPERTY: `Unknown groupBy property "%s".`,
    INVALID_GROUP_BY_PROPERTY: `Invalid groupBy property "%s". Allowed types: "%s", but given "%s".`,
    UNKNOWN_FILTER_PROPERTY: `Unknown filter property "%s".`,
    UNKNOWN_VECTOR_PROPERTY: `Unknown vector property "%s". Make sure the property exists in the schema and is configured as a vector.`,
    INVALID_VECTOR_SIZE: `Vector size must be a number greater than 0. Got "%s" instead.`,
    INVALID_VECTOR_VALUE: `Vector value must be a number greater than 0. Got "%s" instead.`,
    INVALID_INPUT_VECTOR: `Property "%s" was declared as a %s-dimensional vector, but got a %s-dimensional vector instead.\nInput vectors must be of the size declared in the schema, as calculating similarity between vectors of different sizes can lead to unexpected results.`,
    WRONG_SEARCH_PROPERTY_TYPE: `Property "%s" is not searchable. Only "string" properties are searchable.`,
    FACET_NOT_SUPPORTED: `Facet doens't support the type "%s".`,
    INVALID_DISTANCE_SUFFIX: `Invalid distance suffix "%s". Valid suffixes are: cm, m, km, mi, yd, ft.`,
    INVALID_SEARCH_MODE: `Invalid search mode "%s". Valid modes are: "fulltext", "vector", "hybrid".`,
    MISSING_VECTOR_AND_SECURE_PROXY: `No vector was provided and no secure proxy was configured. Please provide a vector or configure an ZBSearch Secure Proxy to perform hybrid search.`,
    MISSING_TERM: `"term" is a required parameter when performing hybrid search. Please provide a search term.`,
    INVALID_VECTOR_INPUT: `Invalid "vector" property. Expected an object with "value" and "property" properties, but got "%s" instead.`,
    PLUGIN_CRASHED: `A plugin crashed during initialization. Please check the error message for more information:`,
    PLUGIN_SECURE_PROXY_NOT_FOUND: `Could not find '@zbsearch/secure-proxy-plugin' installed in your ZBSearch instance.\nPlease install it before proceeding with creating an answer session.\nRead more at https://docs.zbsearch.com/docs/zbsearch-js/plugins/plugin-secure-proxy#plugin-secure-proxy\n`,
    PLUGIN_SECURE_PROXY_MISSING_CHAT_MODEL: `Could not find a chat model defined in the secure proxy plugin configuration.\nPlease provide a chat model before proceeding with creating an answer session.\nRead more at https://docs.zbsearch.com/docs/zbsearch-js/plugins/plugin-secure-proxy#plugin-secure-proxy\n`,
    ANSWER_SESSION_LAST_MESSAGE_IS_NOT_ASSISTANT: `The last message in the session is not an assistant message. Cannot regenerate non-assistant messages.`,
    PLUGIN_COMPONENT_CONFLICT: `The component "%s" is already defined. The plugin "%s" is trying to redefine it.`,
    IVF_INDEX_REQUIRES_FACTORY: `Vector index "%s" was serialized as IVF. Import ivf from "zbsearch/trees/vector-ivf" and pass indexes.%s at create().`,
    RESERVED_SCHEMA_PROPERTY: `"%s" is a reserved property name and cannot be used in the schema.`,
    SUGGEST_NOT_SUPPORTED: `The configured index component does not support "suggest", as it cannot expand a query token into the indexed words it matches.`
};
function createError(code, ...args) {
    const error = new Error((0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$utils$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["sprintf"])(errors[code] ?? `Unsupported ZBSearch Error code: ${code}`, ...args));
    error.code = code;
    const errorCtor = Error;
    if (typeof errorCtor.captureStackTrace === "function") {
        errorCtor.captureStackTrace(error);
    }
    return error;
}
}),
"[project]/node_modules/zbsearch/dist/browser/index.js [app-client] (ecmascript) <locals>", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([]);
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$methods$2f$create$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/zbsearch/dist/browser/methods/create.js [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$methods$2f$insert$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/zbsearch/dist/browser/methods/insert.js [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$methods$2f$remove$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/zbsearch/dist/browser/methods/remove.js [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$methods$2f$search$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/zbsearch/dist/browser/methods/search.js [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$methods$2f$search$2d$vector$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/zbsearch/dist/browser/methods/search-vector.js [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$methods$2f$serialization$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/zbsearch/dist/browser/methods/serialization.js [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$methods$2f$suggest$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/zbsearch/dist/browser/methods/suggest.js [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$methods$2f$update$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/zbsearch/dist/browser/methods/update.js [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$methods$2f$upsert$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/zbsearch/dist/browser/methods/upsert.js [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$types$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__$3c$locals$3e$__ = __turbopack_context__.i("[project]/node_modules/zbsearch/dist/browser/types.js [app-client] (ecmascript) <locals>");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$components$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__$3c$locals$3e$__ = __turbopack_context__.i("[project]/node_modules/zbsearch/dist/browser/components.js [app-client] (ecmascript) <locals>");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$internals$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__$3c$locals$3e$__ = __turbopack_context__.i("[project]/node_modules/zbsearch/dist/browser/internals.js [app-client] (ecmascript) <locals>");
;
;
;
;
;
;
;
;
;
;
;
;
;
;
}),
"[project]/node_modules/zbsearch/dist/browser/internals.js [app-client] (ecmascript) <locals>", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([]);
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$utils$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/zbsearch/dist/browser/utils.js [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$components$2f$tokenizer$2f$index$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/zbsearch/dist/browser/components/tokenizer/index.js [app-client] (ecmascript)");
;
;
;
}),
"[project]/node_modules/zbsearch/dist/browser/methods/create.js [app-client] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "create",
    ()=>create
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$components$2f$defaults$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__$3c$locals$3e$__ = __turbopack_context__.i("[project]/node_modules/zbsearch/dist/browser/components/defaults.js [app-client] (ecmascript) <locals>");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$utils$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/zbsearch/dist/browser/utils.js [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$components$2f$documents$2d$store$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/zbsearch/dist/browser/components/documents-store.js [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$components$2f$hooks$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/zbsearch/dist/browser/components/hooks.js [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$components$2f$index$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/zbsearch/dist/browser/components/index.js [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$components$2f$internal$2d$document$2d$id$2d$store$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/zbsearch/dist/browser/components/internal-document-id-store.js [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$components$2f$pinning$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/zbsearch/dist/browser/components/pinning.js [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$components$2f$plugins$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/zbsearch/dist/browser/components/plugins.js [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$components$2f$sorter$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/zbsearch/dist/browser/components/sorter.js [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$components$2f$tokenizer$2f$index$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/zbsearch/dist/browser/components/tokenizer/index.js [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$errors$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/zbsearch/dist/browser/errors.js [app-client] (ecmascript)");
;
;
;
;
;
;
;
;
;
;
;
function validateComponents(components) {
    const defaultComponents = {
        formatElapsedTime: __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$components$2f$defaults$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__$3c$locals$3e$__["formatElapsedTime"],
        getDocumentIndexId: __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$components$2f$defaults$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__$3c$locals$3e$__["getDocumentIndexId"],
        getDocumentProperties: __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$utils$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["getDocumentProperties"],
        validateSchema: __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$components$2f$defaults$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__$3c$locals$3e$__["validateSchema"]
    };
    for (const rawKey of __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$components$2f$hooks$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["FUNCTION_COMPONENTS"]){
        const key = rawKey;
        if (components[key]) {
            if (typeof components[key] !== "function") {
                throw (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$errors$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["createError"])("COMPONENT_MUST_BE_FUNCTION", key);
            }
        } else {
            components[key] = defaultComponents[key];
        }
    }
    for (const rawKey of Object.keys(components)){
        if (!__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$components$2f$hooks$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["OBJECT_COMPONENTS"].includes(rawKey) && !__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$components$2f$hooks$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["FUNCTION_COMPONENTS"].includes(rawKey)) {
            throw (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$errors$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["createError"])("UNSUPPORTED_COMPONENT", rawKey);
        }
    }
}
function create(args = {}) {
    const { indexes, sort, language, plugins } = args;
    let { id } = args;
    const schema = args.schema ?? {};
    // When no schema is provided, schema inference defaults to on.
    // When a schema is provided, it is enforced strictly unless inference is explicitly enabled for undeclared properties.
    const inferSchema = args.inferSchema ?? args.schema === undefined;
    let components = args.components;
    if (!components) {
        components = {};
    }
    for (const plugin of plugins ?? []){
        if (!("getComponents" in plugin)) {
            continue;
        }
        if (typeof plugin.getComponents !== "function") {
            continue;
        }
        const pluginComponents = plugin.getComponents(schema);
        const keys = Object.keys(pluginComponents);
        for (const key of keys){
            if (components[key]) {
                throw (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$errors$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["createError"])("PLUGIN_COMPONENT_CONFLICT", key, plugin.name);
            }
        }
        components = {
            ...components,
            ...pluginComponents
        };
    }
    if (!id) {
        id = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$utils$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["uniqueId"])();
    }
    (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$components$2f$defaults$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__$3c$locals$3e$__["assertSchemaHasNoReservedKeys"])(schema);
    let tokenizer = components.tokenizer;
    let index = components.index;
    let documentsStore = components.documentsStore;
    let sorter = components.sorter;
    let pinning = components.pinning;
    if (!tokenizer) {
        // Use the default tokenizer
        tokenizer = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$components$2f$tokenizer$2f$index$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["createTokenizer"])({
            language: language ?? "english"
        });
    } else if (!tokenizer.tokenize) {
        // If there is no tokenizer function, we assume this is a TokenizerConfig
        tokenizer = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$components$2f$tokenizer$2f$index$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["createTokenizer"])(tokenizer);
    } else {
        const customTokenizer = tokenizer;
        tokenizer = customTokenizer;
    }
    if (components.tokenizer && language) {
        // Accept language only if a tokenizer is not provided
        throw (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$errors$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["createError"])("NO_LANGUAGE_WITH_CUSTOM_TOKENIZER");
    }
    const internalDocumentStore = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$components$2f$internal$2d$document$2d$id$2d$store$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["createInternalDocumentIDStore"])();
    index ||= (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$components$2f$index$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["createIndex"])();
    sorter ||= (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$components$2f$sorter$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["createSorter"])();
    documentsStore ||= (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$components$2f$documents$2d$store$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["createDocumentsStore"])();
    pinning ||= (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$components$2f$pinning$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["createPinning"])();
    // Validate all other components
    validateComponents(components);
    // Assign only recognized components and hooks
    const { getDocumentProperties, getDocumentIndexId, validateSchema, formatElapsedTime } = components;
    const zbsearch = {
        data: {},
        caches: {},
        schema,
        inferSchema,
        tokenizer,
        index,
        sorter,
        documentsStore,
        pinning,
        internalDocumentIDStore: internalDocumentStore,
        getDocumentProperties,
        getDocumentIndexId,
        validateSchema,
        beforeInsert: [],
        afterInsert: [],
        beforeRemove: [],
        afterRemove: [],
        beforeUpdate: [],
        afterUpdate: [],
        beforeUpsert: [],
        afterUpsert: [],
        beforeSearch: [],
        afterSearch: [],
        beforeInsertMultiple: [],
        afterInsertMultiple: [],
        beforeRemoveMultiple: [],
        afterRemoveMultiple: [],
        beforeUpdateMultiple: [],
        afterUpdateMultiple: [],
        beforeUpsertMultiple: [],
        afterUpsertMultiple: [],
        afterCreate: [],
        formatElapsedTime,
        id,
        indexes,
        plugins,
        version: getVersion()
    };
    zbsearch.data = {
        index: zbsearch.index.create(zbsearch, internalDocumentStore, schema),
        docs: zbsearch.documentsStore.create(zbsearch, internalDocumentStore),
        sorting: zbsearch.sorter.create(zbsearch, internalDocumentStore, schema, sort),
        pinning: zbsearch.pinning.create(internalDocumentStore)
    };
    for (const hook of __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$components$2f$plugins$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["AVAILABLE_PLUGIN_HOOKS"]){
        zbsearch[hook] = (zbsearch[hook] ?? []).concat((0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$components$2f$plugins$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["getAllPluginsByHook"])(zbsearch, hook));
    }
    const afterCreate = zbsearch["afterCreate"];
    if (afterCreate) {
        (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$components$2f$hooks$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["runAfterCreate"])(afterCreate, zbsearch);
    }
    return zbsearch;
}
function getVersion() {
    return "{{VERSION}}";
}
}),
"[project]/node_modules/zbsearch/dist/browser/methods/docs.js [app-client] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "count",
    ()=>count,
    "getByID",
    ()=>getByID
]);
function getByID(db, id) {
    return db.documentsStore.get(db.data.docs, id);
}
function count(db) {
    return db.documentsStore.count(db.data.docs);
}
}),
"[project]/node_modules/zbsearch/dist/browser/methods/fetch-documents.js [app-client] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "fetchDocuments",
    ()=>fetchDocuments,
    "fetchDocumentsWithDistinct",
    ()=>fetchDocumentsWithDistinct
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$components$2f$internal$2d$document$2d$id$2d$store$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/zbsearch/dist/browser/components/internal-document-id-store.js [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$utils$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/zbsearch/dist/browser/utils.js [app-client] (ecmascript)");
;
;
function fetchDocumentsWithDistinct(zbsearch, uniqueDocsArray, offset, limit, distinctOn) {
    const docs = zbsearch.data.docs;
    // Keep track which values we already seen
    const values = new Map();
    // We cannot know how many results we will have in the end,
    // so we need cannot pre-allocate the array.
    const results = [];
    const resultIDs = new Set();
    const uniqueDocsArrayLength = uniqueDocsArray.length;
    let count = 0;
    for(let i = 0; i < uniqueDocsArrayLength; i++){
        const idAndScore = uniqueDocsArray[i];
        // If there are no more results, just break the loop
        if (typeof idAndScore === "undefined") {
            continue;
        }
        const [id, score] = idAndScore;
        if (resultIDs.has(id)) {
            continue;
        }
        const doc = zbsearch.documentsStore.get(docs, id);
        const value = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$utils$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["getNested"])(doc, distinctOn);
        if (typeof value === "undefined" || values.has(value)) {
            continue;
        }
        values.set(value, true);
        count++;
        // We shouldn't consider the document if it's not in the offset range
        if (count <= offset) {
            continue;
        }
        results.push({
            id: (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$components$2f$internal$2d$document$2d$id$2d$store$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["getDocumentIdFromInternalId"])(zbsearch.internalDocumentIDStore, id),
            score,
            document: doc
        });
        resultIDs.add(id);
        // reached the limit, break the loop
        if (count >= offset + limit) {
            break;
        }
    }
    return results;
}
function fetchDocuments(zbsearch, uniqueDocsArray, offset, limit) {
    const docs = zbsearch.data.docs;
    const results = [];
    const end = Math.min(offset + limit, uniqueDocsArray.length);
    for(let i = offset; i < end; i++){
        const idAndScore = uniqueDocsArray[i];
        if (typeof idAndScore === "undefined") {
            break;
        }
        const [id, score] = idAndScore;
        const fullDoc = zbsearch.documentsStore.get(docs, id);
        results.push({
            id: (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$components$2f$internal$2d$document$2d$id$2d$store$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["getDocumentIdFromInternalId"])(zbsearch.internalDocumentIDStore, id),
            score,
            document: fullDoc
        });
    }
    return results;
}
}),
"[project]/node_modules/zbsearch/dist/browser/methods/insert.js [app-client] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "innerInsertMultiple",
    ()=>innerInsertMultiple,
    "insert",
    ()=>insert,
    "insertMultiple",
    ()=>insertMultiple,
    "insertMultipleAsync",
    ()=>insertMultipleAsync
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$components$2f$hooks$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/zbsearch/dist/browser/components/hooks.js [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$components$2f$inference$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/zbsearch/dist/browser/components/inference.js [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$components$2f$internal$2d$document$2d$id$2d$store$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/zbsearch/dist/browser/components/internal-document-id-store.js [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$components$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__$3c$locals$3e$__ = __turbopack_context__.i("[project]/node_modules/zbsearch/dist/browser/components.js [app-client] (ecmascript) <locals>");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$components$2f$defaults$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__$3c$locals$3e$__ = __turbopack_context__.i("[project]/node_modules/zbsearch/dist/browser/components/defaults.js [app-client] (ecmascript) <locals>");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$errors$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/zbsearch/dist/browser/errors.js [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$utils$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/zbsearch/dist/browser/utils.js [app-client] (ecmascript)");
;
;
;
;
;
;
function insert(zbsearch, doc, language, skipHooks, options) {
    (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$components$2f$inference$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["inferSchemaFromDocument"])(zbsearch, doc);
    const errorProperty = zbsearch.validateSchema(doc, zbsearch.schema);
    if (errorProperty) {
        throw (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$errors$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["createError"])("SCHEMA_VALIDATION_FAILURE", errorProperty);
    }
    const asyncNeeded = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$utils$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["isAsyncFunction"])(zbsearch.beforeInsert) || (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$utils$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["isAsyncFunction"])(zbsearch.afterInsert) || (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$utils$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["isAsyncFunction"])(zbsearch.index.beforeInsert) || (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$utils$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["isAsyncFunction"])(zbsearch.index.insert) || (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$utils$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["isAsyncFunction"])(zbsearch.index.afterInsert);
    if (asyncNeeded) {
        return innerInsertAsync(zbsearch, doc, language, skipHooks, options);
    }
    return innerInsertSync(zbsearch, doc, language, skipHooks, options);
}
const ENUM_TYPE = new Set([
    "enum",
    "enum[]"
]);
const STRING_NUMBER_TYPE = new Set([
    "string",
    "number"
]);
async function innerInsertAsync(zbsearch, doc, language, skipHooks, options) {
    const { index, docs } = zbsearch.data;
    const id = zbsearch.getDocumentIndexId(doc);
    if (typeof id !== "string") {
        throw (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$errors$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["createError"])("DOCUMENT_ID_MUST_BE_STRING", typeof id);
    }
    const internalId = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$components$2f$internal$2d$document$2d$id$2d$store$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["getInternalDocumentId"])(zbsearch.internalDocumentIDStore, id);
    if (!skipHooks) {
        await (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$components$2f$hooks$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["runSingleHook"])(zbsearch.beforeInsert, zbsearch, id, doc);
    }
    if (!zbsearch.documentsStore.store(docs, id, internalId, doc)) {
        throw (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$errors$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["createError"])("DOCUMENT_ALREADY_EXISTS", id);
    }
    const docsCount = zbsearch.documentsStore.count(docs);
    const indexableProperties = zbsearch.index.getSearchableProperties(index);
    const indexablePropertiesWithTypes = zbsearch.index.getSearchablePropertiesWithTypes(index);
    const indexableValues = zbsearch.getDocumentProperties(doc, indexableProperties);
    for (const [key, value] of Object.entries(indexableValues)){
        if (typeof value === "undefined") continue;
        const actualType = typeof value;
        const expectedType = indexablePropertiesWithTypes[key];
        validateDocumentProperty(actualType, expectedType, key, value);
    }
    await indexAndSortDocument(zbsearch, id, indexableProperties, indexableValues, docsCount, language, doc, options);
    if (!skipHooks) {
        await (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$components$2f$hooks$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["runSingleHook"])(zbsearch.afterInsert, zbsearch, id, doc);
    }
    return id;
}
function getInsertMetadata(zbsearch) {
    let indexableProperties = zbsearch.caches["indexableProperties"];
    let indexablePropertiesWithTypes = zbsearch.caches["indexablePropertiesWithTypes"];
    let sortableProperties = zbsearch.caches["sortableProperties"];
    let sortablePropertiesWithTypes = zbsearch.caches["sortablePropertiesWithTypes"];
    if (!indexableProperties) {
        indexableProperties = zbsearch.index.getSearchableProperties(zbsearch.data.index);
        indexablePropertiesWithTypes = zbsearch.index.getSearchablePropertiesWithTypes(zbsearch.data.index);
        sortableProperties = zbsearch.sorter.getSortableProperties(zbsearch.data.sorting);
        sortablePropertiesWithTypes = zbsearch.sorter.getSortablePropertiesWithTypes(zbsearch.data.sorting);
        zbsearch.caches["indexableProperties"] = indexableProperties;
        zbsearch.caches["indexablePropertiesWithTypes"] = indexablePropertiesWithTypes;
        zbsearch.caches["sortableProperties"] = sortableProperties;
        zbsearch.caches["sortablePropertiesWithTypes"] = sortablePropertiesWithTypes;
    }
    return {
        indexableProperties,
        indexablePropertiesWithTypes,
        sortableProperties,
        sortablePropertiesWithTypes
    };
}
function innerInsertSync(zbsearch, doc, language, skipHooks, options) {
    const { index: _index, docs } = zbsearch.data;
    const id = zbsearch.getDocumentIndexId(doc);
    if (typeof id !== "string") {
        throw (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$errors$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["createError"])("DOCUMENT_ID_MUST_BE_STRING", typeof id);
    }
    const internalId = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$components$2f$internal$2d$document$2d$id$2d$store$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["getInternalDocumentId"])(zbsearch.internalDocumentIDStore, id);
    if (!skipHooks && zbsearch.beforeInsert?.length) {
        (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$components$2f$hooks$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["runSingleHook"])(zbsearch.beforeInsert, zbsearch, id, doc);
    }
    if (!zbsearch.documentsStore.store(docs, id, internalId, doc)) {
        throw (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$errors$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["createError"])("DOCUMENT_ALREADY_EXISTS", id);
    }
    const docsCount = zbsearch.documentsStore.count(docs);
    const { indexableProperties, indexablePropertiesWithTypes, sortableProperties, sortablePropertiesWithTypes } = getInsertMetadata(zbsearch);
    const indexableValues = zbsearch.getDocumentProperties(doc, indexableProperties);
    for (const [key, value] of Object.entries(indexableValues)){
        if (typeof value === "undefined") continue;
        const actualType = typeof value;
        const expectedType = indexablePropertiesWithTypes[key];
        validateDocumentProperty(actualType, expectedType, key, value);
    }
    indexAndSortDocumentSync(zbsearch, id, indexableProperties, indexableValues, docsCount, language, doc, options, indexablePropertiesWithTypes, sortableProperties, sortablePropertiesWithTypes);
    if (!skipHooks && zbsearch.afterInsert?.length) {
        (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$components$2f$hooks$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["runSingleHook"])(zbsearch.afterInsert, zbsearch, id, doc);
    }
    return id;
}
function validateDocumentProperty(actualType, expectedType, key, value) {
    if ((0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$components$2f$defaults$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__$3c$locals$3e$__["isGeoPointType"])(expectedType) && typeof value === "object" && typeof value.lon === "number" && typeof value.lat === "number") {
        return;
    }
    if ((0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$components$2f$defaults$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__$3c$locals$3e$__["isVectorType"])(expectedType) && Array.isArray(value)) return;
    if ((0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$components$2f$defaults$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__$3c$locals$3e$__["isArrayType"])(expectedType) && Array.isArray(value)) return;
    if (ENUM_TYPE.has(expectedType) && STRING_NUMBER_TYPE.has(actualType)) return;
    if (actualType !== expectedType) {
        throw (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$errors$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["createError"])("INVALID_DOCUMENT_PROPERTY", key, expectedType, actualType);
    }
}
async function indexAndSortDocument(zbsearch, id, indexableProperties, indexableValues, docsCount, language, doc, options) {
    for (const prop of indexableProperties){
        const value = indexableValues[prop];
        if (typeof value === "undefined") continue;
        const expectedType = zbsearch.index.getSearchablePropertiesWithTypes(zbsearch.data.index)[prop];
        await zbsearch.index.beforeInsert?.(zbsearch.data.index, prop, id, value, expectedType, language, zbsearch.tokenizer, docsCount);
        const internalId = zbsearch.internalDocumentIDStore.idToInternalId.get(id);
        await zbsearch.index.insert(zbsearch.index, zbsearch.data.index, prop, id, internalId, value, expectedType, language, zbsearch.tokenizer, docsCount, options);
        await zbsearch.index.afterInsert?.(zbsearch.data.index, prop, id, value, expectedType, language, zbsearch.tokenizer, docsCount);
    }
    const sortableProperties = zbsearch.sorter.getSortableProperties(zbsearch.data.sorting);
    const sortableValues = zbsearch.getDocumentProperties(doc, sortableProperties);
    for (const prop of sortableProperties){
        const value = sortableValues[prop];
        if (typeof value === "undefined") continue;
        const expectedType = zbsearch.sorter.getSortablePropertiesWithTypes(zbsearch.data.sorting)[prop];
        zbsearch.sorter.insert(zbsearch.data.sorting, prop, id, value, expectedType, language);
    }
}
function indexAndSortDocumentSync(zbsearch, id, indexableProperties, indexableValues, docsCount, language, doc, options, cachedIndexTypes, cachedSortableProperties, cachedSortableTypes) {
    const indexTypes = cachedIndexTypes ?? zbsearch.index.getSearchablePropertiesWithTypes(zbsearch.data.index);
    const internalDocumentId = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$components$2f$internal$2d$document$2d$id$2d$store$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["getInternalDocumentId"])(zbsearch.internalDocumentIDStore, id);
    for (const prop of indexableProperties){
        const value = indexableValues[prop];
        if (typeof value === "undefined") continue;
        const expectedType = indexTypes[prop];
        zbsearch.index.beforeInsert?.(zbsearch.data.index, prop, id, value, expectedType, language, zbsearch.tokenizer, docsCount);
        zbsearch.index.insert(zbsearch.index, zbsearch.data.index, prop, id, internalDocumentId, value, expectedType, language, zbsearch.tokenizer, docsCount, options);
        zbsearch.index.afterInsert?.(zbsearch.data.index, prop, id, value, expectedType, language, zbsearch.tokenizer, docsCount);
    }
    const sortableProperties = cachedSortableProperties ?? zbsearch.sorter.getSortableProperties(zbsearch.data.sorting);
    const sortableTypes = cachedSortableTypes ?? zbsearch.sorter.getSortablePropertiesWithTypes(zbsearch.data.sorting);
    const sortableValues = zbsearch.getDocumentProperties(doc, sortableProperties);
    for (const prop of sortableProperties){
        const value = sortableValues[prop];
        if (typeof value === "undefined") continue;
        const expectedType = sortableTypes[prop];
        zbsearch.sorter.insert(zbsearch.data.sorting, prop, id, value, expectedType, language);
    }
}
function insertMultiple(zbsearch, docs, batchSize, language, skipHooks, timeout) {
    const asyncNeeded = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$utils$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["isAsyncFunction"])(zbsearch.afterInsertMultiple) || (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$utils$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["isAsyncFunction"])(zbsearch.beforeInsertMultiple) || (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$utils$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["isAsyncFunction"])(zbsearch.index.beforeInsert) || (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$utils$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["isAsyncFunction"])(zbsearch.index.insert) || (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$utils$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["isAsyncFunction"])(zbsearch.index.afterInsert);
    if (asyncNeeded) {
        return innerInsertMultipleAsync(zbsearch, docs, batchSize, language, skipHooks, timeout);
    }
    return innerInsertMultipleSync(zbsearch, docs, batchSize, language, skipHooks, timeout);
}
async function innerInsertMultipleAsync(zbsearch, docs, batchSize = 1e3, language, skipHooks, timeout = 0) {
    const ids = [];
    const processNextBatch = async (startIndex)=>{
        const endIndex = Math.min(startIndex + batchSize, docs.length);
        const batch = docs.slice(startIndex, endIndex);
        for (const doc of batch){
            const options = {
                avlRebalanceThreshold: batch.length
            };
            const id = await insert(zbsearch, doc, language, skipHooks, options);
            ids.push(id);
        }
        return endIndex;
    };
    const processAllBatches = async ()=>{
        let currentIndex = 0;
        while(currentIndex < docs.length){
            const startTime = Date.now();
            currentIndex = await processNextBatch(currentIndex);
            if (timeout > 0) {
                const elapsedTime = Date.now() - startTime;
                const waitTime = timeout - elapsedTime;
                if (waitTime > 0) {
                    (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$utils$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["sleep"])(waitTime);
                }
            }
        }
    };
    await processAllBatches();
    if (!skipHooks) {
        await (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$components$2f$hooks$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["runMultipleHook"])(zbsearch.afterInsertMultiple, zbsearch, docs);
    }
    return ids;
}
function insertBatchSync(zbsearch, batch, ids, language, skipHooks) {
    let { indexableProperties, indexablePropertiesWithTypes, sortableProperties, sortablePropertiesWithTypes } = getInsertMetadata(zbsearch);
    const batchOptions = {
        avlRebalanceThreshold: batch.length
    };
    const { docs: docsStore } = zbsearch.data;
    for (const doc of batch){
        if ((0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$components$2f$inference$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["inferSchemaFromDocument"])(zbsearch, doc)) {
            // New properties were registered: refresh the memoized metadata.
            ;
            ({ indexableProperties, indexablePropertiesWithTypes, sortableProperties, sortablePropertiesWithTypes } = getInsertMetadata(zbsearch));
        }
        const errorProperty = zbsearch.validateSchema(doc, zbsearch.schema);
        if (errorProperty) {
            throw (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$errors$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["createError"])("SCHEMA_VALIDATION_FAILURE", errorProperty);
        }
        const id = zbsearch.getDocumentIndexId(doc);
        if (typeof id !== "string") {
            throw (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$errors$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["createError"])("DOCUMENT_ID_MUST_BE_STRING", typeof id);
        }
        const internalId = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$components$2f$internal$2d$document$2d$id$2d$store$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["getInternalDocumentId"])(zbsearch.internalDocumentIDStore, id);
        if (!skipHooks && zbsearch.beforeInsert?.length) {
            (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$components$2f$hooks$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["runSingleHook"])(zbsearch.beforeInsert, zbsearch, id, doc);
        }
        if (!zbsearch.documentsStore.store(docsStore, id, internalId, doc)) {
            throw (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$errors$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["createError"])("DOCUMENT_ALREADY_EXISTS", id);
        }
        const docsCount = zbsearch.documentsStore.count(docsStore);
        const indexableValues = zbsearch.getDocumentProperties(doc, indexableProperties);
        indexAndSortDocumentSync(zbsearch, id, indexableProperties, indexableValues, docsCount, language, doc, batchOptions, indexablePropertiesWithTypes, sortableProperties, sortablePropertiesWithTypes);
        if (!skipHooks && zbsearch.afterInsert?.length) {
            (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$components$2f$hooks$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["runSingleHook"])(zbsearch.afterInsert, zbsearch, id, doc);
        }
        ids.push(id);
    }
}
function innerInsertMultipleSync(zbsearch, docs, batchSize = 1e3, language, skipHooks, timeout = 0) {
    const ids = [];
    let i = 0;
    function processNextBatch() {
        const batch = docs.slice(i * batchSize, (i + 1) * batchSize);
        if (batch.length === 0) return false;
        insertBatchSync(zbsearch, batch, ids, language, skipHooks);
        i++;
        return true;
    }
    function processAllBatches() {
        // eslint-disable-next-line no-constant-condition
        while(true){
            const startTime = Date.now();
            const hasMoreBatches = processNextBatch();
            if (!hasMoreBatches) break;
            if (timeout > 0) {
                const elapsedTime = Date.now() - startTime;
                const waitTime = timeout - elapsedTime;
                if (waitTime > 0) {
                    (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$utils$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["sleep"])(waitTime);
                }
            }
        }
    }
    processAllBatches();
    if (!skipHooks && zbsearch.afterInsertMultiple?.length) {
        (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$components$2f$hooks$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["runMultipleHook"])(zbsearch.afterInsertMultiple, zbsearch, docs);
    }
    return ids;
}
function innerInsertMultiple(zbsearch, docs, batchSize, language, skipHooks, timeout) {
    const asyncNeeded = needsAsyncInsert(zbsearch);
    if (asyncNeeded) {
        return innerInsertMultipleAsync(zbsearch, docs, batchSize, language, skipHooks, timeout);
    }
    return innerInsertMultipleSync(zbsearch, docs, batchSize, language, skipHooks, timeout);
}
function needsAsyncInsert(zbsearch) {
    return (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$utils$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["isAsyncFunction"])(zbsearch.beforeInsert) || (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$utils$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["isAsyncFunction"])(zbsearch.afterInsert) || (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$utils$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["isAsyncFunction"])(zbsearch.index.beforeInsert) || (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$utils$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["isAsyncFunction"])(zbsearch.index.insert) || (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$utils$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["isAsyncFunction"])(zbsearch.index.afterInsert);
}
async function insertMultipleAsync(zbsearch, docs, options = {}) {
    const { batchSize = 100, language, skipHooks, onProgress } = options;
    if (!Number.isInteger(batchSize) || batchSize < 1) {
        throw (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$errors$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["createError"])("INVALID_BATCH_SIZE", batchSize);
    }
    const asyncNeeded = needsAsyncInsert(zbsearch);
    const total = docs.length;
    const ids = [];
    for(let start = 0; start < total; start += batchSize){
        const batch = docs.slice(start, start + batchSize);
        if (asyncNeeded) {
            const insertOptions = {
                avlRebalanceThreshold: batch.length
            };
            for (const doc of batch){
                ids.push(await insert(zbsearch, doc, language, skipHooks, insertOptions));
            }
        } else {
            insertBatchSync(zbsearch, batch, ids, language, skipHooks);
        }
        onProgress?.({
            processed: ids.length,
            total
        });
        if (start + batchSize < total) {
            await (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$utils$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["yieldToEventLoop"])();
        }
    }
    if (!skipHooks && zbsearch.afterInsertMultiple?.length) {
        await (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$components$2f$hooks$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["runMultipleHook"])(zbsearch.afterInsertMultiple, zbsearch, docs);
    }
    return ids;
}
}),
"[project]/node_modules/zbsearch/dist/browser/methods/remove.js [app-client] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "remove",
    ()=>remove,
    "removeMultiple",
    ()=>removeMultiple
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$components$2f$hooks$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/zbsearch/dist/browser/components/hooks.js [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$components$2f$internal$2d$document$2d$id$2d$store$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/zbsearch/dist/browser/components/internal-document-id-store.js [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$utils$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/zbsearch/dist/browser/utils.js [app-client] (ecmascript)");
;
;
;
function remove(zbsearch, id, language, skipHooks) {
    const asyncNeeded = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$utils$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["isAsyncFunction"])(zbsearch.index.beforeRemove) || (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$utils$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["isAsyncFunction"])(zbsearch.index.remove) || (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$utils$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["isAsyncFunction"])(zbsearch.index.afterRemove);
    if (asyncNeeded) {
        return removeAsync(zbsearch, id, language, skipHooks);
    }
    return removeSync(zbsearch, id, language, skipHooks);
}
async function removeAsync(zbsearch, id, language, skipHooks) {
    let result = true;
    const { index, docs } = zbsearch.data;
    const doc = zbsearch.documentsStore.get(docs, id);
    if (!doc) {
        return false;
    }
    const internalId = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$components$2f$internal$2d$document$2d$id$2d$store$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["getInternalDocumentId"])(zbsearch.internalDocumentIDStore, id);
    const docId = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$components$2f$internal$2d$document$2d$id$2d$store$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["getDocumentIdFromInternalId"])(zbsearch.internalDocumentIDStore, internalId);
    const docsCount = zbsearch.documentsStore.count(docs);
    if (!skipHooks) {
        await (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$components$2f$hooks$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["runSingleHook"])(zbsearch.beforeRemove, zbsearch, docId);
    }
    const indexableProperties = zbsearch.index.getSearchableProperties(index);
    const indexablePropertiesWithTypes = zbsearch.index.getSearchablePropertiesWithTypes(index);
    const values = zbsearch.getDocumentProperties(doc, indexableProperties);
    for (const prop of indexableProperties){
        const value = values[prop];
        if (typeof value === "undefined") {
            continue;
        }
        const schemaType = indexablePropertiesWithTypes[prop];
        await zbsearch.index.beforeRemove?.(zbsearch.data.index, prop, docId, value, schemaType, language, zbsearch.tokenizer, docsCount);
        if (!await zbsearch.index.remove(zbsearch.index, zbsearch.data.index, prop, id, internalId, value, schemaType, language, zbsearch.tokenizer, docsCount)) {
            result = false;
        }
        await zbsearch.index.afterRemove?.(zbsearch.data.index, prop, docId, value, schemaType, language, zbsearch.tokenizer, docsCount);
    }
    const sortableProperties = await zbsearch.sorter.getSortableProperties(zbsearch.data.sorting);
    const sortableValues = await zbsearch.getDocumentProperties(doc, sortableProperties);
    for (const prop of sortableProperties){
        if (typeof sortableValues[prop] === "undefined") {
            continue;
        }
        zbsearch.sorter.remove(zbsearch.data.sorting, prop, id);
    }
    if (!skipHooks) {
        await (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$components$2f$hooks$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["runSingleHook"])(zbsearch.afterRemove, zbsearch, docId);
    }
    zbsearch.documentsStore.remove(zbsearch.data.docs, id, internalId);
    return result;
}
function removeSync(zbsearch, id, language, skipHooks) {
    let result = true;
    const { index, docs } = zbsearch.data;
    const doc = zbsearch.documentsStore.get(docs, id);
    if (!doc) {
        return false;
    }
    const internalId = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$components$2f$internal$2d$document$2d$id$2d$store$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["getInternalDocumentId"])(zbsearch.internalDocumentIDStore, id);
    const docId = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$components$2f$internal$2d$document$2d$id$2d$store$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["getDocumentIdFromInternalId"])(zbsearch.internalDocumentIDStore, internalId);
    const docsCount = zbsearch.documentsStore.count(docs);
    if (!skipHooks) {
        (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$components$2f$hooks$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["runSingleHook"])(zbsearch.beforeRemove, zbsearch, docId);
    }
    const indexableProperties = zbsearch.index.getSearchableProperties(index);
    const indexablePropertiesWithTypes = zbsearch.index.getSearchablePropertiesWithTypes(index);
    const values = zbsearch.getDocumentProperties(doc, indexableProperties);
    for (const prop of indexableProperties){
        const value = values[prop];
        if (typeof value === "undefined") {
            continue;
        }
        const schemaType = indexablePropertiesWithTypes[prop];
        zbsearch.index.beforeRemove?.(zbsearch.data.index, prop, docId, value, schemaType, language, zbsearch.tokenizer, docsCount);
        if (!zbsearch.index.remove(zbsearch.index, zbsearch.data.index, prop, id, internalId, value, schemaType, language, zbsearch.tokenizer, docsCount)) {
            result = false;
        }
        zbsearch.index.afterRemove?.(zbsearch.data.index, prop, docId, value, schemaType, language, zbsearch.tokenizer, docsCount);
    }
    const sortableProperties = zbsearch.sorter.getSortableProperties(zbsearch.data.sorting);
    const sortableValues = zbsearch.getDocumentProperties(doc, sortableProperties);
    for (const prop of sortableProperties){
        if (typeof sortableValues[prop] === "undefined") {
            continue;
        }
        zbsearch.sorter.remove(zbsearch.data.sorting, prop, id);
    }
    if (!skipHooks) {
        (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$components$2f$hooks$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["runSingleHook"])(zbsearch.afterRemove, zbsearch, docId);
    }
    zbsearch.documentsStore.remove(zbsearch.data.docs, id, internalId);
    return result;
}
function removeMultiple(zbsearch, ids, batchSize, language, skipHooks) {
    const asyncNeeded = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$utils$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["isAsyncFunction"])(zbsearch.index.beforeRemove) || (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$utils$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["isAsyncFunction"])(zbsearch.index.remove) || (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$utils$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["isAsyncFunction"])(zbsearch.index.afterRemove) || (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$utils$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["isAsyncFunction"])(zbsearch.beforeRemoveMultiple) || (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$utils$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["isAsyncFunction"])(zbsearch.afterRemoveMultiple);
    if (asyncNeeded) {
        return removeMultipleAsync(zbsearch, ids, batchSize, language, skipHooks);
    }
    return removeMultipleSync(zbsearch, ids, batchSize, language, skipHooks);
}
async function removeMultipleAsync(zbsearch, ids, batchSize, language, skipHooks) {
    let result = 0;
    if (!batchSize) {
        batchSize = 1e3;
    }
    const docIdsForHooks = skipHooks ? [] : ids.map((id)=>(0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$components$2f$internal$2d$document$2d$id$2d$store$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["getDocumentIdFromInternalId"])(zbsearch.internalDocumentIDStore, (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$components$2f$internal$2d$document$2d$id$2d$store$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["getInternalDocumentId"])(zbsearch.internalDocumentIDStore, id)));
    if (!skipHooks) {
        await (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$components$2f$hooks$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["runMultipleHook"])(zbsearch.beforeRemoveMultiple, zbsearch, docIdsForHooks);
    }
    await new Promise((resolve, reject)=>{
        let i = 0;
        async function _removeMultiple() {
            const batch = ids.slice(i * batchSize, ++i * batchSize);
            if (!batch.length) {
                return resolve();
            }
            for (const doc of batch){
                try {
                    if (await remove(zbsearch, doc, language, skipHooks)) {
                        result++;
                    }
                } catch (err) {
                    reject(err);
                }
            }
            setTimeout(_removeMultiple, 0);
        }
        setTimeout(_removeMultiple, 0);
    });
    if (!skipHooks) {
        await (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$components$2f$hooks$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["runMultipleHook"])(zbsearch.afterRemoveMultiple, zbsearch, docIdsForHooks);
    }
    return result;
}
function removeMultipleSync(zbsearch, ids, batchSize, language, skipHooks) {
    let result = 0;
    if (!batchSize) {
        batchSize = 1e3;
    }
    const docIdsForHooks = skipHooks ? [] : ids.map((id)=>(0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$components$2f$internal$2d$document$2d$id$2d$store$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["getDocumentIdFromInternalId"])(zbsearch.internalDocumentIDStore, (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$components$2f$internal$2d$document$2d$id$2d$store$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["getInternalDocumentId"])(zbsearch.internalDocumentIDStore, id)));
    if (!skipHooks) {
        (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$components$2f$hooks$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["runMultipleHook"])(zbsearch.beforeRemoveMultiple, zbsearch, docIdsForHooks);
    }
    for(let i = 0; i < ids.length; i += batchSize){
        const batch = ids.slice(i, i + batchSize);
        for (const doc of batch){
            if (remove(zbsearch, doc, language, skipHooks)) {
                result++;
            }
        }
    }
    if (!skipHooks) {
        (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$components$2f$hooks$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["runMultipleHook"])(zbsearch.afterRemoveMultiple, zbsearch, docIdsForHooks);
    }
    return result;
}
}),
"[project]/node_modules/zbsearch/dist/browser/methods/search-fulltext.js [app-client] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "applyDefault",
    ()=>applyDefault,
    "defaultBM25Params",
    ()=>defaultBM25Params,
    "fullTextSearch",
    ()=>fullTextSearch,
    "getPropertiesToSearch",
    ()=>getPropertiesToSearch,
    "innerFullTextSearch",
    ()=>innerFullTextSearch
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$components$2f$facets$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/zbsearch/dist/browser/components/facets.js [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$components$2f$groups$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/zbsearch/dist/browser/components/groups.js [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$components$2f$hooks$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/zbsearch/dist/browser/components/hooks.js [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$components$2f$internal$2d$document$2d$id$2d$store$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/zbsearch/dist/browser/components/internal-document-id-store.js [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$components$2f$index$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/zbsearch/dist/browser/components/index.js [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$components$2f$pinning$2d$manager$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/zbsearch/dist/browser/components/pinning-manager.js [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$errors$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/zbsearch/dist/browser/errors.js [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$utils$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/zbsearch/dist/browser/utils.js [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$methods$2f$docs$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/zbsearch/dist/browser/methods/docs.js [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$methods$2f$fetch$2d$documents$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/zbsearch/dist/browser/methods/fetch-documents.js [app-client] (ecmascript)");
;
;
;
;
;
;
;
;
;
;
function getPropertiesToSearch(zbsearch, properties) {
    const index = zbsearch.data.index;
    let propertiesToSearch = zbsearch.caches["propertiesToSearch"];
    if (!propertiesToSearch) {
        const propertiesToSearchWithTypes = zbsearch.index.getSearchablePropertiesWithTypes(index);
        propertiesToSearch = zbsearch.index.getSearchableProperties(index);
        propertiesToSearch = propertiesToSearch.filter((prop)=>propertiesToSearchWithTypes[prop].startsWith("string"));
        zbsearch.caches["propertiesToSearch"] = propertiesToSearch;
    }
    if (properties && properties !== "*") {
        for (const prop of properties){
            if (!propertiesToSearch.includes(prop)) {
                throw (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$errors$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["createError"])("UNKNOWN_INDEX", prop, propertiesToSearch.join(", "));
            }
        }
        propertiesToSearch = propertiesToSearch.filter((prop)=>properties.includes(prop));
    }
    return propertiesToSearch;
}
function innerFullTextSearch(zbsearch, params, language, precomputedWhereFiltersIDs) {
    const { term, properties } = params;
    const index = zbsearch.data.index;
    const propertiesToSearch = getPropertiesToSearch(zbsearch, properties);
    // If filters are enabled, we need to get the IDs of the documents that match the filters.
    const hasFilters = Object.keys(params.where ?? {}).length > 0;
    let whereFiltersIDs = precomputedWhereFiltersIDs;
    if (hasFilters && whereFiltersIDs === undefined) {
        whereFiltersIDs = zbsearch.index.searchByWhereClause(index, zbsearch.tokenizer, params.where, language);
    }
    let uniqueDocsIDs;
    // We need to perform the search if:
    // - we have a search term
    // - or we have properties to search
    //   in this case, we need to return all the documents that contains at least one of the given properties
    const threshold = params.threshold !== undefined && params.threshold !== null ? params.threshold : 1;
    if (term || properties) {
        const docsCount = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$methods$2f$docs$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["count"])(zbsearch);
        uniqueDocsIDs = zbsearch.index.search(index, term || "", zbsearch.tokenizer, language, propertiesToSearch, params.exact || false, params.tolerance || 0, params.boost || {}, applyDefault(params.relevance), docsCount, whereFiltersIDs, threshold, params.prefix);
        // When exact is true and we have a term, filter results to only include documents
        // where the original text contains the exact search term (case-sensitive).
        // This is a highly requested feature and although ZBSearch is not case-sensitive by design,
        // this is a reasonable compromise.
        if (params.exact && term) {
            const searchTerms = term.trim().split(/\s+/);
            const exactRegexes = searchTerms.map((searchTerm)=>new RegExp(`\\b${escapeRegex(searchTerm)}\\b`));
            uniqueDocsIDs = uniqueDocsIDs.filter(([docId])=>{
                const doc = zbsearch.documentsStore.get(zbsearch.data.docs, docId);
                if (!doc) return false;
                for (const prop of propertiesToSearch){
                    const propValue = getPropValue(doc, prop);
                    if (typeof propValue === "string") {
                        const hasAllTerms = exactRegexes.every((regex)=>regex.test(propValue));
                        if (hasAllTerms) {
                            return true;
                        }
                    }
                }
                return false;
            });
        }
    } else {
        // Check if this is a geosearch-only query first
        if (hasFilters) {
            const geoResults = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$components$2f$index$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["searchByGeoWhereClause"])(index, params.where);
            if (geoResults) {
                // This is a geosearch-only query with distance scoring
                uniqueDocsIDs = geoResults;
            } else {
                // Regular filter query without search term
                const docIds = whereFiltersIDs ? Array.from(whereFiltersIDs) : [];
                uniqueDocsIDs = docIds.map((k)=>[
                        +k,
                        0
                    ]);
            }
        } else {
            // No search term and no filters - return all documents
            const docIds = Object.keys(zbsearch.documentsStore.getAll(zbsearch.data.docs));
            uniqueDocsIDs = docIds.map((k)=>[
                    +k,
                    0
                ]);
        }
    }
    return uniqueDocsIDs;
}
// Helper function to escape regex special characters
function escapeRegex(str) {
    return str.replace(/[.*+?^${}()|[\]\\]/g, "\\$&");
}
// Helper function to get nested property value
function getPropValue(obj, path) {
    const keys = path.split(".");
    let value = obj;
    for (const key of keys){
        if (value && typeof value === "object" && key in value) {
            value = value[key];
        } else {
            return undefined;
        }
    }
    return value;
}
function fullTextSearch(zbsearch, params, language) {
    const timeStart = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$utils$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["getNanosecondsTime"])();
    function performSearchLogic() {
        const vectorProperties = Object.keys(zbsearch.data.index.vectorIndexes);
        const shouldCalculateFacets = params.facets && Object.keys(params.facets).length > 0;
        const { limit = 10, offset = 0, distinctOn, includeVectors = false } = params;
        const isPreflight = params.preflight === true;
        let uniqueDocsArray = innerFullTextSearch(zbsearch, params, language);
        if (params.sortBy) {
            if (typeof params.sortBy === "function") {
                const ids = uniqueDocsArray.map(([id])=>id);
                const docs = zbsearch.documentsStore.getMultiple(zbsearch.data.docs, ids);
                const docsWithIdAndScore = docs.map((d, i)=>[
                        uniqueDocsArray[i][0],
                        uniqueDocsArray[i][1],
                        d
                    ]);
                docsWithIdAndScore.sort(params.sortBy);
                uniqueDocsArray = docsWithIdAndScore.map(([id, score])=>[
                        id,
                        score
                    ]);
            } else {
                uniqueDocsArray = zbsearch.sorter.sortBy(zbsearch.data.sorting, uniqueDocsArray, params.sortBy).map(([id, score])=>[
                        (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$components$2f$internal$2d$document$2d$id$2d$store$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["getInternalDocumentId"])(zbsearch.internalDocumentIDStore, id),
                        score
                    ]);
            }
        } else {
            uniqueDocsArray = uniqueDocsArray.sort(__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$utils$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["sortTokenScorePredicate"]);
        }
        // Apply pinning rules after sorting but before pagination
        uniqueDocsArray = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$components$2f$pinning$2d$manager$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["applyPinningRules"])(zbsearch, zbsearch.data.pinning, uniqueDocsArray, params.term);
        let results;
        if (!isPreflight) {
            results = distinctOn ? (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$methods$2f$fetch$2d$documents$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["fetchDocumentsWithDistinct"])(zbsearch, uniqueDocsArray, offset, limit, distinctOn) : (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$methods$2f$fetch$2d$documents$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["fetchDocuments"])(zbsearch, uniqueDocsArray, offset, limit);
        }
        const searchResult = {
            elapsed: {
                formatted: "",
                raw: 0
            },
            hits: [],
            count: uniqueDocsArray.length
        };
        if (typeof results !== "undefined") {
            searchResult.hits = results.filter(Boolean);
            if (!includeVectors) {
                (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$utils$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["removeVectorsFromHits"])(searchResult, vectorProperties);
            }
        }
        if (shouldCalculateFacets) {
            const facets = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$components$2f$facets$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["getFacets"])(zbsearch, uniqueDocsArray, params.facets);
            searchResult.facets = facets;
        }
        if (params.groupBy) {
            searchResult.groups = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$components$2f$groups$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["getGroups"])(zbsearch, uniqueDocsArray, params.groupBy);
        }
        searchResult.elapsed = zbsearch.formatElapsedTime((0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$utils$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["getNanosecondsTime"])() - timeStart);
        return searchResult;
    }
    async function executeSearchAsync() {
        if (zbsearch.beforeSearch) {
            await (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$components$2f$hooks$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["runBeforeSearch"])(zbsearch.beforeSearch, zbsearch, params, language);
        }
        const searchResult = performSearchLogic();
        if (zbsearch.afterSearch) {
            await (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$components$2f$hooks$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["runAfterSearch"])(zbsearch.afterSearch, zbsearch, params, language, searchResult);
        }
        return searchResult;
    }
    const asyncNeeded = zbsearch.beforeSearch?.length || zbsearch.afterSearch?.length;
    if (asyncNeeded) {
        return executeSearchAsync();
    }
    return performSearchLogic();
}
const defaultBM25Params = {
    k: 1.2,
    b: .75,
    d: .5
};
function applyDefault(bm25Relevance) {
    const r = bm25Relevance ?? {};
    r.k = r.k ?? defaultBM25Params.k;
    r.b = r.b ?? defaultBM25Params.b;
    r.d = r.d ?? defaultBM25Params.d;
    return r;
}
}),
"[project]/node_modules/zbsearch/dist/browser/methods/search-hybrid.js [app-client] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "hybridSearch",
    ()=>hybridSearch,
    "innerHybridSearch",
    ()=>innerHybridSearch
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$utils$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/zbsearch/dist/browser/utils.js [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$components$2f$facets$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/zbsearch/dist/browser/components/facets.js [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$components$2f$groups$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/zbsearch/dist/browser/components/groups.js [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$methods$2f$fetch$2d$documents$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/zbsearch/dist/browser/methods/fetch-documents.js [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$methods$2f$search$2d$fulltext$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/zbsearch/dist/browser/methods/search-fulltext.js [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$methods$2f$search$2d$vector$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/zbsearch/dist/browser/methods/search-vector.js [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$components$2f$hooks$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/zbsearch/dist/browser/components/hooks.js [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$components$2f$pinning$2d$manager$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/zbsearch/dist/browser/components/pinning-manager.js [app-client] (ecmascript)");
;
;
;
;
;
;
;
;
function innerHybridSearch(zbsearch, params, language) {
    const hasFilters = Object.keys(params.where ?? {}).length > 0;
    let whereFiltersIDs;
    if (hasFilters) {
        whereFiltersIDs = zbsearch.index.searchByWhereClause(zbsearch.data.index, zbsearch.tokenizer, params.where, language);
    }
    const hasFullTextInput = Boolean(params.term) || Boolean(params.properties);
    const fullTextIDs = hasFullTextInput ? (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$methods$2f$search$2d$fulltext$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["innerFullTextSearch"])(zbsearch, params, language, whereFiltersIDs) : [];
    const vectorIDs = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$methods$2f$search$2d$vector$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["innerVectorSearch"])(zbsearch, params, language, whereFiltersIDs);
    const hybridWeights = params.hybridWeights;
    return mergeAndRankResults(fullTextIDs, vectorIDs, params.term ?? "", hybridWeights);
}
function hybridSearch(zbsearch, params, language) {
    const timeStart = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$utils$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["getNanosecondsTime"])();
    function performSearchLogic() {
        let uniqueTokenScores = innerHybridSearch(zbsearch, params, language);
        // Apply pinning rules after merging results but before pagination
        uniqueTokenScores = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$components$2f$pinning$2d$manager$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["applyPinningRules"])(zbsearch, zbsearch.data.pinning, uniqueTokenScores, params.term);
        let facetsResults;
        const shouldCalculateFacets = params.facets && Object.keys(params.facets).length > 0;
        if (shouldCalculateFacets) {
            facetsResults = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$components$2f$facets$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["getFacets"])(zbsearch, uniqueTokenScores, params.facets);
        }
        let groups;
        if (params.groupBy) {
            groups = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$components$2f$groups$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["getGroups"])(zbsearch, uniqueTokenScores, params.groupBy);
        }
        const offset = params.offset ?? 0;
        const limit = params.limit ?? 10;
        const results = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$methods$2f$fetch$2d$documents$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["fetchDocuments"])(zbsearch, uniqueTokenScores, offset, limit).filter(Boolean);
        const timeEnd = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$utils$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["getNanosecondsTime"])();
        const returningResults = {
            count: uniqueTokenScores.length,
            elapsed: {
                raw: Number(timeEnd - timeStart),
                formatted: (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$utils$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["formatNanoseconds"])(timeEnd - timeStart)
            },
            hits: results,
            ...facetsResults ? {
                facets: facetsResults
            } : {},
            ...groups ? {
                groups
            } : {}
        };
        const includeVectors = params.includeVectors ?? false;
        if (!includeVectors) {
            const vectorProperties = Object.keys(zbsearch.data.index.vectorIndexes);
            (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$utils$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["removeVectorsFromHits"])(returningResults, vectorProperties);
        }
        return returningResults;
    }
    async function executeSearchAsync() {
        if (zbsearch.beforeSearch) {
            await (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$components$2f$hooks$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["runBeforeSearch"])(zbsearch.beforeSearch, zbsearch, params, language);
        }
        const results = performSearchLogic();
        if (zbsearch.afterSearch) {
            await (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$components$2f$hooks$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["runAfterSearch"])(zbsearch.afterSearch, zbsearch, params, language, results);
        }
        return results;
    }
    const asyncNeeded = zbsearch.beforeSearch?.length || zbsearch.afterSearch?.length;
    if (asyncNeeded) {
        return executeSearchAsync();
    }
    return performSearchLogic();
}
function getMaxScore(results) {
    let max = 0;
    const resultsLength = results.length;
    for(let i = 0; i < resultsLength; i++){
        const score = results[i][1];
        if (score > max) {
            max = score;
        }
    }
    return max;
}
function normalizeScore(score, maxScore) {
    return maxScore > 0 ? score / maxScore : 0;
}
function hybridScoreBuilder(textWeight, vectorWeight) {
    return (textScore, vectorScore)=>textScore * textWeight + vectorScore * vectorWeight;
}
function mergeAndRankResults(textResults, vectorResults, query, hybridWeights) {
    // Avoid Math.max(...results): spreading large result sets overflows the call stack.
    const maxTextScore = getMaxScore(textResults);
    const maxVectorScore = getMaxScore(vectorResults);
    const hasHybridWeights = hybridWeights && typeof hybridWeights.text === "number" && typeof hybridWeights.vector === "number";
    const { text: textWeight, vector: vectorWeight } = hasHybridWeights ? hybridWeights : getQueryWeights(query);
    const mergedResults = new Map();
    const textResultsLength = textResults.length;
    const hybridScore = hybridScoreBuilder(textWeight, vectorWeight);
    for(let i = 0; i < textResultsLength; i++){
        const [id, score] = textResults[i];
        const normalizedScore = normalizeScore(score, maxTextScore);
        const hybridScoreValue = hybridScore(normalizedScore, 0);
        mergedResults.set(id, hybridScoreValue);
    }
    const vectorResultsLength = vectorResults.length;
    for(let i = 0; i < vectorResultsLength; i++){
        const [resultId, score] = vectorResults[i];
        const normalizedScore = normalizeScore(score, maxVectorScore);
        const existingRes = mergedResults.get(resultId) ?? 0;
        mergedResults.set(resultId, existingRes + hybridScore(0, normalizedScore));
    }
    return [
        ...mergedResults
    ].sort(__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$utils$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["sortTokenScorePredicate"]);
}
// eslint-disable-next-line @typescript-eslint/no-unused-vars
function getQueryWeights(query) {
    // In the next versions of ZBSearch, we will ship a plugin containing a ML model to adjust the weights
    // based on whether the query is keyword-focused, conceptual, etc.
    // For now, we just return a fixed value.
    return {
        text: .5,
        vector: .5
    };
}
}),
"[project]/node_modules/zbsearch/dist/browser/methods/search-vector.js [app-client] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "innerVectorSearch",
    ()=>innerVectorSearch,
    "searchVector",
    ()=>searchVector
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$utils$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/zbsearch/dist/browser/utils.js [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$components$2f$facets$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/zbsearch/dist/browser/components/facets.js [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$errors$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/zbsearch/dist/browser/errors.js [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$components$2f$groups$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/zbsearch/dist/browser/components/groups.js [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$components$2f$hooks$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/zbsearch/dist/browser/components/hooks.js [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$trees$2f$vector$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__$3c$locals$3e$__ = __turbopack_context__.i("[project]/node_modules/zbsearch/dist/browser/trees/vector.js [app-client] (ecmascript) <locals>");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$components$2f$pinning$2d$manager$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/zbsearch/dist/browser/components/pinning-manager.js [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$methods$2f$fetch$2d$documents$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/zbsearch/dist/browser/methods/fetch-documents.js [app-client] (ecmascript)");
;
;
;
;
;
;
;
;
function innerVectorSearch(zbsearch, params, language, precomputedWhereFiltersIDs) {
    const vector = params.vector;
    if (vector && (!("value" in vector) || !("property" in vector))) {
        throw (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$errors$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["createError"])("INVALID_VECTOR_INPUT", Object.keys(vector).join(", "));
    }
    const vectorIndex = zbsearch.data.index.vectorIndexes[vector.property];
    if (!vectorIndex) {
        throw (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$errors$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["createError"])("UNKNOWN_VECTOR_PROPERTY", vector.property);
    }
    const vectorSize = vectorIndex.node.size;
    if (vector?.value.length !== vectorSize) {
        if (vector?.property === undefined || vector?.value.length === undefined) {
            throw (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$errors$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["createError"])("INVALID_INPUT_VECTOR", "undefined", vectorSize, "undefined");
        }
        throw (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$errors$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["createError"])("INVALID_INPUT_VECTOR", vector.property, vectorSize, vector.value.length);
    }
    const index = zbsearch.data.index;
    let whereFiltersIDs = precomputedWhereFiltersIDs;
    const hasFilters = Object.keys(params.where ?? {}).length > 0;
    if (hasFilters && whereFiltersIDs === undefined) {
        whereFiltersIDs = zbsearch.index.searchByWhereClause(index, zbsearch.tokenizer, params.where, language);
    }
    return vectorIndex.node.find(vector.value, params.similarity ?? __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$trees$2f$vector$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__$3c$locals$3e$__["DEFAULT_SIMILARITY"], whereFiltersIDs);
}
function searchVector(zbsearch, params, language = "english") {
    const timeStart = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$utils$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["getNanosecondsTime"])();
    function performSearchLogic() {
        let results = innerVectorSearch(zbsearch, params, language).sort(__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$utils$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["sortTokenScorePredicate"]);
        // Apply pinning rules after sorting but before pagination
        results = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$components$2f$pinning$2d$manager$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["applyPinningRules"])(zbsearch, zbsearch.data.pinning, results, undefined);
        let facetsResults = [];
        const shouldCalculateFacets = params.facets && Object.keys(params.facets).length > 0;
        if (shouldCalculateFacets) {
            const facets = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$components$2f$facets$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["getFacets"])(zbsearch, results, params.facets);
            facetsResults = facets;
        }
        const vectorProperty = params.vector.property;
        const includeVectors = params.includeVectors ?? false;
        const limit = params.limit ?? 10;
        const offset = params.offset ?? 0;
        const hits = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$methods$2f$fetch$2d$documents$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["fetchDocuments"])(zbsearch, results, offset, limit).filter(Boolean);
        let groups = [];
        if (params.groupBy) {
            groups = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$components$2f$groups$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["getGroups"])(zbsearch, results, params.groupBy);
        }
        const timeEnd = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$utils$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["getNanosecondsTime"])();
        const elapsedTime = timeEnd - timeStart;
        const searchResult = {
            count: results.length,
            hits,
            elapsed: {
                raw: Number(elapsedTime),
                formatted: (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$utils$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["formatNanoseconds"])(elapsedTime)
            },
            ...facetsResults ? {
                facets: facetsResults
            } : {},
            ...groups ? {
                groups
            } : {}
        };
        if (!includeVectors) {
            (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$utils$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["removeVectorsFromHits"])(searchResult, [
                vectorProperty
            ]);
        }
        return searchResult;
    }
    async function executeSearchAsync() {
        if (zbsearch.beforeSearch) {
            await (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$components$2f$hooks$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["runBeforeSearch"])(zbsearch.beforeSearch, zbsearch, params, language);
        }
        const results = performSearchLogic();
        if (zbsearch.afterSearch) {
            await (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$components$2f$hooks$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["runAfterSearch"])(zbsearch.afterSearch, zbsearch, params, language, results);
        }
        return results;
    }
    const asyncNeeded = zbsearch.beforeSearch?.length || zbsearch.afterSearch?.length;
    if (asyncNeeded) {
        return executeSearchAsync();
    }
    // Sync path
    return performSearchLogic();
}
}),
"[project]/node_modules/zbsearch/dist/browser/methods/search.js [app-client] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "search",
    ()=>search
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$errors$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/zbsearch/dist/browser/errors.js [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$constants$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/zbsearch/dist/browser/constants.js [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$methods$2f$search$2d$fulltext$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/zbsearch/dist/browser/methods/search-fulltext.js [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$methods$2f$search$2d$vector$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/zbsearch/dist/browser/methods/search-vector.js [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$methods$2f$search$2d$hybrid$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/zbsearch/dist/browser/methods/search-hybrid.js [app-client] (ecmascript)");
;
;
;
;
;
function search(zbsearch, params, language) {
    const mode = params.mode ?? __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$constants$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["MODE_FULLTEXT_SEARCH"];
    if (mode === __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$constants$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["MODE_FULLTEXT_SEARCH"]) {
        return (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$methods$2f$search$2d$fulltext$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["fullTextSearch"])(zbsearch, params, language);
    }
    if (mode === __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$constants$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["MODE_VECTOR_SEARCH"]) {
        return (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$methods$2f$search$2d$vector$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["searchVector"])(zbsearch, params);
    }
    if (mode === __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$constants$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["MODE_HYBRID_SEARCH"]) {
        return (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$methods$2f$search$2d$hybrid$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["hybridSearch"])(zbsearch, params);
    }
    throw (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$errors$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["createError"])("INVALID_SEARCH_MODE", mode);
}
}),
"[project]/node_modules/zbsearch/dist/browser/methods/serialization.js [app-client] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "CHUNKED_FORMAT_VERSION",
    ()=>CHUNKED_FORMAT_VERSION,
    "DEFAULT_CHUNK_SIZE",
    ()=>DEFAULT_CHUNK_SIZE,
    "fromChunkedRawData",
    ()=>fromChunkedRawData,
    "fromChunkedRawDataAsync",
    ()=>fromChunkedRawDataAsync,
    "isChunkedRawData",
    ()=>isChunkedRawData,
    "load",
    ()=>load,
    "loadAsync",
    ()=>loadAsync,
    "parseChunked",
    ()=>parseChunked,
    "save",
    ()=>save,
    "stringifyChunked",
    ()=>stringifyChunked,
    "toChunkedRawData",
    ()=>toChunkedRawData
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$utils$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/zbsearch/dist/browser/utils.js [app-client] (ecmascript)");
;
const CHUNKED_FORMAT_VERSION = 2;
const DEFAULT_CHUNK_SIZE = 512 * 1024;
const MAX_KEYS_TO_DESCEND = 32;
function isChunkedRawData(data) {
    return typeof data === "object" && data !== null && typeof data.version === "number" && Array.isArray(data.chunks);
}
function isPlainRecord(value) {
    return typeof value === "object" && value !== null && !Array.isArray(value);
}
function splitArray(value, chunkSize, chunks) {
    let start = 0;
    let emitted = 0;
    while(start < value.length){
        let end = start;
        let length = 2;
        while(end < value.length){
            const piece = JSON.stringify(value[end]) ?? "null";
            if (length + piece.length + 1 > chunkSize && end > start) break;
            length += piece.length + 1;
            end++;
        }
        chunks.push(JSON.stringify(value.slice(start, end)));
        emitted++;
        start = end;
    }
    return emitted;
}
function splitRecord(value, chunkSize, chunks) {
    const keys = Object.keys(value);
    let start = 0;
    let emitted = 0;
    while(start < keys.length){
        const slice = {};
        let length = 2;
        let end = start;
        while(end < keys.length){
            const key = keys[end];
            const piece = JSON.stringify(value[key]);
            if (piece === undefined) {
                end++;
                continue;
            }
            const cost = piece.length + key.length + 4;
            if (length + cost > chunkSize && end > start) break;
            slice[key] = value[key];
            length += cost;
            end++;
        }
        if (end === start) end++;
        chunks.push(JSON.stringify(slice));
        emitted++;
        start = end;
    }
    return emitted;
}
function splitValue(value, path, chunkSize, chunks, parts) {
    const serialized = JSON.stringify(value);
    if (serialized === undefined) return;
    if (serialized.length <= chunkSize) {
        parts.push({
            p: path,
            k: "value",
            n: 1
        });
        chunks.push(serialized);
        return;
    }
    if (Array.isArray(value)) {
        const n = splitArray(value, chunkSize, chunks);
        parts.push({
            p: path,
            k: "array",
            n
        });
        return;
    }
    if (isPlainRecord(value)) {
        const keys = Object.keys(value);
        if (keys.length <= MAX_KEYS_TO_DESCEND) {
            for (const key of keys){
                splitValue(value[key], [
                    ...path,
                    key
                ], chunkSize, chunks, parts);
            }
            return;
        }
        const n = splitRecord(value, chunkSize, chunks);
        parts.push({
            p: path,
            k: "record",
            n
        });
        return;
    }
    parts.push({
        p: path,
        k: "value",
        n: 1
    });
    chunks.push(serialized);
}
function toChunkedRawData(raw, chunkSize = DEFAULT_CHUNK_SIZE) {
    if (!Number.isInteger(chunkSize) || chunkSize < 1024) {
        throw new TypeError(`chunkSize must be an integer of at least 1024, got "${chunkSize}"`);
    }
    const chunks = [];
    const parts = [];
    splitValue(raw, [], chunkSize, chunks, parts);
    const header = {
        v: CHUNKED_FORMAT_VERSION,
        parts
    };
    chunks.unshift(JSON.stringify(header));
    return {
        version: CHUNKED_FORMAT_VERSION,
        chunks
    };
}
function stringifyChunked(data) {
    return data.chunks.join("\n");
}
function parseChunked(text) {
    const chunks = text.split("\n");
    const header = JSON.parse(chunks[0]);
    return {
        version: header.v,
        chunks
    };
}
function containerAt(root, path) {
    let node = root;
    const full = [
        "root",
        ...path
    ];
    for(let i = 0; i < full.length - 1; i++){
        const key = full[i];
        if (!isPlainRecord(node[key])) node[key] = {};
        node = node[key];
    }
    return {
        parent: node,
        key: full[full.length - 1]
    };
}
function beginPart(root, part) {
    const { parent, key } = containerAt(root, part.p);
    if (part.k === "value") return {
        parent,
        key,
        target: null
    };
    const target = part.k === "array" ? [] : {};
    parent[key] = target;
    return {
        parent,
        key,
        target
    };
}
function applyPiece(slot, part, piece) {
    if (part.k === "value") {
        slot.parent[slot.key] = piece;
        return;
    }
    if (part.k === "array") {
        const target = slot.target;
        for (const item of piece)target.push(item);
        return;
    }
    Object.assign(slot.target, piece);
}
function readHeader(data) {
    if (data.version !== CHUNKED_FORMAT_VERSION) {
        throw new TypeError(`unsupported chunked index version ${data.version}, this build understands ${CHUNKED_FORMAT_VERSION}`);
    }
    if (!data.chunks.length) {
        throw new TypeError("chunked index is empty");
    }
    return JSON.parse(data.chunks[0]);
}
function fromChunkedRawData(data) {
    const header = readHeader(data);
    const root = {};
    let cursor = 1;
    for (const part of header.parts){
        const slot = beginPart(root, part);
        for(let i = 0; i < part.n; i++){
            applyPiece(slot, part, JSON.parse(data.chunks[cursor++]));
        }
    }
    return root["root"];
}
async function fromChunkedRawDataAsync(data) {
    const header = readHeader(data);
    const root = {};
    let cursor = 1;
    for (const part of header.parts){
        const slot = beginPart(root, part);
        for(let i = 0; i < part.n; i++){
            applyPiece(slot, part, JSON.parse(data.chunks[cursor++]));
            if (cursor < data.chunks.length) await (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$utils$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["yieldToEventLoop"])();
        }
    }
    return root["root"];
}
function load(zbsearch, raw) {
    const data = isChunkedRawData(raw) ? fromChunkedRawData(raw) : raw;
    zbsearch.internalDocumentIDStore.load(zbsearch, data.internalDocumentIDStore);
    zbsearch.data.index = zbsearch.index.load(zbsearch.internalDocumentIDStore, data.index, zbsearch.indexes);
    zbsearch.data.docs = zbsearch.documentsStore.load(zbsearch.internalDocumentIDStore, data.docs);
    zbsearch.data.sorting = zbsearch.sorter.load(zbsearch.internalDocumentIDStore, data.sorting);
    zbsearch.data.pinning = zbsearch.pinning.load(zbsearch.internalDocumentIDStore, data.pinning);
    zbsearch.tokenizer.language = data.language;
}
async function loadAsync(zbsearch, raw) {
    const data = isChunkedRawData(raw) ? await fromChunkedRawDataAsync(raw) : raw;
    zbsearch.internalDocumentIDStore.load(zbsearch, data.internalDocumentIDStore);
    await (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$utils$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["yieldToEventLoop"])();
    zbsearch.data.index = zbsearch.index.loadAsync ? await zbsearch.index.loadAsync(zbsearch.internalDocumentIDStore, data.index, zbsearch.indexes) : zbsearch.index.load(zbsearch.internalDocumentIDStore, data.index, zbsearch.indexes);
    await (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$utils$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["yieldToEventLoop"])();
    zbsearch.data.docs = zbsearch.documentsStore.load(zbsearch.internalDocumentIDStore, data.docs);
    await (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$utils$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["yieldToEventLoop"])();
    zbsearch.data.sorting = zbsearch.sorter.loadAsync ? await zbsearch.sorter.loadAsync(zbsearch.internalDocumentIDStore, data.sorting) : zbsearch.sorter.load(zbsearch.internalDocumentIDStore, data.sorting);
    await (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$utils$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["yieldToEventLoop"])();
    zbsearch.data.pinning = zbsearch.pinning.load(zbsearch.internalDocumentIDStore, data.pinning);
    zbsearch.tokenizer.language = data.language;
}
function save(zbsearch, options) {
    const raw = {
        internalDocumentIDStore: zbsearch.internalDocumentIDStore.save(zbsearch.internalDocumentIDStore),
        index: zbsearch.index.save(zbsearch.data.index),
        docs: zbsearch.documentsStore.save(zbsearch.data.docs),
        sorting: zbsearch.sorter.save(zbsearch.data.sorting),
        pinning: zbsearch.pinning.save(zbsearch.data.pinning),
        language: zbsearch.tokenizer.language
    };
    if (options?.format !== "chunked") return raw;
    return toChunkedRawData(raw, options.chunkSize ?? DEFAULT_CHUNK_SIZE);
}
}),
"[project]/node_modules/zbsearch/dist/browser/methods/suggest.js [app-client] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "autoSuggest",
    ()=>autoSuggest,
    "suggest",
    ()=>suggest
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$components$2f$index$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/zbsearch/dist/browser/components/index.js [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$errors$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/zbsearch/dist/browser/errors.js [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$utils$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/zbsearch/dist/browser/utils.js [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$methods$2f$docs$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/zbsearch/dist/browser/methods/docs.js [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$methods$2f$search$2d$fulltext$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/zbsearch/dist/browser/methods/search-fulltext.js [app-client] (ecmascript)");
;
;
;
;
;
function suggest(zbsearch, params, language) {
    const timeStart = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$utils$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["getNanosecondsTime"])();
    const { term, limit = 10, offset = 0, threshold = 0, prefix = true, tolerance = 0 } = params;
    const searchSuggestions = zbsearch.index.searchSuggestions ?? (zbsearch.index.supportsSuggestions ? __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$components$2f$index$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["searchSuggestions"] : undefined);
    if (typeof searchSuggestions !== "function") {
        throw (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$errors$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["createError"])("SUGGEST_NOT_SUPPORTED");
    }
    const index = zbsearch.data.index;
    const propertiesToSearch = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$methods$2f$search$2d$fulltext$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["getPropertiesToSearch"])(zbsearch, params.properties);
    const tokens = zbsearch.tokenizer.tokenize(term ?? "", language);
    if (!tokens.length || !propertiesToSearch.length) {
        return emptyResults(zbsearch, timeStart);
    }
    const lastToken = tokens.length - 1;
    const queryTokens = tokens.map((token, i)=>({
            token,
            exact: prefix === false || prefix === "last" && i < lastToken,
            tolerance,
            completion: i === lastToken
        }));
    const hasFilters = Object.keys(params.where ?? {}).length > 0;
    const whereFiltersIDs = hasFilters ? zbsearch.index.searchByWhereClause(index, zbsearch.tokenizer, params.where, language) : undefined;
    if (hasFilters && !whereFiltersIDs.size) {
        return emptyResults(zbsearch, timeStart);
    }
    const matches = searchSuggestions(index, queryTokens, propertiesToSearch, params.boost ?? {}, (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$methods$2f$search$2d$fulltext$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["applyDefault"])(params.relevance), (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$methods$2f$docs$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["count"])(zbsearch), whereFiltersIDs);
    const suggestions = aggregateSuggestions(matches, tokens, threshold);
    return {
        elapsed: zbsearch.formatElapsedTime((0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$utils$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["getNanosecondsTime"])() - timeStart),
        count: suggestions.length,
        suggestions: suggestions.slice(offset, offset + limit)
    };
}
const autoSuggest = suggest;
function emptyResults(zbsearch, timeStart) {
    return {
        elapsed: zbsearch.formatElapsedTime((0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$utils$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["getNanosecondsTime"])() - timeStart),
        count: 0,
        suggestions: []
    };
}
function aggregateSuggestions(matches, tokens, threshold) {
    const suggestions = new Map();
    const partialMatches = [];
    for (const match of matches.values()){
        if (match.matchedTokens < tokens.length) {
            if (threshold > 0) {
                partialMatches.push(match);
            }
            continue;
        }
        addSuggestions(suggestions, match, tokens);
    }
    if (partialMatches.length > 0) {
        partialMatches.sort((a, b)=>b.score - a.score);
        const partialsToKeep = Math.ceil(partialMatches.length * Math.min(threshold, 1));
        for(let i = 0; i < partialsToKeep; i++){
            addSuggestions(suggestions, partialMatches[i], tokens);
        }
    }
    return Array.from(suggestions.values()).sort((a, b)=>b.score - a.score || b.count - a.count || (a.suggestion < b.suggestion ? -1 : 1));
}
function addSuggestions(suggestions, match, tokens) {
    const completionIndex = tokens.length - 1;
    const context = match.words.map((word, i)=>word ?? tokens[i]);
    let contextScore = 0;
    for(let i = 0; i < completionIndex; i++){
        contextScore += match.wordScores[i];
    }
    if (!match.completions?.size) {
        addSuggestion(suggestions, context, contextScore + match.wordScores[completionIndex]);
        return;
    }
    for (const [word, score] of match.completions){
        const terms = context.slice();
        terms[completionIndex] = word;
        addSuggestion(suggestions, terms, contextScore + score);
    }
}
function addSuggestion(suggestions, terms, score) {
    const phrase = terms.join(" ");
    const existing = suggestions.get(phrase);
    if (existing) {
        existing.score += score;
        existing.count++;
        return;
    }
    suggestions.set(phrase, {
        suggestion: phrase,
        terms,
        score,
        count: 1
    });
}
}),
"[project]/node_modules/zbsearch/dist/browser/methods/update.js [app-client] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "update",
    ()=>update,
    "updateMultiple",
    ()=>updateMultiple
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$components$2f$hooks$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/zbsearch/dist/browser/components/hooks.js [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$errors$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/zbsearch/dist/browser/errors.js [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$methods$2f$insert$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/zbsearch/dist/browser/methods/insert.js [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$methods$2f$remove$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/zbsearch/dist/browser/methods/remove.js [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$utils$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/zbsearch/dist/browser/utils.js [app-client] (ecmascript)");
;
;
;
;
;
function update(zbsearch, id, doc, language, skipHooks) {
    const asyncNeeded = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$utils$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["isAsyncFunction"])(zbsearch.afterInsert) || (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$utils$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["isAsyncFunction"])(zbsearch.beforeInsert) || (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$utils$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["isAsyncFunction"])(zbsearch.afterRemove) || (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$utils$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["isAsyncFunction"])(zbsearch.beforeRemove) || (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$utils$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["isAsyncFunction"])(zbsearch.beforeUpdate) || (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$utils$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["isAsyncFunction"])(zbsearch.afterUpdate);
    if (asyncNeeded) {
        return updateAsync(zbsearch, id, doc, language, skipHooks);
    }
    return updateSync(zbsearch, id, doc, language, skipHooks);
}
async function updateAsync(zbsearch, id, doc, language, skipHooks) {
    if (!skipHooks && zbsearch.beforeUpdate) {
        await (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$components$2f$hooks$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["runSingleHook"])(zbsearch.beforeUpdate, zbsearch, id);
    }
    await (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$methods$2f$remove$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["remove"])(zbsearch, id, language, skipHooks);
    const newId = await (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$methods$2f$insert$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["insert"])(zbsearch, doc, language, skipHooks);
    if (!skipHooks && zbsearch.afterUpdate) {
        await (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$components$2f$hooks$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["runSingleHook"])(zbsearch.afterUpdate, zbsearch, newId);
    }
    return newId;
}
function updateSync(zbsearch, id, doc, language, skipHooks) {
    if (!skipHooks && zbsearch.beforeUpdate) {
        (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$components$2f$hooks$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["runSingleHook"])(zbsearch.beforeUpdate, zbsearch, id);
    }
    (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$methods$2f$remove$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["remove"])(zbsearch, id, language, skipHooks);
    const newId = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$methods$2f$insert$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["insert"])(zbsearch, doc, language, skipHooks);
    if (!skipHooks && zbsearch.afterUpdate) {
        (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$components$2f$hooks$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["runSingleHook"])(zbsearch.afterUpdate, zbsearch, newId);
    }
    return newId;
}
function updateMultiple(zbsearch, ids, docs, batchSize, language, skipHooks) {
    const asyncNeeded = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$utils$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["isAsyncFunction"])(zbsearch.afterInsert) || (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$utils$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["isAsyncFunction"])(zbsearch.beforeInsert) || (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$utils$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["isAsyncFunction"])(zbsearch.afterRemove) || (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$utils$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["isAsyncFunction"])(zbsearch.beforeRemove) || (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$utils$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["isAsyncFunction"])(zbsearch.beforeUpdate) || (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$utils$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["isAsyncFunction"])(zbsearch.afterUpdate) || (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$utils$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["isAsyncFunction"])(zbsearch.beforeUpdateMultiple) || (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$utils$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["isAsyncFunction"])(zbsearch.afterUpdateMultiple) || (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$utils$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["isAsyncFunction"])(zbsearch.beforeRemoveMultiple) || (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$utils$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["isAsyncFunction"])(zbsearch.afterRemoveMultiple) || (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$utils$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["isAsyncFunction"])(zbsearch.beforeInsertMultiple) || (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$utils$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["isAsyncFunction"])(zbsearch.afterInsertMultiple);
    if (asyncNeeded) {
        return updateMultipleAsync(zbsearch, ids, docs, batchSize, language, skipHooks);
    }
    return updateMultipleSync(zbsearch, ids, docs, batchSize, language, skipHooks);
}
async function updateMultipleAsync(zbsearch, ids, docs, batchSize, language, skipHooks) {
    if (!skipHooks) {
        await (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$components$2f$hooks$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["runMultipleHook"])(zbsearch.beforeUpdateMultiple, zbsearch, ids);
    }
    const docsLength = docs.length;
    for(let i = 0; i < docsLength; i++){
        const errorProperty = zbsearch.validateSchema(docs[i], zbsearch.schema);
        if (errorProperty) {
            throw (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$errors$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["createError"])("SCHEMA_VALIDATION_FAILURE", errorProperty);
        }
    }
    await (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$methods$2f$remove$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["removeMultiple"])(zbsearch, ids, batchSize, language, skipHooks);
    const newIds = await (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$methods$2f$insert$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["innerInsertMultiple"])(zbsearch, docs, batchSize, language, skipHooks);
    if (!skipHooks) {
        await (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$components$2f$hooks$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["runMultipleHook"])(zbsearch.afterUpdateMultiple, zbsearch, newIds);
    }
    return newIds;
}
function updateMultipleSync(zbsearch, ids, docs, batchSize, language, skipHooks) {
    if (!skipHooks) {
        (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$components$2f$hooks$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["runMultipleHook"])(zbsearch.beforeUpdateMultiple, zbsearch, ids);
    }
    const docsLength = docs.length;
    for(let i = 0; i < docsLength; i++){
        const errorProperty = zbsearch.validateSchema(docs[i], zbsearch.schema);
        if (errorProperty) {
            throw (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$errors$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["createError"])("SCHEMA_VALIDATION_FAILURE", errorProperty);
        }
    }
    (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$methods$2f$remove$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["removeMultiple"])(zbsearch, ids, batchSize, language, skipHooks);
    const newIds = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$methods$2f$insert$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["innerInsertMultiple"])(zbsearch, docs, batchSize, language, skipHooks);
    if (!skipHooks) {
        (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$components$2f$hooks$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["runMultipleHook"])(zbsearch.afterUpdateMultiple, zbsearch, newIds);
    }
    return newIds;
}
}),
"[project]/node_modules/zbsearch/dist/browser/methods/upsert.js [app-client] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "upsert",
    ()=>upsert,
    "upsertMultiple",
    ()=>upsertMultiple
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$components$2f$hooks$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/zbsearch/dist/browser/components/hooks.js [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$errors$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/zbsearch/dist/browser/errors.js [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$methods$2f$insert$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/zbsearch/dist/browser/methods/insert.js [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$methods$2f$update$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/zbsearch/dist/browser/methods/update.js [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$utils$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/zbsearch/dist/browser/utils.js [app-client] (ecmascript)");
;
;
;
;
;
function upsert(zbsearch, doc, language, skipHooks, options) {
    const asyncNeeded = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$utils$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["isAsyncFunction"])(zbsearch.afterInsert) || (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$utils$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["isAsyncFunction"])(zbsearch.beforeInsert) || (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$utils$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["isAsyncFunction"])(zbsearch.afterRemove) || (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$utils$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["isAsyncFunction"])(zbsearch.beforeRemove) || (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$utils$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["isAsyncFunction"])(zbsearch.beforeUpdate) || (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$utils$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["isAsyncFunction"])(zbsearch.afterUpdate) || (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$utils$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["isAsyncFunction"])(zbsearch.beforeUpsert) || (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$utils$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["isAsyncFunction"])(zbsearch.afterUpsert) || (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$utils$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["isAsyncFunction"])(zbsearch.index.beforeInsert) || (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$utils$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["isAsyncFunction"])(zbsearch.index.insert) || (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$utils$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["isAsyncFunction"])(zbsearch.index.afterInsert);
    if (asyncNeeded) {
        return upsertAsync(zbsearch, doc, language, skipHooks, options);
    }
    return upsertSync(zbsearch, doc, language, skipHooks, options);
}
async function upsertAsync(zbsearch, doc, language, skipHooks, options) {
    const id = zbsearch.getDocumentIndexId(doc);
    if (typeof id !== "string") {
        throw (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$errors$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["createError"])("DOCUMENT_ID_MUST_BE_STRING", typeof id);
    }
    if (!skipHooks && zbsearch.beforeUpsert) {
        await (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$components$2f$hooks$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["runSingleHook"])(zbsearch.beforeUpsert, zbsearch, id, doc);
    }
    // Check if document exists
    const existingDoc = zbsearch.documentsStore.get(zbsearch.data.docs, id);
    let resultId;
    if (existingDoc) {
        // Document exists, update it
        resultId = await (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$methods$2f$update$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["update"])(zbsearch, id, doc, language, skipHooks);
    } else {
        // Document doesn't exist, insert it
        resultId = await (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$methods$2f$insert$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["insert"])(zbsearch, doc, language, skipHooks, options);
    }
    if (!skipHooks && zbsearch.afterUpsert) {
        await (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$components$2f$hooks$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["runSingleHook"])(zbsearch.afterUpsert, zbsearch, resultId, doc);
    }
    return resultId;
}
function upsertSync(zbsearch, doc, language, skipHooks, options) {
    const id = zbsearch.getDocumentIndexId(doc);
    if (typeof id !== "string") {
        throw (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$errors$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["createError"])("DOCUMENT_ID_MUST_BE_STRING", typeof id);
    }
    if (!skipHooks && zbsearch.beforeUpsert) {
        (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$components$2f$hooks$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["runSingleHook"])(zbsearch.beforeUpsert, zbsearch, id, doc);
    }
    // Check if document exists
    const existingDoc = zbsearch.documentsStore.get(zbsearch.data.docs, id);
    let resultId;
    if (existingDoc) {
        // Document exists, update it
        resultId = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$methods$2f$update$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["update"])(zbsearch, id, doc, language, skipHooks);
    } else {
        // Document doesn't exist, insert it
        resultId = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$methods$2f$insert$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["insert"])(zbsearch, doc, language, skipHooks, options);
    }
    if (!skipHooks && zbsearch.afterUpsert) {
        (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$components$2f$hooks$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["runSingleHook"])(zbsearch.afterUpsert, zbsearch, resultId, doc);
    }
    return resultId;
}
function upsertMultiple(zbsearch, docs, batchSize, language, skipHooks) {
    const asyncNeeded = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$utils$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["isAsyncFunction"])(zbsearch.afterInsert) || (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$utils$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["isAsyncFunction"])(zbsearch.beforeInsert) || (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$utils$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["isAsyncFunction"])(zbsearch.afterRemove) || (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$utils$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["isAsyncFunction"])(zbsearch.beforeRemove) || (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$utils$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["isAsyncFunction"])(zbsearch.beforeUpdate) || (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$utils$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["isAsyncFunction"])(zbsearch.afterUpdate) || (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$utils$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["isAsyncFunction"])(zbsearch.beforeUpsert) || (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$utils$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["isAsyncFunction"])(zbsearch.afterUpsert) || (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$utils$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["isAsyncFunction"])(zbsearch.beforeUpsertMultiple) || (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$utils$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["isAsyncFunction"])(zbsearch.afterUpsertMultiple) || (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$utils$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["isAsyncFunction"])(zbsearch.beforeInsertMultiple) || (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$utils$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["isAsyncFunction"])(zbsearch.afterInsertMultiple) || (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$utils$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["isAsyncFunction"])(zbsearch.beforeUpdateMultiple) || (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$utils$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["isAsyncFunction"])(zbsearch.afterUpdateMultiple) || (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$utils$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["isAsyncFunction"])(zbsearch.beforeRemoveMultiple) || (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$utils$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["isAsyncFunction"])(zbsearch.afterRemoveMultiple) || (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$utils$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["isAsyncFunction"])(zbsearch.index.beforeInsert) || (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$utils$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["isAsyncFunction"])(zbsearch.index.insert) || (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$utils$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["isAsyncFunction"])(zbsearch.index.afterInsert);
    if (asyncNeeded) {
        return upsertMultipleAsync(zbsearch, docs, batchSize, language, skipHooks);
    }
    return upsertMultipleSync(zbsearch, docs, batchSize, language, skipHooks);
}
async function upsertMultipleAsync(zbsearch, docs, batchSize, language, skipHooks) {
    if (!skipHooks && zbsearch.beforeUpsertMultiple) {
        await (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$components$2f$hooks$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["runMultipleHook"])(zbsearch.beforeUpsertMultiple, zbsearch, docs);
    }
    // Validate all documents first
    const docsLength = docs.length;
    for(let i = 0; i < docsLength; i++){
        const errorProperty = zbsearch.validateSchema(docs[i], zbsearch.schema);
        if (errorProperty) {
            throw (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$errors$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["createError"])("SCHEMA_VALIDATION_FAILURE", errorProperty);
        }
    }
    // Separate documents into insert and update arrays
    const docsToInsert = [];
    const docsToUpdate = [];
    const idsToUpdate = [];
    for (const doc of docs){
        const id = zbsearch.getDocumentIndexId(doc);
        if (typeof id !== "string") {
            throw (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$errors$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["createError"])("DOCUMENT_ID_MUST_BE_STRING", typeof id);
        }
        const existingDoc = zbsearch.documentsStore.get(zbsearch.data.docs, id);
        if (existingDoc) {
            docsToUpdate.push(doc);
            idsToUpdate.push(id);
        } else {
            docsToInsert.push(doc);
        }
    }
    // Perform bulk operations
    const results = [];
    if (docsToUpdate.length > 0) {
        const updateResults = await (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$methods$2f$update$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["updateMultiple"])(zbsearch, idsToUpdate, docsToUpdate, batchSize, language, skipHooks);
        results.push(...updateResults);
    }
    if (docsToInsert.length > 0) {
        const insertResults = await (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$methods$2f$insert$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["innerInsertMultiple"])(zbsearch, docsToInsert, batchSize, language, skipHooks);
        results.push(...insertResults);
    }
    if (!skipHooks && zbsearch.afterUpsertMultiple) {
        await (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$components$2f$hooks$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["runMultipleHook"])(zbsearch.afterUpsertMultiple, zbsearch, results);
    }
    return results;
}
function upsertMultipleSync(zbsearch, docs, batchSize, language, skipHooks) {
    if (!skipHooks && zbsearch.beforeUpsertMultiple) {
        (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$components$2f$hooks$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["runMultipleHook"])(zbsearch.beforeUpsertMultiple, zbsearch, docs);
    }
    // Validate all documents first
    const docsLength = docs.length;
    for(let i = 0; i < docsLength; i++){
        const errorProperty = zbsearch.validateSchema(docs[i], zbsearch.schema);
        if (errorProperty) {
            throw (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$errors$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["createError"])("SCHEMA_VALIDATION_FAILURE", errorProperty);
        }
    }
    // Separate documents into insert and update arrays
    const docsToInsert = [];
    const docsToUpdate = [];
    const idsToUpdate = [];
    for (const doc of docs){
        const id = zbsearch.getDocumentIndexId(doc);
        if (typeof id !== "string") {
            throw (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$errors$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["createError"])("DOCUMENT_ID_MUST_BE_STRING", typeof id);
        }
        const existingDoc = zbsearch.documentsStore.get(zbsearch.data.docs, id);
        if (existingDoc) {
            docsToUpdate.push(doc);
            idsToUpdate.push(id);
        } else {
            docsToInsert.push(doc);
        }
    }
    // Perform bulk operations
    const results = [];
    if (docsToUpdate.length > 0) {
        const updateResults = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$methods$2f$update$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["updateMultiple"])(zbsearch, idsToUpdate, docsToUpdate, batchSize, language, skipHooks);
        results.push(...updateResults);
    }
    if (docsToInsert.length > 0) {
        const insertResults = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$methods$2f$insert$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["innerInsertMultiple"])(zbsearch, docsToInsert, batchSize, language, skipHooks);
        results.push(...insertResults);
    }
    if (!skipHooks && zbsearch.afterUpsertMultiple) {
        (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$components$2f$hooks$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["runMultipleHook"])(zbsearch.afterUpsertMultiple, zbsearch, results);
    }
    return results;
}
}),
"[project]/node_modules/zbsearch/dist/browser/trees/avl.js [app-client] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "AVLNode",
    ()=>AVLNode,
    "AVLTree",
    ()=>AVLTree
]);
class AVLNode {
    k;
    v;
    l = null;
    r = null;
    h = 1;
    constructor(key, value){
        this.k = key;
        this.v = new Set(value);
    }
    updateHeight() {
        const leftHeight = this.l ? this.l.h : 0;
        const rightHeight = this.r ? this.r.h : 0;
        this.h = leftHeight >= rightHeight ? leftHeight + 1 : rightHeight + 1;
    }
    static getHeight(node) {
        return node ? node.h : 0;
    }
    getBalanceFactor() {
        return (this.l ? this.l.h : 0) - (this.r ? this.r.h : 0);
    }
    rotateLeft() {
        const newRoot = this.r;
        this.r = newRoot.l;
        newRoot.l = this;
        this.updateHeight();
        newRoot.updateHeight();
        return newRoot;
    }
    rotateRight() {
        const newRoot = this.l;
        this.l = newRoot.r;
        newRoot.r = this;
        this.updateHeight();
        newRoot.updateHeight();
        return newRoot;
    }
    toJSON() {
        return {
            k: this.k,
            v: Array.from(this.v),
            l: this.l ? this.l.toJSON() : null,
            r: this.r ? this.r.toJSON() : null,
            h: this.h
        };
    }
    static fromJSON(json) {
        const node = new AVLNode(json.k, json.v);
        node.l = json.l ? AVLNode.fromJSON(json.l) : null;
        node.r = json.r ? AVLNode.fromJSON(json.r) : null;
        node.h = json.h;
        return node;
    }
}
class AVLTree {
    root = null;
    insertCount = 0;
    constructor(key, value){
        if (key !== undefined && value !== undefined) {
            this.root = new AVLNode(key, value);
        }
    }
    insert(key, value, rebalanceThreshold = 1e3) {
        this.root = this.insertNode(this.root, key, value, rebalanceThreshold);
    }
    insertMultiple(key, value, rebalanceThreshold = 1e3) {
        for(let i = 0; i < value.length; i++){
            this.insert(key, value[i], rebalanceThreshold);
        }
    }
    // Rebalance the tree if the insert count reaches the threshold.
    // This will improve insertion performance since we won't be rebalancing the tree on every insert.
    // When inserting docs using `insertMultiple`, the threshold will be set to the number of docs being inserted.
    // We can force rebalancing the tree by setting the threshold to 1 (default).
    rebalance() {
        if (this.root) {
            this.root = this.rebalanceNode(this.root);
        }
    }
    toJSON() {
        return {
            root: this.root ? this.root.toJSON() : null,
            insertCount: this.insertCount
        };
    }
    static fromJSON(json) {
        const tree = new AVLTree();
        tree.root = json.root ? AVLNode.fromJSON(json.root) : null;
        tree.insertCount = json.insertCount || 0;
        return tree;
    }
    insertNode(node, key, value, rebalanceThreshold) {
        if (node === null) {
            return new AVLNode(key, [
                value
            ]);
        }
        const willRebalance = rebalanceThreshold === 1 || (this.insertCount + 1) % rebalanceThreshold === 0;
        const pathNodes = [];
        const pathParents = willRebalance ? [] : [];
        let current = node;
        let parent = null;
        while(current !== null){
            pathNodes.push(current);
            if (willRebalance) {
                pathParents.push(parent);
            }
            const currentKey = current.k;
            if (key < currentKey) {
                const left = current.l;
                if (left === null) {
                    const newNode = new AVLNode(key, [
                        value
                    ]);
                    current.l = newNode;
                    pathNodes.push(newNode);
                    if (willRebalance) {
                        pathParents.push(current);
                    }
                    break;
                }
                parent = current;
                current = left;
            } else if (key > currentKey) {
                const right = current.r;
                if (right === null) {
                    const newNode = new AVLNode(key, [
                        value
                    ]);
                    current.r = newNode;
                    pathNodes.push(newNode);
                    if (willRebalance) {
                        pathParents.push(current);
                    }
                    break;
                }
                parent = current;
                current = right;
            } else {
                current.v.add(value);
                return node;
            }
        }
        this.insertCount++;
        if (willRebalance) {
            for(let i = pathNodes.length - 1; i >= 0; i--){
                const currentNode = pathNodes[i];
                const nodeParent = pathParents[i];
                currentNode.updateHeight();
                const balanceFactor = currentNode.getBalanceFactor();
                if (balanceFactor > 1 || balanceFactor < -1) {
                    const rebalancedNode = this.rebalanceNode(currentNode);
                    if (nodeParent) {
                        if (nodeParent.l === currentNode) {
                            nodeParent.l = rebalancedNode;
                        } else {
                            nodeParent.r = rebalancedNode;
                        }
                    } else {
                        node = rebalancedNode;
                    }
                }
            }
        } else {
            for(let i = pathNodes.length - 1; i >= 0; i--){
                pathNodes[i].updateHeight();
            }
        }
        return node;
    }
    rebalanceNode(node) {
        const balanceFactor = node.getBalanceFactor();
        if (balanceFactor > 1) {
            const left = node.l;
            if (left && left.getBalanceFactor() >= 0) {
                return node.rotateRight();
            }
            if (left) {
                node.l = left.rotateLeft();
                return node.rotateRight();
            }
        }
        if (balanceFactor < -1) {
            const right = node.r;
            if (right && right.getBalanceFactor() <= 0) {
                return node.rotateLeft();
            }
            if (right) {
                node.r = right.rotateRight();
                return node.rotateLeft();
            }
        }
        return node;
    }
    find(key) {
        const node = this.findNodeByKey(key);
        return node ? node.v : null;
    }
    contains(key) {
        let node = this.root;
        while(node !== null){
            const nodeKey = node.k;
            if (key < nodeKey) {
                node = node.l;
            } else if (key > nodeKey) {
                node = node.r;
            } else {
                return true;
            }
        }
        return false;
    }
    getSize() {
        let count = 0;
        const stack = [];
        let current = this.root;
        while(current || stack.length > 0){
            while(current){
                stack.push(current);
                current = current.l;
            }
            current = stack.pop();
            count++;
            current = current.r;
        }
        return count;
    }
    isBalanced() {
        if (!this.root) return true;
        const stack = [
            this.root
        ];
        while(stack.length > 0){
            const node = stack.pop();
            const balanceFactor = node.getBalanceFactor();
            if (balanceFactor > 1 || balanceFactor < -1) {
                return false;
            }
            if (node.l) stack.push(node.l);
            if (node.r) stack.push(node.r);
        }
        return true;
    }
    remove(key) {
        this.root = this.removeNode(this.root, key);
    }
    removeDocument(key, id) {
        const node = this.findNodeByKey(key);
        if (!node) {
            return;
        }
        if (node.v.size === 1) {
            this.root = this.removeNode(this.root, key);
        } else {
            node.v.delete(id);
        }
    }
    findNodeByKey(key) {
        let node = this.root;
        while(node !== null){
            const nodeKey = node.k;
            if (key < nodeKey) {
                node = node.l;
            } else if (key > nodeKey) {
                node = node.r;
            } else {
                return node;
            }
        }
        return null;
    }
    removeNode(node, key) {
        if (node === null) return null;
        const path = [];
        let current = node;
        while(current !== null && current.k !== key){
            path.push(current);
            if (key < current.k) {
                current = current.l;
            } else {
                current = current.r;
            }
        }
        if (current === null) {
            return node;
        }
        if (current.l === null || current.r === null) {
            const child = current.l ?? current.r;
            if (path.length === 0) {
                node = child;
            } else {
                const parent = path[path.length - 1];
                if (parent.l === current) {
                    parent.l = child;
                } else {
                    parent.r = child;
                }
            }
        } else {
            let successorParent = current;
            let successor = current.r;
            while(successor.l !== null){
                successorParent = successor;
                successor = successor.l;
            }
            current.k = successor.k;
            current.v = successor.v;
            if (successorParent.l === successor) {
                successorParent.l = successor.r;
            } else {
                successorParent.r = successor.r;
            }
            current = successorParent;
        }
        path.push(current);
        for(let i = path.length - 1; i >= 0; i--){
            const currentNode = path[i];
            currentNode.updateHeight();
            const balanceFactor = currentNode.getBalanceFactor();
            if (balanceFactor > 1 || balanceFactor < -1) {
                const rebalancedNode = this.rebalanceNode(currentNode);
                if (i > 0) {
                    const parent = path[i - 1];
                    if (parent.l === currentNode) {
                        parent.l = rebalancedNode;
                    } else {
                        parent.r = rebalancedNode;
                    }
                } else {
                    node = rebalancedNode;
                }
            }
        }
        return node;
    }
    rangeSearch(min, max) {
        const result = new Set();
        const stack = [];
        let current = this.root;
        while(current || stack.length > 0){
            while(current){
                if (current.k < min) {
                    current = current.r;
                } else {
                    stack.push(current);
                    current = current.l;
                }
            }
            current = stack.pop();
            if (current.k > max) {
                break;
            }
            for (const value of current.v){
                result.add(value);
            }
            current = current.r;
        }
        return result;
    }
    greaterThan(key, inclusive = false) {
        const result = new Set();
        const stack = [];
        let current = this.root;
        while(current || stack.length > 0){
            while(current){
                if (inclusive ? current.k < key : current.k <= key) {
                    current = current.r;
                } else {
                    stack.push(current);
                    current = current.r;
                }
            }
            if (stack.length === 0) {
                break;
            }
            current = stack.pop();
            if (inclusive && current.k >= key || !inclusive && current.k > key) {
                for (const value of current.v){
                    result.add(value);
                }
                current = current.l;
            } else {
                break;
            }
        }
        return result;
    }
    lessThan(key, inclusive = false) {
        const result = new Set();
        const stack = [];
        let current = this.root;
        while(current || stack.length > 0){
            while(current){
                if (inclusive ? current.k > key : current.k >= key) {
                    current = current.l;
                } else {
                    stack.push(current);
                    current = current.l;
                }
            }
            if (stack.length === 0) {
                break;
            }
            current = stack.pop();
            if (inclusive && current.k <= key || !inclusive && current.k < key) {
                for (const value of current.v){
                    result.add(value);
                }
                current = current.r;
            } else {
                break;
            }
        }
        return result;
    }
}
}),
"[project]/node_modules/zbsearch/dist/browser/trees/bkd.js [app-client] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "BKDTree",
    ()=>BKDTree
]);
const _K = 2;
const EARTH_RADIUS = 6371e3;
class BKDNode {
    point;
    docIDs;
    left;
    right;
    parent;
    constructor(point, docIDs){
        this.point = point;
        this.docIDs = new Set(docIDs);
        this.left = null;
        this.right = null;
        this.parent = null;
    }
    toJSON() {
        return {
            point: this.point,
            docIDs: Array.from(this.docIDs),
            left: this.left ? this.left.toJSON() : null,
            right: this.right ? this.right.toJSON() : null
        };
    }
    static fromJSON(json, parent = null) {
        const node = new BKDNode(json.point, json.docIDs);
        node.parent = parent;
        if (json.left) {
            node.left = BKDNode.fromJSON(json.left, node);
        }
        if (json.right) {
            node.right = BKDNode.fromJSON(json.right, node);
        }
        return node;
    }
}
class BKDTree {
    root;
    nodeMap;
    constructor(){
        this.root = null;
        this.nodeMap = new Map();
    }
    getPointKey(point) {
        return point.lon + "," + point.lat;
    }
    insert(point, docIDs) {
        const lon = point.lon;
        const lat = point.lat;
        const pointKey = lon + "," + lat;
        const existingNode = this.nodeMap.get(pointKey);
        if (existingNode) {
            for(let i = 0; i < docIDs.length; i++){
                existingNode.docIDs.add(docIDs[i]);
            }
            return;
        }
        const newNode = new BKDNode(point, docIDs);
        this.nodeMap.set(pointKey, newNode);
        const root = this.root;
        if (root == null) {
            this.root = newNode;
            return;
        }
        let node = root;
        let depth = 0;
        // eslint-disable-next-line no-constant-condition
        while(true){
            const axis = depth & 1;
            const nodePoint = node.point;
            const goLeft = (axis ? lat : lon) < (axis ? nodePoint.lat : nodePoint.lon);
            if (goLeft) {
                const left = node.left;
                if (left == null) {
                    node.left = newNode;
                    newNode.parent = node;
                    return;
                }
                node = left;
            } else {
                const right = node.right;
                if (right == null) {
                    node.right = newNode;
                    newNode.parent = node;
                    return;
                }
                node = right;
            }
            depth++;
        }
    }
    contains(point) {
        return this.nodeMap.has(point.lon + "," + point.lat);
    }
    getDocIDsByCoordinates(point) {
        const pointKey = this.getPointKey(point);
        const node = this.nodeMap.get(pointKey);
        if (node) {
            return Array.from(node.docIDs);
        }
        return null;
    }
    removeDocByID(point, docID) {
        const pointKey = this.getPointKey(point);
        const node = this.nodeMap.get(pointKey);
        if (node) {
            node.docIDs.delete(docID);
            if (node.docIDs.size === 0) {
                this.nodeMap.delete(pointKey);
                this.deleteNode(node);
            }
        }
    }
    deleteNode(node) {
        const parent = node.parent;
        const child = node.left ? node.left : node.right;
        if (child) {
            child.parent = parent;
        }
        if (parent) {
            if (parent.left === node) {
                parent.left = child;
            } else if (parent.right === node) {
                parent.right = child;
            }
        } else {
            this.root = child;
            if (this.root) {
                this.root.parent = null;
            }
        }
    }
    searchByRadius(center, radius, inclusive = true, sort = "asc", highPrecision = false) {
        const root = this.root;
        if (root == null) {
            return [];
        }
        const distanceFn = highPrecision ? BKDTree.vincentyDistance : BKDTree.haversineDistance;
        const axisDistanceFn = highPrecision ? BKDTree.vincentyAxisDistance : BKDTree.haversineAxisDistance;
        const result = [];
        const sortedEntries = [];
        const isAsc = sort == null || sort.toLowerCase() === "asc";
        if (inclusive) {
            const stack = [
                root
            ];
            const depths = [
                0
            ];
            while(stack.length > 0){
                const node = stack.pop();
                const depth = depths.pop();
                const dist = distanceFn(center, node.point);
                if (dist <= radius) {
                    const entry = {
                        point: node.point,
                        docIDs: Array.from(node.docIDs)
                    };
                    if (sort) {
                        sortedEntries.push([
                            dist,
                            entry
                        ]);
                    } else {
                        result.push(entry);
                    }
                }
                const axis = depth & 1;
                const centerCoord = axis === 0 ? center.lon : center.lat;
                const nodeCoord = axis === 0 ? node.point.lon : node.point.lat;
                const searchLeft = centerCoord < nodeCoord;
                const near = searchLeft ? node.left : node.right;
                const far = searchLeft ? node.right : node.left;
                if (near != null) {
                    stack.push(near);
                    depths.push(depth + 1);
                }
                if (far != null && axisDistanceFn(center, node.point, axis) <= radius) {
                    stack.push(far);
                    depths.push(depth + 1);
                }
            }
        } else {
            const stack = [
                root
            ];
            while(stack.length > 0){
                const node = stack.pop();
                const dist = distanceFn(center, node.point);
                if (dist > radius) {
                    const entry = {
                        point: node.point,
                        docIDs: Array.from(node.docIDs)
                    };
                    if (sort) {
                        sortedEntries.push([
                            dist,
                            entry
                        ]);
                    } else {
                        result.push(entry);
                    }
                }
                if (node.left != null) {
                    stack.push(node.left);
                }
                if (node.right != null) {
                    stack.push(node.right);
                }
            }
        }
        if (sort && sortedEntries.length > 0) {
            sortedEntries.sort((a, b)=>isAsc ? a[0] - b[0] : b[0] - a[0]);
            return sortedEntries.map(([, entry])=>entry);
        }
        return result;
    }
    searchByPolygon(polygon, inclusive = true, sort = null, highPrecision = false) {
        const root = this.root;
        if (root == null) {
            return [];
        }
        const bounds = BKDTree.getPolygonBounds(polygon);
        const result = [];
        const sortedEntries = [];
        const isAsc = sort == null || sort.toLowerCase() === "asc";
        const centroid = sort ? BKDTree.calculatePolygonCentroid(polygon) : null;
        const distanceFn = sort != null ? highPrecision ? BKDTree.vincentyDistance : BKDTree.haversineDistance : null;
        if (inclusive) {
            const stack = [
                root
            ];
            const depths = [
                0
            ];
            while(stack.length > 0){
                const node = stack.pop();
                const depth = depths.pop();
                const axis = depth & 1;
                if (axis === 0 ? bounds.minLon < node.point.lon : bounds.minLat < node.point.lat) {
                    const left = node.left;
                    if (left != null) {
                        stack.push(left);
                        depths.push(depth + 1);
                    }
                }
                if (axis === 0 ? bounds.maxLon >= node.point.lon : bounds.maxLat >= node.point.lat) {
                    const right = node.right;
                    if (right != null) {
                        stack.push(right);
                        depths.push(depth + 1);
                    }
                }
                const point = node.point;
                if (point.lon >= bounds.minLon && point.lon <= bounds.maxLon && point.lat >= bounds.minLat && point.lat <= bounds.maxLat && BKDTree.isPointInPolygon(polygon, point)) {
                    const entry = {
                        point,
                        docIDs: Array.from(node.docIDs)
                    };
                    if (sort && distanceFn && centroid) {
                        sortedEntries.push([
                            distanceFn(centroid, point),
                            entry
                        ]);
                    } else {
                        result.push(entry);
                    }
                }
            }
        } else {
            const stack = [
                {
                    node: root,
                    depth: 0
                }
            ];
            while(stack.length > 0){
                const { node, depth } = stack.pop();
                if (node == null) continue;
                if (node.left != null) {
                    stack.push({
                        node: node.left,
                        depth: depth + 1
                    });
                }
                if (node.right != null) {
                    stack.push({
                        node: node.right,
                        depth: depth + 1
                    });
                }
                const point = node.point;
                const inBounds = point.lon >= bounds.minLon && point.lon <= bounds.maxLon && point.lat >= bounds.minLat && point.lat <= bounds.maxLat;
                const isInsidePolygon = inBounds && BKDTree.isPointInPolygon(polygon, point);
                if (!isInsidePolygon) {
                    const entry = {
                        point,
                        docIDs: Array.from(node.docIDs)
                    };
                    if (sort && distanceFn && centroid) {
                        sortedEntries.push([
                            distanceFn(centroid, point),
                            entry
                        ]);
                    } else {
                        result.push(entry);
                    }
                }
            }
        }
        if (sort && sortedEntries.length > 0) {
            sortedEntries.sort((a, b)=>isAsc ? a[0] - b[0] : b[0] - a[0]);
            return sortedEntries.map(([, entry])=>entry);
        }
        return result;
    }
    toJSON() {
        return {
            root: this.root ? this.root.toJSON() : null
        };
    }
    static fromJSON(json) {
        const tree = new BKDTree();
        if (json.root) {
            tree.root = BKDNode.fromJSON(json.root);
            tree.buildNodeMap(tree.root);
        }
        return tree;
    }
    buildNodeMap(node) {
        if (node == null) return;
        const pointKey = this.getPointKey(node.point);
        this.nodeMap.set(pointKey, node);
        if (node.left) {
            this.buildNodeMap(node.left);
        }
        if (node.right) {
            this.buildNodeMap(node.right);
        }
    }
    static getPolygonBounds(polygon) {
        let minLon = polygon[0].lon;
        let maxLon = polygon[0].lon;
        let minLat = polygon[0].lat;
        let maxLat = polygon[0].lat;
        for(let i = 1; i < polygon.length; i++){
            const p = polygon[i];
            if (p.lon < minLon) minLon = p.lon;
            if (p.lon > maxLon) maxLon = p.lon;
            if (p.lat < minLat) minLat = p.lat;
            if (p.lat > maxLat) maxLat = p.lat;
        }
        return {
            minLon,
            maxLon,
            minLat,
            maxLat
        };
    }
    static calculatePolygonCentroid(polygon) {
        let totalArea = 0;
        let centroidX = 0;
        let centroidY = 0;
        const polygonLength = polygon.length;
        for(let i = 0, j = polygonLength - 1; i < polygonLength; j = i++){
            const xi = polygon[i].lon;
            const yi = polygon[i].lat;
            const xj = polygon[j].lon;
            const yj = polygon[j].lat;
            const areaSegment = xi * yj - xj * yi;
            totalArea += areaSegment;
            centroidX += (xi + xj) * areaSegment;
            centroidY += (yi + yj) * areaSegment;
        }
        totalArea /= 2;
        const centroidCoordinate = 6 * totalArea;
        centroidX /= centroidCoordinate;
        centroidY /= centroidCoordinate;
        return {
            lon: centroidX,
            lat: centroidY
        };
    }
    static isPointInPolygon(polygon, point) {
        let isInside = false;
        const x = point.lon;
        const y = point.lat;
        const polygonLength = polygon.length;
        for(let i = 0, j = polygonLength - 1; i < polygonLength; j = i++){
            const xi = polygon[i].lon;
            const yi = polygon[i].lat;
            const xj = polygon[j].lon;
            const yj = polygon[j].lat;
            const intersect = yi > y !== yj > y && x < (xj - xi) * (y - yi) / (yj - yi) + xi;
            if (intersect) isInside = !isInside;
        }
        return isInside;
    }
    static haversineAxisDistance(center, planePoint, axis) {
        if (axis === 0) {
            return BKDTree.haversineDistance(center, {
                lon: planePoint.lon,
                lat: center.lat
            });
        }
        return BKDTree.haversineDistance(center, {
            lon: center.lon,
            lat: planePoint.lat
        });
    }
    static vincentyAxisDistance(center, planePoint, axis) {
        if (axis === 0) {
            return BKDTree.vincentyDistance(center, {
                lon: planePoint.lon,
                lat: center.lat
            });
        }
        return BKDTree.vincentyDistance(center, {
            lon: center.lon,
            lat: planePoint.lat
        });
    }
    static haversineDistance(coord1, coord2) {
        const P = Math.PI / 180;
        const lat1 = coord1.lat * P;
        const lat2 = coord2.lat * P;
        const deltaLat = (coord2.lat - coord1.lat) * P;
        const deltaLon = (coord2.lon - coord1.lon) * P;
        const a = Math.sin(deltaLat / 2) * Math.sin(deltaLat / 2) + Math.cos(lat1) * Math.cos(lat2) * Math.sin(deltaLon / 2) * Math.sin(deltaLon / 2);
        const c = 2 * Math.atan2(Math.sqrt(a), Math.sqrt(1 - a));
        return EARTH_RADIUS * c;
    }
    static vincentyDistance(coord1, coord2) {
        const a = 6378137;
        const f = 1 / 298.257223563;
        const b = (1 - f) * a;
        const P = Math.PI / 180;
        const lat1 = coord1.lat * P;
        const lat2 = coord2.lat * P;
        const deltaLon = (coord2.lon - coord1.lon) * P;
        const U1 = Math.atan((1 - f) * Math.tan(lat1));
        const U2 = Math.atan((1 - f) * Math.tan(lat2));
        const sinU1 = Math.sin(U1);
        const cosU1 = Math.cos(U1);
        const sinU2 = Math.sin(U2);
        const cosU2 = Math.cos(U2);
        let lambda = deltaLon;
        let prevLambda;
        let iterationLimit = 1e3;
        let sinSigma;
        let cosSigma;
        let sigma;
        let sinAlpha;
        let cos2Alpha;
        let cos2SigmaM;
        do {
            const sinLambda = Math.sin(lambda);
            const cosLambda = Math.cos(lambda);
            sinSigma = Math.sqrt(cosU2 * sinLambda * (cosU2 * sinLambda) + (cosU1 * sinU2 - sinU1 * cosU2 * cosLambda) * (cosU1 * sinU2 - sinU1 * cosU2 * cosLambda));
            if (sinSigma === 0) return 0;
            cosSigma = sinU1 * sinU2 + cosU1 * cosU2 * cosLambda;
            sigma = Math.atan2(sinSigma, cosSigma);
            sinAlpha = cosU1 * cosU2 * sinLambda / sinSigma;
            cos2Alpha = 1 - sinAlpha * sinAlpha;
            cos2SigmaM = cosSigma - 2 * sinU1 * sinU2 / cos2Alpha;
            if (isNaN(cos2SigmaM)) cos2SigmaM = 0;
            const C = f / 16 * cos2Alpha * (4 + f * (4 - 3 * cos2Alpha));
            prevLambda = lambda;
            lambda = deltaLon + (1 - C) * f * sinAlpha * (sigma + C * sinSigma * (cos2SigmaM + C * cosSigma * (-1 + 2 * cos2SigmaM * cos2SigmaM)));
        }while (Math.abs(lambda - prevLambda) > 1e-12 && --iterationLimit > 0)
        if (iterationLimit === 0) {
            return NaN;
        }
        const uSquared = cos2Alpha * (a * a - b * b) / (b * b);
        const A = 1 + uSquared / 16384 * (4096 + uSquared * (-768 + uSquared * (320 - 175 * uSquared)));
        const B = uSquared / 1024 * (256 + uSquared * (-128 + uSquared * (74 - 47 * uSquared)));
        const deltaSigma = B * sinSigma * (cos2SigmaM + B / 4 * (cosSigma * (-1 + 2 * cos2SigmaM * cos2SigmaM) - B / 6 * cos2SigmaM * (-3 + 4 * sinSigma * sinSigma) * (-3 + 4 * cos2SigmaM * cos2SigmaM)));
        const s = b * A * (sigma - deltaSigma);
        return s;
    }
}
}),
"[project]/node_modules/zbsearch/dist/browser/trees/bool.js [app-client] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "BoolNode",
    ()=>BoolNode
]);
class BoolNode {
    true;
    false;
    constructor(){
        this.true = new Set();
        this.false = new Set();
    }
    insert(value, bool) {
        if (bool) {
            this.true.add(value);
        } else {
            this.false.add(value);
        }
    }
    delete(value, bool) {
        if (bool) {
            this.true.delete(value);
        } else {
            this.false.delete(value);
        }
    }
    getSize() {
        return this.true.size + this.false.size;
    }
    toJSON() {
        return {
            true: Array.from(this.true),
            false: Array.from(this.false)
        };
    }
    static fromJSON(json) {
        const node = new BoolNode();
        node.true = new Set(json.true);
        node.false = new Set(json.false);
        return node;
    }
}
}),
"[project]/node_modules/zbsearch/dist/browser/trees/flat.js [app-client] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "FlatTree",
    ()=>FlatTree
]);
class FlatTree {
    numberToDocumentId;
    constructor(){
        this.numberToDocumentId = new Map();
    }
    insert(key, value) {
        if (this.numberToDocumentId.has(key)) {
            this.numberToDocumentId.get(key).add(value);
        } else {
            this.numberToDocumentId.set(key, new Set([
                value
            ]));
        }
    }
    find(key) {
        const idSet = this.numberToDocumentId.get(key);
        return idSet ? Array.from(idSet) : null;
    }
    remove(key) {
        this.numberToDocumentId.delete(key);
    }
    removeDocument(id, key) {
        const idSet = this.numberToDocumentId.get(key);
        if (idSet) {
            idSet.delete(id);
            if (idSet.size === 0) {
                this.numberToDocumentId.delete(key);
            }
        }
    }
    contains(key) {
        return this.numberToDocumentId.has(key);
    }
    getSize() {
        let size = 0;
        for (const idSet of this.numberToDocumentId.values()){
            size += idSet.size;
        }
        return size;
    }
    filter(operation) {
        const operationKeys = Object.keys(operation);
        if (operationKeys.length !== 1) {
            throw new Error("Invalid operation");
        }
        const operationType = operationKeys[0];
        switch(operationType){
            case "eq":
                {
                    const value = operation[operationType];
                    const idSet = this.numberToDocumentId.get(value);
                    return idSet ? Array.from(idSet) : [];
                }
            case "in":
                {
                    const values = operation[operationType];
                    const resultSet = new Set();
                    for (const value of values){
                        const idSet = this.numberToDocumentId.get(value);
                        if (idSet) {
                            for (const id of idSet){
                                resultSet.add(id);
                            }
                        }
                    }
                    return Array.from(resultSet);
                }
            case "nin":
                {
                    const excludeValues = new Set(operation[operationType]);
                    const resultSet = new Set();
                    for (const [key, idSet] of this.numberToDocumentId.entries()){
                        if (!excludeValues.has(key)) {
                            for (const id of idSet){
                                resultSet.add(id);
                            }
                        }
                    }
                    return Array.from(resultSet);
                }
            default:
                throw new Error("Invalid operation");
        }
    }
    filterArr(operation) {
        const operationKeys = Object.keys(operation);
        if (operationKeys.length !== 1) {
            throw new Error("Invalid operation");
        }
        const operationType = operationKeys[0];
        switch(operationType){
            case "containsAll":
                {
                    const values = operation[operationType];
                    const idSets = values.map((value)=>this.numberToDocumentId.get(value) ?? new Set());
                    if (idSets.length === 0) return [];
                    const intersection = idSets.reduce((prev, curr)=>{
                        return new Set([
                            ...prev
                        ].filter((id)=>curr.has(id)));
                    });
                    return Array.from(intersection);
                }
            case "containsAny":
                {
                    const values = operation[operationType];
                    const idSets = values.map((value)=>this.numberToDocumentId.get(value) ?? new Set());
                    if (idSets.length === 0) return [];
                    const union = idSets.reduce((prev, curr)=>{
                        return new Set([
                            ...prev,
                            ...curr
                        ]);
                    });
                    return Array.from(union);
                }
            default:
                throw new Error("Invalid operation");
        }
    }
    static fromJSON(json) {
        if (!json.numberToDocumentId) {
            throw new Error("Invalid Flat Tree JSON");
        }
        const tree = new FlatTree();
        for (const [key, ids] of json.numberToDocumentId){
            tree.numberToDocumentId.set(key, new Set(ids));
        }
        return tree;
    }
    toJSON() {
        return {
            numberToDocumentId: Array.from(this.numberToDocumentId.entries()).map(([key, idSet])=>[
                    key,
                    Array.from(idSet)
                ])
        };
    }
}
}),
"[project]/node_modules/zbsearch/dist/browser/trees/postings.js [app-client] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "addPosting",
    ()=>addPosting,
    "appendPosting",
    ()=>appendPosting,
    "clearPostings",
    ()=>clearPostings,
    "collectLegacyNodePostings",
    ()=>collectLegacyNodePostings,
    "createPostingsMap",
    ()=>createPostingsMap,
    "decodePostings",
    ()=>decodePostings,
    "deserializePostingsMap",
    ()=>deserializePostingsMap,
    "encodePostings",
    ()=>encodePostings,
    "getDocumentFrequency",
    ()=>getDocumentFrequency,
    "getPostings",
    ()=>getPostings,
    "removePosting",
    ()=>removePosting,
    "serializePostingsMap",
    ()=>serializePostingsMap
]);
function createPostingsMap() {
    return new Map();
}
function getPostings(postings, word) {
    return postings.get(word) ?? [];
}
function addPosting(postings, word, docId) {
    let list = postings.get(word);
    if (!list) {
        list = [];
        postings.set(word, list);
    }
    if (!list.includes(docId)) {
        list.push(docId);
    }
}
function appendPosting(postings, word, docId) {
    let list = postings.get(word);
    if (!list) {
        list = [
            docId
        ];
        postings.set(word, list);
        return;
    }
    list.push(docId);
}
function removePosting(postings, word, docId) {
    const list = postings.get(word);
    if (!list) {
        return false;
    }
    const index = list.indexOf(docId);
    if (index === -1) {
        return false;
    }
    list.splice(index, 1);
    if (list.length === 0) {
        postings.delete(word);
    }
    return true;
}
function clearPostings(postings, word) {
    postings.delete(word);
}
function getDocumentFrequency(postings, word) {
    return postings.get(word)?.length ?? 0;
}
function encodePostings(ids) {
    if (ids.length === 0) {
        return [];
    }
    const sorted = [
        ...ids
    ].sort((a, b)=>a - b);
    const out = [
        sorted[0]
    ];
    for(let i = 1; i < sorted.length; i++){
        out.push(sorted[i] - sorted[i - 1]);
    }
    return out;
}
function decodePostings(encoded) {
    if (encoded.length === 0) {
        return [];
    }
    const out = [
        encoded[0]
    ];
    for(let i = 1; i < encoded.length; i++){
        out.push(out[i - 1] + encoded[i]);
    }
    return out;
}
function serializePostingsMap(postings) {
    const out = {};
    for (const [word, ids] of postings){
        if (ids.length > 0) {
            out[word] = encodePostings(ids);
        }
    }
    return out;
}
function deserializePostingsMap(data) {
    const postings = createPostingsMap();
    if (!data) {
        return postings;
    }
    for (const word of Object.keys(data)){
        const ids = decodePostings(data[word]);
        if (ids.length > 0) {
            postings.set(word, ids);
        }
    }
    return postings;
}
function collectLegacyNodePostings(nodeJson, postings) {
    if (nodeJson.e && nodeJson.w !== undefined) {
        const ids = nodeJson.d ?? [];
        if (ids.length > 0) {
            postings.set(nodeJson.w, [
                ...ids
            ]);
        }
    }
    const children = nodeJson.c ?? [];
    for(let i = 0; i < children.length; i++){
        collectLegacyNodePostings(children[i][1], postings);
    }
}
}),
"[project]/node_modules/zbsearch/dist/browser/trees/radix.js [app-client] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "RadixNode",
    ()=>RadixNode,
    "RadixTree",
    ()=>RadixTree
]);
/* eslint-disable @typescript-eslint/no-this-alias */ var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$components$2f$levenshtein$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/zbsearch/dist/browser/components/levenshtein.js [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$trees$2f$postings$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/zbsearch/dist/browser/trees/postings.js [app-client] (ecmascript)");
;
;
const EMPTY_POSTINGS = [];
class RadixNode {
    // Node key
    k;
    // Node subword
    s;
    // Node children
    c = new Map();
    // Node end
    e;
    // Node word
    w = "";
    // Cached postings list for this word (same array as in the tree PostingsMap)
    d;
    constructor(key, subWord, end){
        this.k = key;
        this.s = subWord;
        this.e = end;
    }
    updateParent(parent) {
        this.w = parent.w + this.s;
    }
    addDocumentToPostings(postings, docID) {
        let list = this.d;
        if (list) {
            list.push(docID);
            return;
        }
        list = postings.get(this.w);
        if (!list) {
            list = [
                docID
            ];
            postings.set(this.w, list);
            this.d = list;
            return;
        }
        list.push(docID);
        this.d = list;
    }
    removeDocumentFromPostings(postings, docID) {
        const list = this.d ?? postings.get(this.w);
        if (!list) {
            return false;
        }
        const index = list.indexOf(docID);
        if (index === -1) {
            return false;
        }
        list.splice(index, 1);
        if (list.length === 0) {
            postings.delete(this.w);
            this.d = undefined;
        } else {
            this.d = list;
        }
        return true;
    }
    getDocumentsFromPostings(postings) {
        if (this.d) {
            return this.d;
        }
        const list = postings.get(this.w);
        if (!list) {
            return EMPTY_POSTINGS;
        }
        this.d = list;
        return list;
    }
    hasDocumentsInPostings(postings) {
        if (this.d) {
            return this.d.length > 0;
        }
        const list = postings.get(this.w);
        return list !== undefined && list.length > 0;
    }
    findAllWords(output, term, postings, exact, tolerance) {
        const stack = [
            this
        ];
        while(stack.length > 0){
            const node = stack.pop();
            if (node.e) {
                const { w } = node;
                if (exact && w !== term) {
                    continue;
                }
                if (tolerance) {
                    const difference = Math.abs(term.length - w.length);
                    if (difference > tolerance || !(0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$components$2f$levenshtein$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["syncBoundedLevenshtein"])(term, w, tolerance).isBounded) {
                        continue;
                    }
                }
                const docIDs = node.getDocumentsFromPostings(postings);
                if (docIDs.length > 0) {
                    output[w] = [
                        ...docIDs
                    ];
                } else {
                    output[w] = [];
                }
            }
            const children = node.c;
            if (children.size > 0) {
                for (const child of children.values()){
                    stack.push(child);
                }
            }
        }
        return output;
    }
    insertWithPostings(word, docId, postings) {
        let node = this;
        let i = 0;
        const wordLength = word.length;
        while(i < wordLength){
            const currentCharacter = word[i];
            const childNode = node.c.get(currentCharacter);
            if (childNode) {
                const edgeLabel = childNode.s;
                const edgeLabelLength = edgeLabel.length;
                let j = 0;
                while(j < edgeLabelLength && i + j < wordLength && edgeLabel.charCodeAt(j) === word.charCodeAt(i + j)){
                    j++;
                }
                if (j === edgeLabelLength) {
                    node = childNode;
                    i += j;
                    if (i === wordLength) {
                        if (!childNode.e) {
                            childNode.e = true;
                        }
                        childNode.addDocumentToPostings(postings, docId);
                        return;
                    }
                    continue;
                }
                const commonPrefix = edgeLabel.slice(0, j);
                const newEdgeLabel = edgeLabel.slice(j);
                const newWordLabel = word.slice(i + j);
                const inbetweenNode = new RadixNode(commonPrefix[0], commonPrefix, false);
                inbetweenNode.w = node.w + commonPrefix;
                node.c.set(commonPrefix[0], inbetweenNode);
                childNode.s = newEdgeLabel;
                childNode.k = newEdgeLabel[0];
                inbetweenNode.c.set(newEdgeLabel[0], childNode);
                childNode.w = inbetweenNode.w + newEdgeLabel;
                if (newWordLabel) {
                    const newNode = new RadixNode(newWordLabel[0], newWordLabel, true);
                    newNode.w = inbetweenNode.w + newWordLabel;
                    inbetweenNode.c.set(newWordLabel[0], newNode);
                    newNode.addDocumentToPostings(postings, docId);
                } else {
                    inbetweenNode.e = true;
                    inbetweenNode.addDocumentToPostings(postings, docId);
                }
                return;
            } else {
                const suffix = word.slice(i);
                const newNode = new RadixNode(currentCharacter, suffix, true);
                newNode.w = node.w + suffix;
                node.c.set(currentCharacter, newNode);
                newNode.addDocumentToPostings(postings, docId);
                return;
            }
        }
        if (!node.e) {
            node.e = true;
        }
        node.addDocumentToPostings(postings, docId);
    }
    _findLevenshtein(term, tolerance, output, postings) {
        const termLength = term.length;
        if (!termLength) {
            this.findAllWords(output, term, postings, false, 0);
            return;
        }
        const initialRow = new Array(termLength + 1);
        for(let i = 0; i <= termLength; i++){
            initialRow[i] = i;
        }
        const stack = [
            {
                node: this,
                row: initialRow
            }
        ];
        while(stack.length > 0){
            const { node, row } = stack.pop();
            if (node.e && row[termLength] <= tolerance) {
                const docIDs = node.getDocumentsFromPostings(postings);
                output[node.w] = docIDs.length > 0 ? [
                    ...docIDs
                ] : [];
            }
            for (const child of node.c.values()){
                const label = child.s;
                let currentRow = row;
                let pruned = false;
                let prefixMatched = false;
                for(let charIndex = 0; charIndex < label.length; charIndex++){
                    const charCode = label.charCodeAt(charIndex);
                    const nextRow = new Array(termLength + 1);
                    nextRow[0] = currentRow[0] + 1;
                    let rowMin = nextRow[0];
                    for(let i = 1; i <= termLength; i++){
                        const value = term.charCodeAt(i - 1) === charCode ? currentRow[i - 1] : Math.min(currentRow[i] + 1, nextRow[i - 1] + 1, currentRow[i - 1] + 1);
                        nextRow[i] = value;
                        if (value < rowMin) {
                            rowMin = value;
                        }
                    }
                    currentRow = nextRow;
                    if (!nextRow[termLength]) {
                        prefixMatched = true;
                        break;
                    }
                    if (rowMin > tolerance) {
                        pruned = true;
                        break;
                    }
                }
                if (prefixMatched) {
                    child.findAllWords(output, term, postings, false, 0);
                } else if (!pruned) {
                    stack.push({
                        node: child,
                        row: currentRow
                    });
                }
            }
        }
    }
    findWithPostings(params, postings) {
        const { term, exact, tolerance } = params;
        if (tolerance && !exact) {
            const output = {};
            this._findLevenshtein(term, tolerance, output, postings);
            return output;
        }
        let node = this;
        let i = 0;
        const termLength = term.length;
        while(i < termLength){
            const character = term[i];
            const childNode = node.c.get(character);
            if (childNode) {
                const edgeLabel = childNode.s;
                const edgeLabelLength = edgeLabel.length;
                let j = 0;
                while(j < edgeLabelLength && i + j < termLength && edgeLabel.charCodeAt(j) === term.charCodeAt(i + j)){
                    j++;
                }
                if (j === edgeLabelLength) {
                    node = childNode;
                    i += j;
                } else if (i + j === termLength) {
                    if (j === termLength - i) {
                        if (exact) {
                            return {};
                        }
                        const output = {};
                        childNode.findAllWords(output, term, postings, exact, tolerance);
                        return output;
                    }
                    return {};
                } else {
                    return {};
                }
            } else {
                return {};
            }
        }
        const output = {};
        node.findAllWords(output, term, postings, exact, tolerance);
        return output;
    }
    contains(term) {
        let node = this;
        let i = 0;
        const termLength = term.length;
        while(i < termLength){
            const character = term[i];
            const childNode = node.c.get(character);
            if (childNode) {
                const edgeLabel = childNode.s;
                const edgeLabelLength = edgeLabel.length;
                let j = 0;
                while(j < edgeLabelLength && i + j < termLength && edgeLabel.charCodeAt(j) === term.charCodeAt(i + j)){
                    j++;
                }
                if (j < edgeLabelLength) {
                    return false;
                }
                i += edgeLabelLength;
                node = childNode;
            } else {
                return false;
            }
        }
        return true;
    }
    removeWordWithPostings(term, postings) {
        if (!term) {
            return false;
        }
        let node = this;
        const termLength = term.length;
        const stack = [];
        for(let i = 0; i < termLength; i++){
            const character = term[i];
            if (node.c.has(character)) {
                const childNode = node.c.get(character);
                stack.push({
                    parent: node,
                    character
                });
                i += childNode.s.length - 1;
                node = childNode;
            } else {
                return false;
            }
        }
        (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$trees$2f$postings$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["clearPostings"])(postings, node.w);
        node.d = undefined;
        node.e = false;
        while(stack.length > 0 && node.c.size === 0 && !node.e && !node.hasDocumentsInPostings(postings)){
            const { parent, character } = stack.pop();
            parent.c.delete(character);
            node = parent;
        }
        return true;
    }
    removeDocumentByWordWithPostings(term, docID, postings, exact = true) {
        if (!term) {
            return true;
        }
        let node = this;
        const termLength = term.length;
        for(let i = 0; i < termLength; i++){
            const character = term[i];
            if (node.c.has(character)) {
                const childNode = node.c.get(character);
                i += childNode.s.length - 1;
                node = childNode;
                if (exact && node.w !== term) {} else {
                    node.removeDocumentFromPostings(postings, docID);
                }
            } else {
                return false;
            }
        }
        return true;
    }
    toNodeJSON() {
        return {
            w: this.w,
            s: this.s,
            e: this.e,
            k: this.k,
            c: Array.from(this.c.entries()).map(([key, node])=>[
                    key,
                    node.toNodeJSON()
                ])
        };
    }
    static fromNodeJSON(json) {
        const node = new RadixNode(json.k, json.s, json.e);
        node.w = json.w;
        node.c = new Map(json.c?.map(([key, nodeJson])=>[
                key,
                RadixNode.fromNodeJSON(nodeJson)
            ]) || []);
        return node;
    }
}
class RadixTree extends RadixNode {
    postings = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$trees$2f$postings$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["createPostingsMap"])();
    constructor(){
        super("", "", false);
    }
    insert(word, docId) {
        this.insertWithPostings(word, docId, this.postings);
    }
    find(params) {
        return this.findWithPostings(params, this.postings);
    }
    removeWord(term) {
        return this.removeWordWithPostings(term, this.postings);
    }
    removeDocumentByWord(term, docID, exact = true) {
        return this.removeDocumentByWordWithPostings(term, docID, this.postings, exact);
    }
    getDocumentFrequency(term) {
        return (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$trees$2f$postings$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["getDocumentFrequency"])(this.postings, term);
    }
    toJSON() {
        return {
            ...this.toNodeJSON(),
            postings: (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$trees$2f$postings$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["serializePostingsMap"])(this.postings)
        };
    }
    static fromJSON(json) {
        const tree = new RadixTree();
        tree.w = json.w;
        tree.s = json.s;
        tree.e = json.e;
        tree.k = json.k;
        tree.c = new Map(json.c?.map(([key, nodeJson])=>[
                key,
                RadixNode.fromNodeJSON(nodeJson)
            ]) || []);
        if (json.postings) {
            tree.postings = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$trees$2f$postings$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["deserializePostingsMap"])(json.postings);
        } else {
            (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$trees$2f$postings$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["collectLegacyNodePostings"])(json, tree.postings);
        }
        hydrateNodePostings(tree, tree.postings);
        return tree;
    }
}
function hydrateNodePostings(node, postings) {
    if (node.e) {
        const list = postings.get(node.w);
        if (list) {
            node.d = list;
        }
    }
    for (const child of node.c.values()){
        hydrateNodePostings(child, postings);
    }
}
}),
"[project]/node_modules/zbsearch/dist/browser/trees/vector-index.js [app-client] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "createFlatVectorIndexFactory",
    ()=>createFlatVectorIndexFactory,
    "defaultFlatVectorIndexFactory",
    ()=>defaultFlatVectorIndexFactory,
    "deserializeVectorIndex",
    ()=>deserializeVectorIndex,
    "resolveVectorIndexConfig",
    ()=>resolveVectorIndexConfig,
    "resolveVectorIndexFactory",
    ()=>resolveVectorIndexFactory
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$constants$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/zbsearch/dist/browser/constants.js [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$errors$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/zbsearch/dist/browser/errors.js [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$trees$2f$vector$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__$3c$locals$3e$__ = __turbopack_context__.i("[project]/node_modules/zbsearch/dist/browser/trees/vector.js [app-client] (ecmascript) <locals>");
;
;
;
function createFactory(kind, create, fromJSON) {
    return Object.assign(create, {
        kind,
        fromJSON
    });
}
function createFlatVectorIndexFactory() {
    return createFactory("flat", (ctx)=>new __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$trees$2f$vector$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__$3c$locals$3e$__["VectorIndex"](ctx.dim), (json)=>__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$trees$2f$vector$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__$3c$locals$3e$__["VectorIndex"].fromJSON(json));
}
const defaultFlatVectorIndexFactory = createFlatVectorIndexFactory();
function resolveVectorIndexFactory(property, indexes) {
    if (!indexes) {
        return defaultFlatVectorIndexFactory;
    }
    const propertyConfig = property === __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$constants$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["RESERVED_VECTOR_INDEX_KEY"] ? undefined : indexes[property];
    if (propertyConfig !== undefined) {
        return resolveVectorIndexConfig(propertyConfig);
    }
    const defaultConfig = indexes[__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$constants$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["RESERVED_VECTOR_INDEX_KEY"]];
    if (defaultConfig !== undefined) {
        return resolveVectorIndexConfig(defaultConfig);
    }
    return defaultFlatVectorIndexFactory;
}
function resolveVectorIndexConfig(config) {
    if (config === "flat") {
        return defaultFlatVectorIndexFactory;
    }
    return config;
}
function deserializeVectorIndex(property, raw, indexes) {
    const payload = raw;
    const kind = payload.kind ?? "flat";
    const factory = resolveVectorIndexFactory(property, indexes);
    if (kind === "ivf" && factory.kind !== "ivf") {
        throw (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$errors$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["createError"])("IVF_INDEX_REQUIRES_FACTORY", property, property);
    }
    return factory.fromJSON(raw);
}
}),
"[project]/node_modules/zbsearch/dist/browser/trees/vector-math.js [app-client] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "dotProduct",
    ()=>dotProduct,
    "getMagnitude",
    ()=>getMagnitude,
    "nearestCentroidIndex",
    ()=>nearestCentroidIndex,
    "normalizeVector",
    ()=>normalizeVector
]);
function getMagnitude(vector, vectorLength) {
    let magnitude = 0;
    for(let i = 0; i < vectorLength; i++){
        magnitude += vector[i] * vector[i];
    }
    return Math.sqrt(magnitude);
}
function normalizeVector(vector, vectorLength) {
    let magnitudeSq = 0;
    for(let i = 0; i < vectorLength; i++){
        magnitudeSq += vector[i] * vector[i];
    }
    const magnitude = Math.sqrt(magnitudeSq);
    if (magnitude === 0) {
        return 0;
    }
    const invMagnitude = 1 / magnitude;
    for(let i = 0; i < vectorLength; i++){
        vector[i] *= invMagnitude;
    }
    return magnitude;
}
function dotProduct(targetVector, vector, length) {
    let sum = 0;
    let i = 0;
    const limit = length - 3;
    for(; i < limit; i += 4){
        sum += targetVector[i] * vector[i] + targetVector[i + 1] * vector[i + 1] + targetVector[i + 2] * vector[i + 2] + targetVector[i + 3] * vector[i + 3];
    }
    for(; i < length; i++){
        sum += targetVector[i] * vector[i];
    }
    return sum > 1 ? 1 : sum < -1 ? -1 : sum;
}
function nearestCentroidIndex(vector, centroids, length) {
    let bestIndex = 0;
    let bestScore = -Infinity;
    for(let i = 0; i < centroids.length; i++){
        const score = dotProduct(vector, centroids[i], length);
        if (score > bestScore) {
            bestScore = score;
            bestIndex = i;
        }
    }
    return bestIndex;
}
}),
"[project]/node_modules/zbsearch/dist/browser/trees/vector.js [app-client] (ecmascript) <locals>", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "DEFAULT_SIMILARITY",
    ()=>DEFAULT_SIMILARITY,
    "VectorIndex",
    ()=>VectorIndex,
    "findSimilarVectors",
    ()=>findSimilarVectors,
    "flat",
    ()=>flat
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$trees$2f$vector$2d$math$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/zbsearch/dist/browser/trees/vector-math.js [app-client] (ecmascript)");
;
const DEFAULT_SIMILARITY = .8;
;
class VectorIndex {
    size;
    vectors = new Map();
    constructor(size){
        this.size = size;
    }
    add(internalDocumentId, value) {
        const stored = new Float32Array(value);
        (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$trees$2f$vector$2d$math$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["normalizeVector"])(stored, this.size);
        this.vectors.set(internalDocumentId, [
            1,
            stored
        ]);
    }
    remove(internalDocumentId) {
        this.vectors.delete(internalDocumentId);
    }
    find(vector, similarity, whereFiltersIDs) {
        const queryVector = new Float32Array(vector);
        if ((0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$trees$2f$vector$2d$math$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["normalizeVector"])(queryVector, this.size) === 0) {
            return [];
        }
        return findSimilarVectors(queryVector, whereFiltersIDs, this.vectors, this.size, similarity);
    }
    toJSON() {
        const vectors = [];
        for (const [id, [magnitude, vector]] of this.vectors){
            vectors.push([
                id,
                [
                    magnitude,
                    Array.from(vector)
                ]
            ]);
        }
        return {
            kind: "flat",
            size: this.size,
            vectors
        };
    }
    static fromJSON(json) {
        const raw = json;
        const index = new VectorIndex(raw.size);
        for (const [id, [, vector]] of raw.vectors){
            const stored = new Float32Array(vector);
            (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$trees$2f$vector$2d$math$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["normalizeVector"])(stored, raw.size);
            index.vectors.set(id, [
                1,
                stored
            ]);
        }
        return index;
    }
}
function flat() {
    return Object.assign((ctx)=>new VectorIndex(ctx.dim), {
        kind: "flat",
        fromJSON: VectorIndex.fromJSON
    });
}
function findSimilarVectors(targetVector, keys, vectors, length, threshold) {
    const similarVectors = [];
    if (keys) {
        for (const vectorId of keys){
            const entry = vectors.get(vectorId);
            if (!entry) {
                continue;
            }
            const score = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$trees$2f$vector$2d$math$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["dotProduct"])(targetVector, entry[1], length);
            if (score >= threshold) {
                similarVectors.push([
                    vectorId,
                    score
                ]);
            }
        }
        return similarVectors;
    }
    for (const [vectorId, [, vector]] of vectors){
        const score = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$trees$2f$vector$2d$math$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["dotProduct"])(targetVector, vector, length);
        if (score >= threshold) {
            similarVectors.push([
                vectorId,
                score
            ]);
        }
    }
    return similarVectors;
}
}),
"[project]/node_modules/zbsearch/dist/browser/types.js [app-client] (ecmascript) <locals>", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "kInsertions",
    ()=>kInsertions,
    "kRemovals",
    ()=>kRemovals
]);
;
const kInsertions = Symbol("zbsearch.insertions");
const kRemovals = Symbol("zbsearch.removals");
}),
"[project]/node_modules/zbsearch/dist/browser/utils.js [app-client] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "MAX_ARGUMENT_FOR_STACK",
    ()=>MAX_ARGUMENT_FOR_STACK,
    "convertDistanceToMeters",
    ()=>convertDistanceToMeters,
    "flattenObject",
    ()=>flattenObject,
    "formatBytes",
    ()=>formatBytes,
    "formatNanoseconds",
    ()=>formatNanoseconds,
    "getDocumentProperties",
    ()=>getDocumentProperties,
    "getNanosecondTimeViaPerformance",
    ()=>getNanosecondTimeViaPerformance,
    "getNanosecondsTime",
    ()=>getNanosecondsTime,
    "getNested",
    ()=>getNested,
    "getOwnProperty",
    ()=>getOwnProperty,
    "getTokenFrequency",
    ()=>getTokenFrequency,
    "insertSortedValue",
    ()=>insertSortedValue,
    "intersect",
    ()=>intersect,
    "isAsyncFunction",
    ()=>isAsyncFunction,
    "isInsideNode",
    ()=>isInsideNode,
    "isInsideWebWorker",
    ()=>isInsideWebWorker,
    "isPromise",
    ()=>isPromise,
    "isServer",
    ()=>isServer,
    "removeVectorsFromHits",
    ()=>removeVectorsFromHits,
    "safeArrayPush",
    ()=>safeArrayPush,
    "setDifference",
    ()=>setDifference,
    "setIntersection",
    ()=>setIntersection,
    "setUnion",
    ()=>setUnion,
    "sleep",
    ()=>sleep,
    "sortTokenScorePredicate",
    ()=>sortTokenScorePredicate,
    "sprintf",
    ()=>sprintf,
    "uniqueId",
    ()=>uniqueId,
    "yieldToEventLoop",
    ()=>yieldToEventLoop
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$errors$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/zbsearch/dist/browser/errors.js [app-client] (ecmascript)");
;
const baseId = Date.now().toString().slice(5);
let lastId = 0;
const k = 1024;
const nano = BigInt(1e3);
const milli = BigInt(1e6);
const second = BigInt(1e9);
function getNodeProcess() {
    return globalThis.process;
}
const isServer = typeof window === "undefined";
const MAX_ARGUMENT_FOR_STACK = 65535;
function safeArrayPush(arr, newArr) {
    if (newArr.length < MAX_ARGUMENT_FOR_STACK) {
        Array.prototype.push.apply(arr, newArr);
    } else {
        const newArrLength = newArr.length;
        for(let i = 0; i < newArrLength; i += MAX_ARGUMENT_FOR_STACK){
            Array.prototype.push.apply(arr, newArr.slice(i, i + MAX_ARGUMENT_FOR_STACK));
        }
    }
}
function sprintf(template, ...args) {
    return template.replace(/%(?:(?<position>\d+)\$)?(?<width>-?\d*\.?\d*)(?<type>[dfs])/g, function(...replaceArgs) {
        const groups = replaceArgs[replaceArgs.length - 1];
        const { width: rawWidth, type, position } = groups;
        const replacement = position ? args[Number.parseInt(position) - 1] : args.shift();
        const width = rawWidth === "" ? 0 : Number.parseInt(rawWidth);
        switch(type){
            case "d":
                return replacement.toString().padStart(width, "0");
            case "f":
                {
                    let value = replacement;
                    const [padding, precision] = rawWidth.split(".").map((w)=>Number.parseFloat(w));
                    if (typeof precision === "number" && precision >= 0) {
                        value = value.toFixed(precision);
                    }
                    return typeof padding === "number" && padding >= 0 ? value.toString().padStart(width, "0") : value.toString();
                }
            case "s":
                return width < 0 ? replacement.toString().padEnd(-width, " ") : replacement.toString().padStart(width, " ");
            default:
                return replacement;
        }
    });
}
function formatBytes(bytes, decimals = 2) {
    if (bytes === 0) {
        return "0 Bytes";
    }
    const dm = decimals < 0 ? 0 : decimals;
    const sizes = [
        "Bytes",
        "KB",
        "MB",
        "GB",
        "TB",
        "PB",
        "EB",
        "ZB",
        "YB"
    ];
    const i = Math.floor(Math.log(bytes) / Math.log(k));
    return `${parseFloat((bytes / Math.pow(k, i)).toFixed(dm))} ${sizes[i]}`;
}
function isInsideWebWorker() {
    // @ts-expect-error - WebWorker global scope
    return typeof WorkerGlobalScope !== "undefined" && self instanceof WorkerGlobalScope;
}
function isInsideNode() {
    const nodeProcess = getNodeProcess();
    return nodeProcess?.release?.name === "node";
}
function getNanosecondTimeViaPerformance() {
    return BigInt(Math.floor(performance.now() * 1e6));
}
function formatNanoseconds(value) {
    if (typeof value === "number") {
        value = BigInt(value);
    }
    if (value < nano) {
        return `${value}ns`;
    } else if (value < milli) {
        return `${value / nano}μs`;
    } else if (value < second) {
        return `${value / milli}ms`;
    }
    return `${value / second}s`;
}
function getNanosecondsTime() {
    if (isInsideWebWorker()) {
        return getNanosecondTimeViaPerformance();
    }
    if (isInsideNode()) {
        const nodeProcess = getNodeProcess();
        if (nodeProcess?.hrtime?.bigint) {
            return nodeProcess.hrtime.bigint();
        }
    }
    const nodeProcess = getNodeProcess();
    if (typeof nodeProcess?.hrtime?.bigint === "function") {
        return nodeProcess.hrtime.bigint();
    }
    if (typeof performance !== "undefined") {
        return getNanosecondTimeViaPerformance();
    }
    // @todo: fallback to V8 native method to get microtime
    return BigInt(0);
}
function uniqueId() {
    return `${baseId}-${lastId++}`;
}
function getOwnProperty(object, property) {
    // Checks if `hasOwn` method is defined avoiding errors with older Node.js versions
    if (Object.hasOwn === undefined) {
        return Object.prototype.hasOwnProperty.call(object, property) ? object[property] : undefined;
    }
    return Object.hasOwn(object, property) ? object[property] : undefined;
}
function getTokenFrequency(token, tokens) {
    let count = 0;
    for (const t of tokens){
        if (t === token) {
            count++;
        }
    }
    return count;
}
function insertSortedValue(arr, el, compareFn = sortTokenScorePredicate) {
    let low = 0;
    let high = arr.length;
    let mid;
    while(low < high){
        mid = low + high >>> 1;
        if (compareFn(el, arr[mid]) < 0) {
            high = mid;
        } else {
            low = mid + 1;
        }
    }
    arr.splice(low, 0, el);
    return arr;
}
function sortTokenScorePredicate(a, b) {
    if (b[1] === a[1]) {
        return a[0] - b[0];
    }
    return b[1] - a[1];
}
function intersect(arrays) {
    if (arrays.length === 0) {
        return [];
    } else if (arrays.length === 1) {
        return arrays[0];
    }
    for(let i = 1; i < arrays.length; i++){
        if (arrays[i].length < arrays[0].length) {
            const tmp = arrays[0];
            arrays[0] = arrays[i];
            arrays[i] = tmp;
        }
    }
    const set = new Map();
    for (const elem of arrays[0]){
        set.set(elem, 1);
    }
    for(let i = 1; i < arrays.length; i++){
        let found = 0;
        for (const elem of arrays[i]){
            const count = set.get(elem);
            if (count === i) {
                set.set(elem, count + 1);
                found++;
            }
        }
        if (found === 0) return [];
    }
    return arrays[0].filter((e)=>{
        const count = set.get(e);
        if (count !== undefined) set.set(e, 0);
        return count === arrays.length;
    });
}
function getDocumentProperties(doc, paths) {
    const properties = {};
    const pathsLength = paths.length;
    for(let i = 0; i < pathsLength; i++){
        const path = paths[i];
        const pathTokens = path.split(".");
        let current = doc;
        const pathTokensLength = pathTokens.length;
        for(let j = 0; j < pathTokensLength; j++){
            current = current[pathTokens[j]];
            // We found an object but we were supposed to be done
            if (typeof current === "object") {
                if (current !== null && "lat" in current && "lon" in current && typeof current.lat === "number" && typeof current.lon === "number") {
                    current = properties[path] = current;
                    break;
                } else if (!Array.isArray(current) && current !== null && j === pathTokensLength - 1) {
                    current = undefined;
                    break;
                }
            } else if ((current === null || typeof current !== "object") && j < pathTokensLength - 1) {
                // We can't recurse anymore but we were supposed to
                current = undefined;
                break;
            }
        }
        if (typeof current !== "undefined") {
            properties[path] = current;
        }
    }
    return properties;
}
function getNested(obj, path) {
    const props = getDocumentProperties(obj, [
        path
    ]);
    return props[path];
}
function flattenObject(obj, prefix = "") {
    const result = {};
    for(const key in obj){
        const prop = `${prefix}${key}`;
        const objKey = obj[key];
        if (typeof objKey === "object" && objKey !== null) {
            Object.assign(result, flattenObject(objKey, `${prop}.`));
        } else {
            result[prop] = objKey;
        }
    }
    return result;
}
const mapDistanceToMeters = {
    cm: .01,
    m: 1,
    km: 1e3,
    ft: .3048,
    yd: .9144,
    mi: 1609.344
};
function convertDistanceToMeters(distance, unit) {
    const ratio = mapDistanceToMeters[unit];
    if (ratio === undefined) {
        throw new Error((0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zbsearch$2f$dist$2f$browser$2f$errors$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["createError"])("INVALID_DISTANCE_SUFFIX", distance).message);
    }
    return distance * ratio;
}
function removeVectorsFromHits(searchResult, vectorProperties) {
    for (const result of searchResult.hits){
        const document = result.document;
        for (const prop of vectorProperties){
            const path = prop.split(".");
            const lastKey = path.pop();
            let obj = document;
            for (const key of path){
                if (obj == null) {
                    break;
                }
                obj[key] = obj[key] ?? {};
                obj = obj[key];
            }
            if (obj != null) {
                obj[lastKey] = null;
            }
        }
    }
}
function isPromise(obj) {
    return !!obj && (typeof obj === "object" || typeof obj === "function") && typeof obj.then === "function";
}
function isAsyncFunction(func) {
    if (Array.isArray(func)) {
        return func.some((item)=>isAsyncFunction(item));
    }
    return func?.constructor?.name === "AsyncFunction";
}
const withIntersection = "intersection" in new Set();
function setIntersection(...sets) {
    // Fast path 1
    if (sets.length === 0) {
        return new Set();
    }
    // Fast path 2
    if (sets.length === 1) {
        return sets[0];
    }
    // Fast path 3
    if (sets.length === 2) {
        const set1 = sets[0];
        const set2 = sets[1];
        if (withIntersection) {
            return set1.intersection(set2);
        }
        const result = new Set();
        const base = set1.size < set2.size ? set1 : set2;
        const other = base === set1 ? set2 : set1;
        for (const value of base){
            if (other.has(value)) {
                result.add(value);
            }
        }
        return result;
    }
    // Slow path
    // Find the smallest set
    const min = {
        index: 0,
        size: sets[0].size
    };
    for(let i = 1; i < sets.length; i++){
        if (sets[i].size < min.size) {
            min.index = i;
            min.size = sets[i].size;
        }
    }
    if (withIntersection) {
        let base = sets[min.index];
        for(let i = 0; i < sets.length; i++){
            if (i === min.index) {
                continue;
            }
            base = base.intersection(sets[i]);
        }
        return base;
    }
    // manual implementation:
    // intersect all sets with the smallest set
    const base = sets[min.index];
    for(let i = 0; i < sets.length; i++){
        if (i === min.index) {
            continue;
        }
        const other = sets[i];
        for (const value of base){
            if (!other.has(value)) {
                base.delete(value);
            }
        }
    }
    return base;
}
const withUnion = "union" in new Set();
function setUnion(set1, set2) {
    if (withUnion) {
        if (set1) {
            return set1.union(set2);
        }
        return set2;
    }
    if (!set1) {
        return new Set(set2);
    }
    return new Set([
        ...set1,
        ...set2
    ]);
}
function setDifference(set1, set2) {
    const result = new Set();
    for (const value of set1){
        if (!set2.has(value)) {
            result.add(value);
        }
    }
    return result;
}
function sleep(ms) {
    if (typeof SharedArrayBuffer !== "undefined" && typeof Atomics !== "undefined") {
        const nil = new Int32Array(new SharedArrayBuffer(4));
        const valid = ms > 0 && ms < Infinity;
        if (valid === false) {
            if (typeof ms !== "number" && typeof ms !== "bigint") {
                throw TypeError("sleep: ms must be a number");
            }
            throw RangeError("sleep: ms must be a number that is greater than 0 but less than Infinity");
        }
        Atomics.wait(nil, 0, 0, Number(ms));
    } else {
        const valid = ms > 0 && ms < Infinity;
        if (valid === false) {
            if (typeof ms !== "number" && typeof ms !== "bigint") {
                throw TypeError("sleep: ms must be a number");
            }
            throw RangeError("sleep: ms must be a number that is greater than 0 but less than Infinity");
        }
        const target = Date.now() + Number(ms);
        while(target > Date.now()){}
    }
}
const pendingYields = [];
let yieldChannel;
function drainPendingYields() {
    pendingYields.shift()?.();
}
function yieldToEventLoop() {
    const scheduler = globalThis.scheduler;
    if (typeof scheduler?.yield === "function") {
        return scheduler.yield();
    }
    const setImmediateFn = globalThis.setImmediate;
    if (typeof setImmediateFn === "function") {
        return new Promise((resolve)=>{
            setImmediateFn(resolve);
        });
    }
    if (typeof MessageChannel !== "undefined") {
        if (!yieldChannel) {
            yieldChannel = new MessageChannel();
            yieldChannel.port1.onmessage = drainPendingYields;
        }
        return new Promise((resolve)=>{
            pendingYields.push(resolve);
            yieldChannel.port2.postMessage(null);
        });
    }
    return new Promise((resolve)=>{
        setTimeout(resolve, 0);
    });
}
}),
]);

//# sourceMappingURL=node_modules_zbsearch_dist_browser_0g-g04a._.js.map