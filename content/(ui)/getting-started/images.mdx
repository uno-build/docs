---
title: Images
---

## Load your first image

Use the `resources` instance created with `ResourcesWebGPU.create()` to register an image before displaying it in your layout.

```js
const image_url = 'assets/coin.png'
const response = await fetch(image_url)
const blob = await response.blob()
const image = await createImageBitmap(blob)

resources.registerImage(image_url, { image })

ui.root.style('backgroundImage', image_url)
ui.root.style('backgroundSize', '32px 32px')
ui.root.style('backgroundRepeat', 'repeat')
```

- `resources.registerImage()` requires two parameters. The first is the asset identifier, which is also passed to `backgroundImage`. The second is an object containing the source bitmap.

## Prevent atlas bleeding

Images share a texture atlas. With linear filtering, small icons or enlarged images can pick up unwanted colors around their edges. Enable `preventBleeding` when registering an affected image to copy its edge pixels into the atlas padding:

```js
resources.registerImage(image_url, { image, preventBleeding: true })
```

Use this in place of the registration above; registering the same key twice throws.

## Demo

<iframe
  src="https://codesandbox.io/embed/x5dj2k?view=preview&initialpath=%2F/images.html&module=%2Fimages.html&hidenavigation=1&hidedevtools=1"
  style={{
    width: '100%',
    height: '350px',
    border: 0,
    borderRadius: '4px',
    overflow: 'hidden',
  }}
  title="uno-docs"
  allow="accelerometer; ambient-light-sensor; camera; encrypted-media; geolocation; gyroscope; hid; microphone; midi; payment; usb; vr; xr-spatial-tracking"
  sandbox="allow-forms allow-modals allow-popups allow-presentation allow-same-origin allow-scripts"
></iframe>
